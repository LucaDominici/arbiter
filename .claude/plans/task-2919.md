---
context:
  issue: '#2919'
  type: fix
  pipeline: 'plan → red → green → refactor → review → close'
  branch_convention: 'task/#2919-gate-mutex-flake'
  base_branch: main
  key_constraints:
    - 'edit only __tests__/scripts/gate-mutex.test.ts (plus the new RED test); no change to scripts/lib/gate-mutex.mjs or its template twin'
    - 'fixture git calls run hermetic: no GIT_* from parent, GIT_CONFIG_NOSYSTEM=1, GIT_CONFIG_GLOBAL=/dev/null, core.hooksPath=/dev/null'
    - 'raise a timeout only with a measurement in the PR'
  red_team_warnings:
    - 'the cause may be in gate-mutex.mjs itself; if so stop and report (template to bake change)'
    - 'a parent GIT_DIR or GIT_CONFIG_* leaking into the fixture repo runs the real repo hooks'
  estimate: 'S'
premortem: .claude/plans/PREMORTEM_2919.md
files:
  - .claude/plans/PREMORTEM_2919.md
  - __tests__/scripts/gate-mutex.test.ts
  - __tests__/scripts/gate-mutex-hermetic-2919.test.ts
---

# Task #2919 — gate-mutex fixture flake

## Acceptance criteria

- AC-2919.1: The test is deterministic under CI load. Its git setup uses bounded, minimal work (for example a single shared fixture repo), or it has a per-test timeout justified by a measured p99. It never depends on the 30 s default under contention.
- AC-2919.2: 20 consecutive runs of the file under `--pool=forks` on a loaded runner show no timeout. Record the measurement in the PR.

## Non-goals

- No change to `scripts/lib/gate-mutex.mjs` or its template twin.
