---
context:
  issue: '#2938'
  type: refactor
  pipeline: 'plan → red → green → refactor → review → close'
  branch_convention: 'task/#2938-cut-audit-run'
  base_branch: main
  key_constraints:
    - 'CUT: remove exactly `arbiter audit run` (template `## Rerun command` section + the "planned, not yet shipped" bullet); src delta < 0'
    - 'gdpr-overlay.md names check-gdpr-controls.mjs but never the literal scripts/ path; no scanner change'
  red_team_warnings:
    - 'The playbook is rendered by no generator: do not add a generator, bake or examples change'
    - 'Keep doc-gate-allowlist-2917.test.ts; it now proves the gates pass with entries: []'
  estimate: 'XS'
premortem: .claude/plans/PREMORTEM_2938.md
files:
  - .claude/plans/task-2938.md
  - .claude/plans/PREMORTEM_2938.md
  - src/templates/governance/qa-audit-phases.md.ejs
  - docs/REFERENCE/gdpr-overlay.md
  - scripts/data/doc-gate-allowlist.json
  - __tests__/templates/governance-render.test.ts
  - __tests__/scripts/doc-gate-cut-2938.test.ts
  - .arbiter/evidence/tdd/#2938.json
  - .arbiter/evidence/ac-fit/2938.json
---

# #2938 — CUT `arbiter audit run` from the playbook; gdpr doc stops citing the consumer path

Resolves the two residuals left open on #2917 (closed). One mechanism removed; `src/` delta negative.

## Acceptance Criteria

- AC-2938.1: The governance playbook template (`src/templates/governance/qa-audit-phases.md.ejs`) no longer documents `arbiter audit run`, and no pin test requires it.
- AC-2938.2: `docs/REFERENCE/gdpr-overlay.md` no longer cites the consumer-only `scripts/check-gdpr-controls.mjs` path literally.
- AC-2938.3: `scripts/data/doc-gate-allowlist.json` holds no #2917 entries. `check-doc-links`, `check-doc-style` and `check-phantom-command-scan` pass without them, which removes the 2026-10-10 expiry.

## Non-goals

- Changing the phantom-command scanner or teaching it emitted-tree resolution.
- Deleting or wiring the orphan playbook template; adding generator, bake or examples changes.
- Touching #2917's frozen plan, premortem or evidence.
