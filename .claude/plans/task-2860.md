context:
  issue: '#2860'
  type: fix
  pipeline: 'plan → impl → gate → PR'
  branch_convention: 'task/#2860-refutation-quorum'
  base_branch: main
  key_constraints:
    - 'No new src/ file; every new helper stays module-local (publicApi baseline 1174, no `export` at line start).'
    - 'No threshold, baseline or tolerance change.'
    - 'No relaxing any existing exit-code contract (0/1/2 preserved); #2614 AC-2 vacuous pass stays reachable.'
    - 'scripts/check-refutation-verdicts.mjs and its .ejs twin must stay byte-identical (cmp).'
  red_team_warnings:
    - 'A bad/duplicate corpus must not silently pick markers[0] once a task identity is declared.'
    - 'One skeptic envelope repeating the same verdict N times must not satisfy quorum on its own.'
    - '--require-marker semantics (exit 1 missing, exit 2 malformed) must stay unchanged; the two flags disagreeing is exit 2.'
  estimate: 'S (4h)'
files:
  - .arbiter/evidence/tdd/#2860.json
  - .claude/plans/task-2860.md
  - scripts/check-refutation-verdicts.mjs
  - src/templates/scripts/check-refutation-verdicts.mjs.ejs
  - __tests__/scripts/check-refutation-verdicts.test.ts
acceptance_criteria:
  AC-2860.1: >
    A03-003 — with a task identity declared (via `.claude/.task/status.json` and `--repo-root`),
    the checker selects the marker BOUND to that declared task, not `markers[0]`. A clean earlier
    marker for another task never hides a later failing marker for the declared task.
  AC-2860.2: >
    A03-003 — with two or more refutation markers present and no task identity available (no
    status.json, or an unknown/null taskId), the checker refuses to pick one: exit 2, stderr
    matches `/2 refutation markers .* no task identity/`. With exactly one marker present and no
    identity, existing markers[0] behaviour is unchanged.
  AC-2860.3: >
    A03-004 — quorum counts distinct skeptic envelopes, not raw refutation entries. One envelope
    repeating the same verdict on a finding 3 times counts as 1 vote; a self-contradicting
    envelope (UPHELD + REFUTED on the same target within one envelope) counts as 0.
  AC-2860.4: >
    Declared-task selection composes with #2614: a declared task without a bound marker stays a
    vacuous pass (exit 0), and `--require-marker` for that same task still fails the requirement
    (exit 1) — the required-and-missing semantics of `--require-marker` are untouched.
  AC-2860.5: >
    `scripts/check-refutation-verdicts.mjs` and `src/templates/scripts/check-refutation-verdicts.mjs.ejs`
    stay byte-identical (verified with `cmp`); no diff to `scripts/check-all.mjs`,
    `src/templates/scripts/gate-registry.yml.ejs`, `src/generators/check-all.ts`, or the
    high-fanout snapshot.
non_goals:
  - No new review system, graph, or quorum-policy expansion.
  - No L2 hardness promotion.
  - No merge of #2579's malformed-control-marker boundary (stays separate).
  - No caller wiring in check-all.mjs / gate-registry.yml.ejs: the checker resolves its own
    declared task from `.claude/.task/status.json` via `--repo-root`, mirroring how
    check-review-completion.mjs and check-acceptance.mjs already do it. Deviates from the issue's
    "Expected files" list; stated as a deviation in the PR body.
  - Agent-identity dedupe across envelopes is out of scope; every envelope from the existing
    skeptic-dispatch helper already carries `agent: 'red-team'`. Noted as a follow-up in the PR.
premortem:
  - failure: 'Slice-5 fixtures flip because the live repo has a status.json (#2550) under .claude/.task/'
    prevention: '`run()` passes `--repo-root tmpDir`; add `writeStatus(taskId)` writing tmpDir/.claude/.task/status.json'
  - failure: 'Threads --require-marker unconditionally, regressing #2614 AC-2'
    prevention: 'Declared-task selection is a separate code path from --require-marker; vacuous-pass test for a declared task without a marker (#300)'
  - failure: 'Reviewer demands caller wiring per the issue text'
    prevention: 'PR body states the deviation and why (zero caller diff, same behaviour, no new API)'
  - failure: 'Template twin drift (.mjs vs .ejs)'
    prevention: 'cp + cmp in the same commit; check-self-dogfood R-02'
  - failure: 'Reviewer widens scope to pre-existing main behaviour'
    prevention: 'PR body / review threat model bounds scope to the delta; pre-existing issues are non-blocking'
