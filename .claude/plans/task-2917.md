---
context:
  issue: '#2917'
  type: fix
  pipeline: 'plan → red → green → refactor → review → close'
  branch_convention: 'task/#2917-allowlist-redate'
  base_branch: main
  key_constraints:
    - 'Only the two expired entries in scripts/data/doc-gate-allowlist.json change: expires 2026-10-10, issue #2917, reason states the open decision'
    - 'No scanner, doc or template change; the real fixes stay open on #2917'
  red_team_warnings:
    - 'Re-dating hides a real residual; the gate goes red again on 2026-10-11'
  estimate: 'XS'
files:
  - scripts/data/doc-gate-allowlist.json
  - __tests__/scripts/doc-gate-allowlist-2917.test.ts
  - .claude/plans/PREMORTEM_2917.md
premortem: .claude/plans/PREMORTEM_2917.md
---

# #2917 — re-date the two expired doc-gate allowlist entries

## Acceptance Criteria

- AC-2917.1: Both entries are re-dated to 2026-10-10 with this issue as owner, so `check-doc-links`, `check-doc-style` and `check-phantom-command-scan` pass again.
- AC-2917.2: The real fixes stay open here, one per residual, and each entry's `reason` states its remaining decision.

## Non-goals

- Fixing either residual (scanner resolving emitted-doc citations; shipping or dropping `arbiter audit run`).
