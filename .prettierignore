node_modules/
dist/
coverage/
.coverage-tmp/
report/
.arbiter/
.claude/worktrees/
# Worktrees are self-contained checkouts; the parent repo must not lint/track them.
arbiter.worktrees/
docs/PLAN/
# Python cache directories (#1686)
.pytest_cache/
__pycache__/
.mypy_cache/
__tests__/fixtures/real-projects/**/target/
__tests__/fixtures/real-projects/**/.pytest_cache/
docs/REFERENCE/coverage/dim-*.md
docs/REFERENCE/GLOBAL_KIT.md
docs/REFERENCE/SELF-KIT-AUDIT.md
docs/INDEX.md
# #2053: the RTM's test_ref / source_ref cells hold bare `__tests__/...` paths, and
# prettier reads leading `__` as markdown bold-underscore and rewrites them to
# `**tests**/...` — silently corrupting every path check-feature-matrix.mjs resolves
# (INV-112 goes red with a "glob test_ref" that is really mangled markdown). This file
# was protected only by the docs/internal/ blanket gitignore; that rule is now narrowed,
# so the protection has to be stated here instead of inherited by accident.
docs/internal/PRODUCT/FEATURE_MATRIX.md
# Generated slash command file — template system (ship.md.ejs) owns formatting
.claude/commands/ship.md
# #1721: generated agent-native root index — gen-llms-txt.mjs owns the bytes; no
# prettier parser exists for .txt (an explicit path would hard-error)
llms.txt
# ADR index (gen-adr-readme.mjs) and DECISIONS digest — generators own table formatting.
# #2053: the docs/* spellings below are arbiter's SELF files as they were BEFORE the SSOT
# move to docs/internal/, kept because the dogfood run emits a target tree at those paths.
# The docs/internal/* twins are the live self paths; they were protected only by the
# docs/internal/ blanket gitignore, so narrowing that rule left them unlisted and prettier
# reformatted them out of parity with their generators (INV-107 red).
docs/ADR/README.md
docs/SYSTEM/DECISIONS.md
docs/internal/ADR/README.md
docs/internal/SYSTEM/DECISIONS.md
# #1100: generated inventory region — gen-ssot-core.mjs owns formatting; keep
# prettier from fighting the generator over markdown in doc titles (*, _, |, etc.)
docs/METHOD/SSOT_CORE_SET.md
docs/internal/METHOD/SSOT_CORE_SET.md
# Generated LLM wiki (gen-wiki.mjs, INV-116) — the generator owns markdown
# formatting; check-wiki-lint.mjs verifies it. Keep prettier from fighting the
# generated tables/titles (pre-existing format-gate red on main, surfaced by #1304).
wiki/
.github/
graphify-out/
# tokensave MCP server local code-graph cache (regenerable, machine-local)
.tokensave/
# #1840 F4 (examples/ viventi): generated `arbiter init` OUTPUT for a downstream
# project, regenerated verbatim by scripts/regenerate-examples.mjs. Formatted
# under the TARGET project's own (copied) .prettierrc/.prettierignore, not
# arbiter's — checking it against arbiter's root config is a false positive,
# the same reasoning __tests__/fixtures/real-projects/**/target/ already gets.
examples/ts-library/
examples/python-library/
examples/go-library/
# Full-content approval snapshots (toMatchFileSnapshot) — the EXACT bytes
# `renderTemplate` produces, captured for review; must NOT be reformatted or
# the snapshot no longer matches its own source of truth. Same reasoning as
# the examples/ ignores above.
__tests__/templates/__snapshots__/high-fanout/

# #2221: byte-exact archive of a render arbiter once emitted for a now-retired
# path. Its sha256 IS the assertion (it must equal the hash pinned in
# src/state/retired-renders.ts), so reformatting it would silently void the
# proof this fixture exists to provide. Same reasoning as the approval snapshots.
__tests__/fixtures/retired-renders/

# Vendored copies of another repository's schemas (INV-143). These must stay BYTE-IDENTICAL to the
# file their owner ships — scripts/check-forma-contract.mjs hashes them against schemas/CONTRACT.json,
# so reformatting one silently breaks the pin. Caught exactly that way on 2026-09-02.
schemas/vendor/
