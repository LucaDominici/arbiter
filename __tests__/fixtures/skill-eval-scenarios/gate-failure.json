{
  "name": "gate-failure",
  "skill": "explain",
  "backend": "cli",
  "description": "An invalid request fails closed: `arbiter explain BOGUS-999` does not silently succeed — it reports the unknown code and exits non-zero. Regression guard against a gate/command that stops failing on bad input.",
  "command": "node",
  "args": ["dist/cli.js", "explain", "BOGUS-999"],
  "timeoutMs": 20000,
  "expect": {
    "exitCode": 1,
    "stderrMatches": "Unknown code"
  }
}
