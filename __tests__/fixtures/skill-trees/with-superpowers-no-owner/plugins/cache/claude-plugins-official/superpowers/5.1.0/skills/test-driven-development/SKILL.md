---
name: test-driven-development
description: Use when implementing any feature or bugfix, before writing implementation code
---

# Test-Driven Development

Mirrors a REAL upstream superpowers skill: `name` + `description` frontmatter only,
no arbiter-invented `pluginOwner` key. The owner must be derived from the install path.
