[package]
name = "rust-embedded-fixture"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_embedded_fixture"

[dependencies]
# no_std — uses only core crate, no std dependency

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
