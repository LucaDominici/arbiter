{
  "$schemaVersion": 1,
  "task_id": "#2673",
  "test_path": "__tests__/templates/05-release-render.test.ts",
  "test_commit_sha": "4137ee3957db438e971656ffb86f161b883838a1",
  "test_blob_sha": "5c419ec2c44147bb8b0e06a6e10517b2ec3d24a6",
  "test_run_log": "\n RUN  v4.1.11 .\n\n ❯ __tests__/templates/05-release-render.test.ts (236 tests | 6 failed) 1706ms\n     × 'typescript': sbom-attest job uses attest-blob, not attest (INV-92) 6ms\n     × 'java': sbom-attest job uses attest-blob, not attest (INV-92) 6ms\n     × 'go': sbom-attest job uses attest-blob, not attest (INV-92) 2ms\n     × 'python': sbom-attest job uses attest-blob, not attest (INV-92) 2ms\n     × 'rust': sbom-attest job uses attest-blob, not attest (INV-92) 2ms\n     × materialized: sbom-attest job uses attest-blob, not attest, and uploads the bundle (INV-92, #2673) 15ms\n\n Test Files  1 failed (1)\n      Tests  6 failed | 230 passed (236)\n   Start at  19:11:04\n   Duration  2.09s (transform 182ms, setup 9ms, import 236ms, tests 1.71s, environment 0ms)\n\n\n\n⎯⎯⎯⎯⎯⎯⎯ Failed Tests 6 ⎯⎯⎯⎯⎯⎯⎯\n\n FAIL  __tests__/templates/05-release-render.test.ts > 05-release.yml.ejs — structural invariants (CANON-18) > 'typescript': sbom-attest job uses attest-blob, not attest (INV-92)\nAssertionError: expected 'name: Release (T3)\\n\\n# E3: productiv…' to contain 'sbom-attest:'\n\n- Expected\n+ Received\n\n- sbom-attest:\n+ name: Release (T3)\n+\n+ # E3: productive jobs run only on tag pushes; PR builds previously leaked through unguarded roots.\n+ # Manual npm authentication is separate and cannot reach the productive chain.\n+ on:\n+\n+   workflow_dispatch:\n+   push:\n+     tags:\n+       - 'v*'\n+       - '!v0.0.0-verify-*'\n+\n+ permissions:\n+   contents: read\n+\n+ concurrency:\n+   group: release-${{ github.ref }}\n+   cancel-in-progress: false\n+\n+ jobs:\n+\n+   npm-auth-smoke:\n+     name: Read-only npm authentication smoke\n+     if: github.event_name == 'workflow_dispatch'\n+     runs-on: ubuntu-latest\n+     timeout-minutes: 5\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+       - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0\n+         with:\n+           node-version-file: .nvmrc\n+           package-manager-cache: false\n+       - name: Check identity and scope visibility (publication remains unproven)\n+         id: npm-auth\n+         env:\n+           NPM_TOKEN: ${{ secrets.NPM_TOKEN }}\n+         run: |\n+           node --input-type=module <<'NODE'\n+           const token = process.env.NPM_TOKEN;\n+           if (!token) { console.error('NPM_TOKEN is missing'); process.exit(1); }\n+           const request = path => fetch('https://registry.npmjs.org' + path, {\n+             headers: { authorization: 'Bearer ' + token },\n+             redirect: 'error', signal: AbortSignal.timeout(15000)\n+           });\n+           try {\n+             const identity = await request('/-/whoami');\n+             if (!identity.ok) throw new Error('Authentication rejected');\n+             const { username } = await identity.json();\n+             if (typeof username !== 'string' || !username.trim() || username === token) throw new Error('Invalid identity');\n+             let scopeHttp = null, ownRole = 'UNPROVEN';\n+             try {\n+               const scope = await request('/-/org/arbiter/user');\n+               scopeHttp = scope.status;\n+               if (scope.ok) {\n+                 const members = await scope.json();\n+                 const role = members?.[username];\n+                 if (['owner', 'admin', 'developer'].includes(role)) ownRole = role;\n+               }\n+             } catch { /* Identity is proven; unavailable scope access is not. */ }\n+             console.log(JSON.stringify({ authentication: 'PASS', username, scopeHttp, ownRole, publicationAuthorization: 'NOT_PROVEN' }));\n+           } catch {\n+             console.error('npm authentication failed (missing, rejected, malformed or unavailable identity)');\n+             process.exit(1);\n+           }\n+           NODE\n+\n+\n+   build-superset:\n+     name: Build superset\n+\n+     if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')\n+\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_BUILD || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     outputs:\n+       hashes: ${{ steps.hash.outputs.hashes }}\n+     permissions:\n+       contents: read  # checkout only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       - name: Verify the tagged commit is reachable from the reviewed default branch (#2679)\n+         # (H) red-team finding 2: any `v*` tag reached this job — a tag pushed at an\n+         # unreviewed commit, or at a re-pointed tag object, still built/signed/published.\n+         # Fail closed unless GITHUB_SHA is an ancestor of origin/<default branch>, and\n+         # unless the pushed tag name itself resolves to that exact commit (works for both\n+         # annotated and lightweight tags — `^{commit}` dereferences an annotated tag\n+         # object, and is a no-op on a lightweight tag that already points at a commit).\n+         #\n+         # HONESTY NOTE (round 2, #2679): this step is DEFENSE IN DEPTH, not the enforcing\n+         # control. A workflow runs at the tagged commit's own content, so a commit that is\n+         # itself off the reviewed branch can simply omit or alter this step before its\n+         # author pushes the tag — the step cannot defend against a workflow file it does\n+         # not control. The actual enforcing control MUST be a repository tag ruleset that\n+         # restricts who may CREATE a `v*` tag to admins/a release team (GitHub rulesets,\n+         # evaluated server-side before the workflow ever runs, independent of what any\n+         # commit's .github/workflows/*.yml says). See docs/internal/release-playbook.md\n+         # for the `gh api` command that creates it; this in-workflow check only catches a\n+         # tag pushed by someone who already has that permission but targeted the wrong\n+         # commit, plus gives a fast, readable failure instead of a silent bypass.\n+         id: ancestry-check\n+         env:\n+           DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}\n+         run: |\n+           git fetch --no-tags origin \"+refs/heads/$DEFAULT_BRANCH:refs/remotes/origin/$DEFAULT_BRANCH\"\n+           git fetch --force origin \"+refs/tags/$GITHUB_REF_NAME:refs/tags/$GITHUB_REF_NAME\"\n+           TAG_COMMIT=$(git rev-parse \"refs/tags/$GITHUB_REF_NAME^{commit}\")\n+           if [ \"$TAG_COMMIT\" != \"$GITHUB_SHA\" ]; then\n+             echo \"::error::Tag $GITHUB_REF_NAME resolves to $TAG_COMMIT, not the checked-out commit $GITHUB_SHA — refusing to release.\"\n+             exit 1\n+           fi\n+           if ! git merge-base --is-ancestor \"$GITHUB_SHA\" \"refs/remotes/origin/$DEFAULT_BRANCH\"; then\n+             echo \"::error::$GITHUB_SHA is not reachable from origin/$DEFAULT_BRANCH — refusing to release an unreviewed commit.\"\n+             exit 1\n+           fi\n+           echo \"Tag $GITHUB_REF_NAME ($GITHUB_SHA) is reachable from origin/$DEFAULT_BRANCH.\"\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+\n+       - name: Verify release tag matches package version\n+         id: release-version\n+         env:\n+           RELEASE_TAG: ${{ github.ref_name }}\n+         run: |\n+           VERSION=$(node -e \"const v=require('./package.json').version; if(typeof v!=='string'||!v) process.exit(1); process.stdout.write(v)\")\n+           test \"$RELEASE_TAG\" = \"v$VERSION\" || { echo \"::error::Release tag does not match package.json\"; exit 1; }\n+\n+       - run: npm run build\n+\n+       - name: Package artifact (#2138 — pack ONCE, this exact tarball is signed and published)\n+         # #2138: all signatures and attestations follow this tarball through publication.\n+         run: |\n+           TARBALL=$(npm pack --silent --pack-destination .)\n+           mv \"$TARBALL\" release-artifact.tgz\n+\n+\n+       - name: Compute artifact hash\n+         id: hash\n+         run: |\n+           sha256sum release-artifact.tgz > sha256sums.txt\n+           echo \"hashes=$(base64 -w0 sha256sums.txt)\" >> \"$GITHUB_OUTPUT\"\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: release-artifact\n+           path: |\n+             release-artifact.tgz\n+             sha256sums.txt\n+           retention-days: 30\n+\n+   trivy-fs-scan:\n+     name: Trivy filesystem scan (CRITICAL block, INV-92)\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read  # checkout only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+       - name: Scan filesystem for CRITICAL vulnerabilities\n+         uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0\n+         with:\n+           scan-type: fs\n+           scan-ref: .\n+           format: table\n+           exit-code: '1'\n+           severity: CRITICAL\n+           trivyignores: .trivyignore\n+\n+   mutation-blocking:\n+     name: Mutation tests (consume nightly, fallback run)\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     # #1505: mutation and fail-on-empty block at every emitted level, including L1.\n+     continue-on-error: false\n+     permissions:\n+       contents: read  # checkout + tool build\n+       actions: read   # gh run list / gh api against the nightly mutation-deep run\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+       # #1502: consume fresh nightly mutation evidence to avoid a synchronous rerun.\n+       # Missing/stale evidence requires a blocking, nonempty fallback (#1505).\n+       - name: Consume latest nightly mutation-deep result\n+         id: nightly\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+           GH_REPO: ${{ github.repository }}\n+           DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}\n+           # Freshness window (days) for accepting a nightly mutation result; repo/org var override.\n+           MUTATION_NIGHTLY_MAX_AGE_DAYS: ${{ vars.MUTATION_NIGHTLY_MAX_AGE_DAYS || '7' }}\n+         run: |\n+           set -uo pipefail\n+           FRESH=false\n+           MAX_AGE=\"${MUTATION_NIGHTLY_MAX_AGE_DAYS:-7}\"\n+           RUN_JSON=$(gh run list --repo \"$GH_REPO\" --workflow 06-nightly.yml \\\n+             --branch \"$DEFAULT_BRANCH\" --status completed --limit 1 \\\n+             --json databaseId,createdAt 2>/dev/null || echo '[]')\n+           RUN_ID=$(printf '%s' \"$RUN_JSON\" | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d[0]['databaseId'] if d else '')\" 2>/dev/null || echo '')\n+           if [ -n \"$RUN_ID\" ]; then\n+             CREATED=$(printf '%s' \"$RUN_JSON\" | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d[0]['createdAt'] if d else '')\" 2>/dev/null || echo '')\n+             CONCLUSION=$(gh api \"repos/$GH_REPO/actions/runs/$RUN_ID/jobs\" \\\n+               --jq '.jobs[] | select(.name==\"Deep mutation testing\") | .conclusion' 2>/dev/null || echo '')\n+             AGE_DAYS=99999\n+             if [ -n \"$CREATED\" ]; then\n+               AGE_DAYS=$(( ( $(date -u +%s) - $(date -u -d \"$CREATED\" +%s) ) / 86400 ))\n+             fi\n+             echo \"Latest nightly run $RUN_ID: mutation-deep=${CONCLUSION:-unknown}, age=${AGE_DAYS}d (max ${MAX_AGE}d)\"\n+             if [ \"$CONCLUSION\" = \"success\" ] && [ \"$AGE_DAYS\" -le \"$MAX_AGE\" ]; then\n+               FRESH=true\n+               echo \"::notice::Release mutation gate satisfied by nightly mutation-deep (run $RUN_ID).\"\n+             fi\n+           else\n+             echo \"No completed nightly run found on $DEFAULT_BRANCH.\"\n+           fi\n+           echo \"fresh=$FRESH\" >> \"$GITHUB_OUTPUT\"\n+           [ \"$FRESH\" = \"true\" ] || echo \"No fresh nightly mutation result — running mutation synchronously as fallback.\"\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+         if: steps.nightly.outputs.fresh != 'true'\n+       # #2673: some suites spawn a child process against dist/ (e.g. __tests__/utils/fs.test.ts's\n+       # \"#2635 built provider contained reader\"). This job never built dist, so those tests failed\n+       # with an empty stdout even though T1's gate-full job (which builds first) passes them.\n+       - run: npm run build\n+         if: steps.nightly.outputs.fresh != 'true'\n+       - name: Stryker mutation (fallback — no fresh nightly)\n+         if: steps.nightly.outputs.fresh != 'true'\n+         # errexit + pipefail (GitHub bash default) make a below-threshold mutation score fail the\n+         # step; the tee feeds the fail-on-empty guard below (#1505).\n+         run: npx stryker run 2>&1 | tee mutation-fallback.log\n+\n+       # #1505: require a positive tool-specific mutant count; empty or unrecognized output fails closed.\n+       - name: Fail when the mutation run produced no mutants (#1505)\n+         if: steps.nightly.outputs.fresh != 'true'\n+         run: |\n+           if [ ! -s mutation-fallback.log ]; then\n+             echo \"::error::No mutation output captured — failing closed (an empty run is not a pass).\"\n+             exit 1\n+           fi\n+           if ! grep -Eqi 'tests per mutant|mutation score|[1-9][0-9]* mutants|Generated [1-9][0-9]* mutations|total is [1-9]|Caught [1-9]|KILLED|SURVIVED' mutation-fallback.log; then\n+             echo \"::error::Mutation run generated zero mutants — an empty run is not a pass (#1505).\"\n+             exit 1\n+           fi\n+           echo \"Mutation run produced mutants — fail-on-empty check passed.\"\n+\n+   secret-scan-history:\n+     name: Full-history secret scan\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read  # checkout (full history) only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       # #2100: stale /tmp/gitleaks.tmp on self-hosted runners breaks gitleaks-action retries.\n+       # ponytail: delete assumes one download per runner; concurrent jobs need the\n+       # curl -> $RUNNER_TEMP/bin installation used in 01-pr-fast.\n+       - name: Clear stale gitleaks download temp\n+         run: rm -f /tmp/gitleaks.tmp\n+       # #2673: gitleaks-action always attempts to upload a results.sarif artifact (regardless of\n+       # findings) via actions/upload-artifact's validateRootDirectory, which requires the SARIF\n+       # path to sit under the action runner's rootDirectory. On a self-hosted runner with\n+       # HOME=/root and the workspace under /tmp, that check throws even on a clean scan.\n+       # GITLEAKS_ENABLE_UPLOAD_ARTIFACT=false (verified against the pinned SHA's dist/index.js)\n+       # skips the upload path entirely; the scan itself stays blocking.\n+       - uses: gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e  # v3.0.0\n+         env:\n+           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}\n+           GITLEAKS_ENABLE_UPLOAD_ARTIFACT: 'false'\n+         with:\n+           args: '--log-opts --full-history'\n+\n+   sbom:\n+     name: Generate SBOM\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read  # checkout only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+       - name: Generate CycloneDX SBOM\n+         run: npm sbom --omit dev --package-lock-only --sbom-format cyclonedx > sbom.cdx.json\n+\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: sbom\n+           path: sbom.cdx.*\n+           retention-days: 90\n+\n+   cosign-sign:\n+     name: Sign artifact (cosign keyless)\n+     needs: [build-superset, mutation-blocking, secret-scan-history, trivy-fs-scan]\n+     if: github.event_name != 'pull_request'\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       # (M) red-team finding 6: this job only downloads an artifact, signs it and\n+       # uploads a bundle — no checkout, no push. id-token is the only grant it uses.\n+       id-token: write\n+     steps:\n+       - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: release-artifact\n+       - uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6  # v4.1.2\n+       - name: Sigstore preflight check\n+         run: |\n+           STATUS=$(curl -sf https://status.sigstore.dev/api/v2/status.json \\\n+             --max-time 10 | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d.get('status',{}).get('indicator','unknown'))\" 2>/dev/null || echo \"unknown\")\n+           if [[ \"$STATUS\" != \"none\" ]]; then\n+             echo \"::warning::Sigstore status is '${STATUS}' — cosign sign may fail\"\n+           fi\n+           echo \"sigstore-status=$STATUS\"\n+       - name: Sign artifact (keyless OIDC)\n+         run: cosign sign-blob --yes release-artifact.tgz --bundle release-artifact.tgz.bundle\n+\n+       - name: Verify signature (L2+ strict)\n+         run: |\n+           cosign verify-blob \\\n+             --bundle release-artifact.tgz.bundle \\\n+             --certificate-identity-regexp \".*\" \\\n+             --certificate-oidc-issuer \"https://token.actions.githubusercontent.com\" \\\n+             release-artifact.tgz\n+\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: cosign-bundle\n+           path: release-artifact.tgz.bundle\n+           retention-days: 90\n+\n+   slsa-provenance:\n+     name: SLSA provenance (L2 signed)\n+     needs: [build-superset]\n+     if: github.event_name != 'pull_request'\n+     permissions:\n+       id-token: write\n+       contents: write\n+       actions: read\n+     uses: slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@f7dd8c54c2067bafc12ca7a55595d5ee9b75204a  # v2.1.0\n+     with:\n+       base64-subjects: ${{ needs.build-superset.outputs.hashes }}\n+       # #2673: the generator halts by default on a private repo to avoid exposing the repo name\n+       # in the public Rekor transparency log. Opt in: the name is already public via the npm\n+       # package @getarbiter/cli's trusted-publisher metadata, so the entry reveals nothing new.\n+       private-repository: true\n+       # #2673: compile-generator: false (default) downloads a prebuilt release binary, which\n+       # requires the pinned ref to look like refs/tags/vX.Y.Z (\"Invalid ref: <sha>. Expected ref\n+       # of the form refs/tags/vX.Y.Z\" — generate-builder.sh). Incompatible with pinning this call\n+       # by commit SHA (this repo's convention for every action). Compile from the pinned SHA\n+       # instead (git checkout + go build, ~2 min slower per the input's own description).\n+       compile-generator: true\n+\n+   attest-build-provenance:\n+     name: Attest build provenance\n+     needs: [build-superset]\n+     if: github.event_name != 'pull_request'\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       id-token: write\n+       attestations: write\n+       contents: read\n+     steps:\n+       - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: release-artifact\n+       - uses: actions/attest-build-provenance@e8998f949152b193b063cb0ec769d69d929409be # v2.4.0\n+         with:\n+           subject-path: release-artifact.tgz\n+\n+\n+   publish-package:\n+     name: Publish package (lib)\n+     needs: [cosign-sign, attest-build-provenance, slsa-provenance, sbom, doc-freshness-gate]\n+     # #2138/#2624: tag-shaped refs alone also admit manual dispatch; require a push.\n+     if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')\n+\n+     runs-on: ubuntu-latest\n+\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read\n+       id-token: write  # required by `npm publish --provenance` (OIDC)\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           ref: ${{ github.ref }}\n+       - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: release-artifact\n+       - name: Verify the downloaded artifact is the one that was signed (#2138)\n+         # #2138: reject downloaded bytes that differ from the signed build artifact.\n+         run: sha256sum -c sha256sums.txt\n+\n+       - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0\n+         with:\n+           node-version-file: .nvmrc\n+           package-manager-cache: false\n+           registry-url: https://registry.npmjs.org\n+       # npm >=11.5.1 is required for OIDC trusted publishing (`npm publish --provenance`\n+       # with no token, https://docs.npmjs.com/trusted-publishers/); --ignore-scripts\n+       # because this only swaps the npm binary — no package lifecycle script should run.\n+       - run: npm install --global npm@11.16.0 --ignore-scripts\n+       - name: Verify release tag matches package version\n+         id: release-version\n+         env:\n+           RELEASE_TAG: ${{ github.ref_name }}\n+         run: |\n+           VERSION=$(tar -xOf release-artifact.tgz package/package.json | node -e \"const v=JSON.parse(require('node:fs').readFileSync(0,'utf8')).version; if(typeof v!=='string'||!v) process.exit(1); process.stdout.write(v)\")\n+           test \"$RELEASE_TAG\" = \"v$VERSION\" || { echo \"::error::Release tag does not match archive package.json\"; exit 1; }\n+       # (M) red-team finding 5, round 2: OIDC trusted publishing is the ONLY publish path.\n+       # No NPM_TOKEN/NODE_AUTH_TOKEN anywhere in this job, and no repo/org variable\n+       # re-enables one — a Write-settable CI variable gating a secret is not a control.\n+       # The trusted-publisher entry is configured and @getarbiter/cli has already shipped\n+       # through it; a failed OIDC publish is recovered by a manual `npm publish` from a\n+       # maintainer machine (docs/internal/release-playbook.md), never a CI-held token.\n+       - name: Publish to npm (with provenance, OIDC trusted publisher)\n+         run: npm publish --provenance --access public release-artifact.tgz\n+\n+\n+\n+   doc-freshness-gate:\n+     name: Per-doc freshness gate (T4, gold-doc-tranches-t3-t5.md §2.3 — release-tag blocking)\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 30\n+     permissions:\n+       contents: read  # checkout (full history) only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       - uses: ./.github/actions/setup-node-pnpm\n+       - name: Per-doc freshness audit — no stale gold doc ships in a tagged release\n+         run: node scripts/check-doc-freshness.mjs\n+\n+   release-required:\n+     name: Release gate\n+     needs:\n+       - mutation-blocking\n+       - secret-scan-history\n+       - trivy-fs-scan\n+       - sbom\n+       - cosign-sign\n+       - slsa-provenance\n+       - attest-build-provenance\n+       - doc-freshness-gate\n+\n+       - publish-package\n+\n+\n+     if: always() && github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')\n+\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions: {}  # no checkout; only reads job outcomes via toJSON(needs)\n+     steps:\n+       - name: Release gate summary\n+         id: release-results\n+         env:\n+           NEEDS_JSON: ${{ toJSON(needs) }}\n+         run: |\n+           jq -e 'length > 0 and all(.[]; .result == \"success\")' <<< \"$NEEDS_JSON\" >/dev/null\n+           echo \"Release checks completed.\"\n+           echo \"Signing, provenance, and mutation verified.\"\n+\n\n ❯ __tests__/templates/05-release-render.test.ts:86:24\n     84|     ({ language, buildTool }) => {\n     85|       const rendered = renderRelease({ language, buildTool })\n     86|       expect(rendered).toContain('sbom-attest:')\n       |                        ^\n     87|       const jobSection = rendered.split('sbom-attest:')[1]?.split(/\\n …\n     88|       expect(jobSection).toContain('cosign attest-blob')\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/6]⎯\n\n FAIL  __tests__/templates/05-release-render.test.ts > 05-release.yml.ejs — structural invariants (CANON-18) > 'java': sbom-attest job uses attest-blob, not attest (INV-92)\nAssertionError: expected 'name: Release (T3)\\n\\n# E3: productiv…' to contain 'sbom-attest:'\n\n- Expected\n+ Received\n\n- sbom-attest:\n+ name: Release (T3)\n+\n+ # E3: productive jobs run only on tag pushes; PR builds previously leaked through unguarded roots.\n+ # Manual npm authentication is separate and cannot reach the productive chain.\n+ on:\n+   push:\n+     tags:\n+       - 'v*'\n+       - '!v0.0.0-verify-*'\n+\n+ permissions:\n+   contents: read\n+\n+ concurrency:\n+   group: release-${{ github.ref }}\n+   cancel-in-progress: false\n+\n+ jobs:\n+\n+   build-superset:\n+     name: Build superset\n+\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_BUILD || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     outputs:\n+       hashes: ${{ steps.hash.outputs.hashes }}\n+     permissions:\n+       contents: read  # checkout only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       - name: Verify the tagged commit is reachable from the reviewed default branch (#2679)\n+         # (H) red-team finding 2: any `v*` tag reached this job — a tag pushed at an\n+         # unreviewed commit, or at a re-pointed tag object, still built/signed/published.\n+         # Fail closed unless GITHUB_SHA is an ancestor of origin/<default branch>, and\n+         # unless the pushed tag name itself resolves to that exact commit (works for both\n+         # annotated and lightweight tags — `^{commit}` dereferences an annotated tag\n+         # object, and is a no-op on a lightweight tag that already points at a commit).\n+         #\n+         # HONESTY NOTE (round 2, #2679): this step is DEFENSE IN DEPTH, not the enforcing\n+         # control. A workflow runs at the tagged commit's own content, so a commit that is\n+         # itself off the reviewed branch can simply omit or alter this step before its\n+         # author pushes the tag — the step cannot defend against a workflow file it does\n+         # not control. The actual enforcing control MUST be a repository tag ruleset that\n+         # restricts who may CREATE a `v*` tag to admins/a release team (GitHub rulesets,\n+         # evaluated server-side before the workflow ever runs, independent of what any\n+         # commit's .github/workflows/*.yml says). See docs/internal/release-playbook.md\n+         # for the `gh api` command that creates it; this in-workflow check only catches a\n+         # tag pushed by someone who already has that permission but targeted the wrong\n+         # commit, plus gives a fast, readable failure instead of a silent bypass.\n+         id: ancestry-check\n+         env:\n+           DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}\n+         run: |\n+           git fetch --no-tags origin \"+refs/heads/$DEFAULT_BRANCH:refs/remotes/origin/$DEFAULT_BRANCH\"\n+           git fetch --force origin \"+refs/tags/$GITHUB_REF_NAME:refs/tags/$GITHUB_REF_NAME\"\n+           TAG_COMMIT=$(git rev-parse \"refs/tags/$GITHUB_REF_NAME^{commit}\")\n+           if [ \"$TAG_COMMIT\" != \"$GITHUB_SHA\" ]; then\n+             echo \"::error::Tag $GITHUB_REF_NAME resolves to $TAG_COMMIT, not the checked-out commit $GITHUB_SHA — refusing to release.\"\n+             exit 1\n+           fi\n+           if ! git merge-base --is-ancestor \"$GITHUB_SHA\" \"refs/remotes/origin/$DEFAULT_BRANCH\"; then\n+             echo \"::error::$GITHUB_SHA is not reachable from origin/$DEFAULT_BRANCH — refusing to release an unreviewed commit.\"\n+             exit 1\n+           fi\n+           echo \"Tag $GITHUB_REF_NAME ($GITHUB_SHA) is reachable from origin/$DEFAULT_BRANCH.\"\n+\n+       - uses: ./.github/actions/setup-java-maven\n+\n+       - uses: gradle/actions/setup-gradle@ed408507eac070d1f99cc633dbcf757c94c7933a  # v4\n+       - run: ./gradlew build -x test\n+       - name: Package artifact\n+         run: zip -r release-artifact.zip build/libs/\n+\n+\n+       - name: Compute artifact hash\n+         id: hash\n+         run: |\n+           sha256sum release-artifact.zip > sha256sums.txt\n+           echo \"hashes=$(base64 -w0 sha256sums.txt)\" >> \"$GITHUB_OUTPUT\"\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: release-artifact\n+           path: |\n+             release-artifact.zip\n+             sha256sums.txt\n+           retention-days: 30\n+\n+   trivy-fs-scan:\n+     name: Trivy filesystem scan (CRITICAL block, INV-92)\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read  # checkout only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+       - name: Scan filesystem for CRITICAL vulnerabilities\n+         uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0\n+         with:\n+           scan-type: fs\n+           scan-ref: .\n+           format: table\n+           exit-code: '1'\n+           severity: CRITICAL\n+           trivyignores: .trivyignore\n+\n+   mutation-blocking:\n+     name: Mutation tests (consume nightly, fallback run)\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     # #1505: mutation and fail-on-empty block at every emitted level, including L1.\n+     continue-on-error: false\n+     permissions:\n+       contents: read  # checkout + tool build\n+       actions: read   # gh run list / gh api against the nightly mutation-deep run\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+       # #1502: consume fresh nightly mutation evidence to avoid a synchronous rerun.\n+       # Missing/stale evidence requires a blocking, nonempty fallback (#1505).\n+       - name: Consume latest nightly mutation-deep result\n+         id: nightly\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+           GH_REPO: ${{ github.repository }}\n+           DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}\n+           # Freshness window (days) for accepting a nightly mutation result; repo/org var override.\n+           MUTATION_NIGHTLY_MAX_AGE_DAYS: ${{ vars.MUTATION_NIGHTLY_MAX_AGE_DAYS || '7' }}\n+         run: |\n+           set -uo pipefail\n+           FRESH=false\n+           MAX_AGE=\"${MUTATION_NIGHTLY_MAX_AGE_DAYS:-7}\"\n+           RUN_JSON=$(gh run list --repo \"$GH_REPO\" --workflow 06-nightly.yml \\\n+             --branch \"$DEFAULT_BRANCH\" --status completed --limit 1 \\\n+             --json databaseId,createdAt 2>/dev/null || echo '[]')\n+           RUN_ID=$(printf '%s' \"$RUN_JSON\" | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d[0]['databaseId'] if d else '')\" 2>/dev/null || echo '')\n+           if [ -n \"$RUN_ID\" ]; then\n+             CREATED=$(printf '%s' \"$RUN_JSON\" | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d[0]['createdAt'] if d else '')\" 2>/dev/null || echo '')\n+             CONCLUSION=$(gh api \"repos/$GH_REPO/actions/runs/$RUN_ID/jobs\" \\\n+               --jq '.jobs[] | select(.name==\"Deep mutation testing\") | .conclusion' 2>/dev/null || echo '')\n+             AGE_DAYS=99999\n+             if [ -n \"$CREATED\" ]; then\n+               AGE_DAYS=$(( ( $(date -u +%s) - $(date -u -d \"$CREATED\" +%s) ) / 86400 ))\n+             fi\n+             echo \"Latest nightly run $RUN_ID: mutation-deep=${CONCLUSION:-unknown}, age=${AGE_DAYS}d (max ${MAX_AGE}d)\"\n+             if [ \"$CONCLUSION\" = \"success\" ] && [ \"$AGE_DAYS\" -le \"$MAX_AGE\" ]; then\n+               FRESH=true\n+               echo \"::notice::Release mutation gate satisfied by nightly mutation-deep (run $RUN_ID).\"\n+             fi\n+           else\n+             echo \"No completed nightly run found on $DEFAULT_BRANCH.\"\n+           fi\n+           echo \"fresh=$FRESH\" >> \"$GITHUB_OUTPUT\"\n+           [ \"$FRESH\" = \"true\" ] || echo \"No fresh nightly mutation result — running mutation synchronously as fallback.\"\n+\n+       - uses: ./.github/actions/setup-java-maven\n+         if: steps.nightly.outputs.fresh != 'true'\n+\n+       - uses: gradle/actions/setup-gradle@ed408507eac070d1f99cc633dbcf757c94c7933a  # v4\n+         if: steps.nightly.outputs.fresh != 'true'\n+       - name: PITest mutation (fallback — no fresh nightly)\n+         if: steps.nightly.outputs.fresh != 'true'\n+         run: ./gradlew pitest 2>&1 | tee mutation-fallback.log\n+\n+\n+       # #1505: require a positive tool-specific mutant count; empty or unrecognized output fails closed.\n+       - name: Fail when the mutation run produced no mutants (#1505)\n+         if: steps.nightly.outputs.fresh != 'true'\n+         run: |\n+           if [ ! -s mutation-fallback.log ]; then\n+             echo \"::error::No mutation output captured — failing closed (an empty run is not a pass).\"\n+             exit 1\n+           fi\n+           if ! grep -Eqi 'tests per mutant|mutation score|[1-9][0-9]* mutants|Generated [1-9][0-9]* mutations|total is [1-9]|Caught [1-9]|KILLED|SURVIVED' mutation-fallback.log; then\n+             echo \"::error::Mutation run generated zero mutants — an empty run is not a pass (#1505).\"\n+             exit 1\n+           fi\n+           echo \"Mutation run produced mutants — fail-on-empty check passed.\"\n+\n+   secret-scan-history:\n+     name: Full-history secret scan\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read  # checkout (full history) only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       # #2100: stale /tmp/gitleaks.tmp on self-hosted runners breaks gitleaks-action retries.\n+       # ponytail: delete assumes one download per runner; concurrent jobs need the\n+       # curl -> $RUNNER_TEMP/bin installation used in 01-pr-fast.\n+       - name: Clear stale gitleaks download temp\n+         run: rm -f /tmp/gitleaks.tmp\n+       # #2673: gitleaks-action always attempts to upload a results.sarif artifact (regardless of\n+       # findings) via actions/upload-artifact's validateRootDirectory, which requires the SARIF\n+       # path to sit under the action runner's rootDirectory. On a self-hosted runner with\n+       # HOME=/root and the workspace under /tmp, that check throws even on a clean scan.\n+       # GITLEAKS_ENABLE_UPLOAD_ARTIFACT=false (verified against the pinned SHA's dist/index.js)\n+       # skips the upload path entirely; the scan itself stays blocking.\n+       - uses: gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e  # v3.0.0\n+         env:\n+           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}\n+           GITLEAKS_ENABLE_UPLOAD_ARTIFACT: 'false'\n+         with:\n+           args: '--log-opts --full-history'\n+\n+   sbom:\n+     name: Generate SBOM\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read  # checkout only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+\n+       - uses: ./.github/actions/setup-java-maven\n+\n+       - uses: gradle/actions/setup-gradle@ed408507eac070d1f99cc633dbcf757c94c7933a  # v4\n+       - name: Generate CycloneDX SBOM\n+         run: ./gradlew cyclonedxBom\n+       - run: mv build/reports/bom.json sbom.cdx.json 2>/dev/null || mv build/reports/bom.xml sbom.cdx.xml || true\n+\n+\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: sbom\n+           path: sbom.cdx.*\n+           retention-days: 90\n+\n+   cosign-sign:\n+     name: Sign artifact (cosign keyless)\n+     needs: [build-superset, mutation-blocking, secret-scan-history, trivy-fs-scan]\n+     if: github.event_name != 'pull_request'\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       # (M) red-team finding 6: this job only downloads an artifact, signs it and\n+       # uploads a bundle — no checkout, no push. id-token is the only grant it uses.\n+       id-token: write\n+     steps:\n+       - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: release-artifact\n+       - uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6  # v4.1.2\n+       - name: Sigstore preflight check\n+         run: |\n+           STATUS=$(curl -sf https://status.sigstore.dev/api/v2/status.json \\\n+             --max-time 10 | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d.get('status',{}).get('indicator','unknown'))\" 2>/dev/null || echo \"unknown\")\n+           if [[ \"$STATUS\" != \"none\" ]]; then\n+             echo \"::warning::Sigstore status is '${STATUS}' — cosign sign may fail\"\n+           fi\n+           echo \"sigstore-status=$STATUS\"\n+       - name: Sign artifact (keyless OIDC)\n+         run: cosign sign-blob --yes release-artifact.zip --bundle release-artifact.zip.bundle\n+\n+       - name: Verify signature (L2+ strict)\n+         run: |\n+           cosign verify-blob \\\n+             --bundle release-artifact.zip.bundle \\\n+             --certificate-identity-regexp \".*\" \\\n+             --certificate-oidc-issuer \"https://token.actions.githubusercontent.com\" \\\n+             release-artifact.zip\n+\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: cosign-bundle\n+           path: release-artifact.zip.bundle\n+           retention-days: 90\n+\n+   slsa-provenance:\n+     name: SLSA provenance (L2 signed)\n+     needs: [build-superset]\n+     if: github.event_name != 'pull_request'\n+     permissions:\n+       id-token: write\n+       contents: write\n+       actions: read\n+     uses: slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@f7dd8c54c2067bafc12ca7a55595d5ee9b75204a  # v2.1.0\n+     with:\n+       base64-subjects: ${{ needs.build-superset.outputs.hashes }}\n+       # #2673: the generator halts by default on a private repo to avoid exposing the repo name\n+       # in the public Rekor transparency log. Opt in: the name is already public via the npm\n+       # package @getarbiter/cli's trusted-publisher metadata, so the entry reveals nothing new.\n+       private-repository: true\n+       # #2673: compile-generator: false (default) downloads a prebuilt release binary, which\n+       # requires the pinned ref to look like refs/tags/vX.Y.Z (\"Invalid ref: <sha>. Expected ref\n+       # of the form refs/tags/vX.Y.Z\" — generate-builder.sh). Incompatible with pinning this call\n+       # by commit SHA (this repo's convention for every action). Compile from the pinned SHA\n+       # instead (git checkout + go build, ~2 min slower per the input's own description).\n+       compile-generator: true\n+\n+   attest-build-provenance:\n+     name: Attest build provenance\n+     needs: [build-superset]\n+     if: github.event_name != 'pull_request'\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       id-token: write\n+       attestations: write\n+       contents: read\n+     steps:\n+       - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: release-artifact\n+       - uses: actions/attest-build-provenance@e8998f949152b193b063cb0ec769d69d929409be # v2.4.0\n+         with:\n+           subject-path: release-artifact.zip\n+\n+\n+   publish-package:\n+     name: Publish package (lib)\n+     needs: [cosign-sign, attest-build-provenance, slsa-provenance, sbom, doc-freshness-gate]\n+     # #2138/#2624: tag-shaped refs alone also admit manual dispatch; require a push.\n+     if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')\n+\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read\n+       id-token: write  # required by `npm publish --provenance` (OIDC)\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           ref: ${{ github.ref }}\n+       - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: release-artifact\n+       - name: Verify the downloaded artifact is the one that was signed (#2138)\n+         # #2138: reject downloaded bytes that differ from the signed build artifact.\n+         run: sha256sum -c sha256sums.txt\n+\n+       - uses: ./.github/actions/setup-java-maven\n+\n+       - uses: gradle/actions/setup-gradle@ed408507eac070d1f99cc633dbcf757c94c7933a  # v4\n+       - name: Publish to Maven Central\n+         run: ./gradlew publish\n+         env:\n+           MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}\n+           MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}\n+           ORG_GRADLE_PROJECT_signingKey: ${{ secrets.GPG_SIGNING_KEY }}\n+           ORG_GRADLE_PROJECT_signingPassword: ${{ secrets.GPG_SIGNING_PASSWORD }}\n+\n+\n+\n+\n+   doc-freshness-gate:\n+     name: Per-doc freshness gate (T4, gold-doc-tranches-t3-t5.md §2.3 — release-tag blocking)\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 30\n+     permissions:\n+       contents: read  # checkout (full history) only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       - uses: ./.github/actions/setup-node-pnpm\n+       - name: Per-doc freshness audit — no stale gold doc ships in a tagged release\n+         run: node scripts/check-doc-freshness.mjs\n+\n+   release-required:\n+     name: Release gate\n+     needs:\n+       - mutation-blocking\n+       - secret-scan-history\n+       - trivy-fs-scan\n+       - sbom\n+       - cosign-sign\n+       - slsa-provenance\n+       - attest-build-provenance\n+       - doc-freshness-gate\n+\n+       - publish-package\n+\n+\n+     if: always()\n+\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions: {}  # no checkout; only reads job outcomes via toJSON(needs)\n+     steps:\n+       - name: Release gate summary\n+         id: release-results\n+         env:\n+           NEEDS_JSON: ${{ toJSON(needs) }}\n+         run: |\n+           jq -e 'length > 0 and all(.[]; .result == \"success\")' <<< \"$NEEDS_JSON\" >/dev/null\n+           echo \"Release checks completed.\"\n+           echo \"Signing, provenance, and mutation verified.\"\n+\n\n ❯ __tests__/templates/05-release-render.test.ts:86:24\n     84|     ({ language, buildTool }) => {\n     85|       const rendered = renderRelease({ language, buildTool })\n     86|       expect(rendered).toContain('sbom-attest:')\n       |                        ^\n     87|       const jobSection = rendered.split('sbom-attest:')[1]?.split(/\\n …\n     88|       expect(jobSection).toContain('cosign attest-blob')\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/6]⎯\n\n FAIL  __tests__/templates/05-release-render.test.ts > 05-release.yml.ejs — structural invariants (CANON-18) > 'go': sbom-attest job uses attest-blob, not attest (INV-92)\nAssertionError: expected 'name: Release (T3)\\n\\n# E3: productiv…' to contain 'sbom-attest:'\n\n- Expected\n+ Received\n\n- sbom-attest:\n+ name: Release (T3)\n+\n+ # E3: productive jobs run only on tag pushes; PR builds previously leaked through unguarded roots.\n+ # Manual npm authentication is separate and cannot reach the productive chain.\n+ on:\n+   push:\n+     tags:\n+       - 'v*'\n+       - '!v0.0.0-verify-*'\n+\n+ permissions:\n+   contents: read\n+\n+ concurrency:\n+   group: release-${{ github.ref }}\n+   cancel-in-progress: false\n+\n+ jobs:\n+\n+   build-superset:\n+     name: Build superset\n+\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_BUILD || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     outputs:\n+       hashes: ${{ steps.hash.outputs.hashes }}\n+     permissions:\n+       contents: read  # checkout only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       - name: Verify the tagged commit is reachable from the reviewed default branch (#2679)\n+         # (H) red-team finding 2: any `v*` tag reached this job — a tag pushed at an\n+         # unreviewed commit, or at a re-pointed tag object, still built/signed/published.\n+         # Fail closed unless GITHUB_SHA is an ancestor of origin/<default branch>, and\n+         # unless the pushed tag name itself resolves to that exact commit (works for both\n+         # annotated and lightweight tags — `^{commit}` dereferences an annotated tag\n+         # object, and is a no-op on a lightweight tag that already points at a commit).\n+         #\n+         # HONESTY NOTE (round 2, #2679): this step is DEFENSE IN DEPTH, not the enforcing\n+         # control. A workflow runs at the tagged commit's own content, so a commit that is\n+         # itself off the reviewed branch can simply omit or alter this step before its\n+         # author pushes the tag — the step cannot defend against a workflow file it does\n+         # not control. The actual enforcing control MUST be a repository tag ruleset that\n+         # restricts who may CREATE a `v*` tag to admins/a release team (GitHub rulesets,\n+         # evaluated server-side before the workflow ever runs, independent of what any\n+         # commit's .github/workflows/*.yml says). See docs/internal/release-playbook.md\n+         # for the `gh api` command that creates it; this in-workflow check only catches a\n+         # tag pushed by someone who already has that permission but targeted the wrong\n+         # commit, plus gives a fast, readable failure instead of a silent bypass.\n+         id: ancestry-check\n+         env:\n+           DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}\n+         run: |\n+           git fetch --no-tags origin \"+refs/heads/$DEFAULT_BRANCH:refs/remotes/origin/$DEFAULT_BRANCH\"\n+           git fetch --force origin \"+refs/tags/$GITHUB_REF_NAME:refs/tags/$GITHUB_REF_NAME\"\n+           TAG_COMMIT=$(git rev-parse \"refs/tags/$GITHUB_REF_NAME^{commit}\")\n+           if [ \"$TAG_COMMIT\" != \"$GITHUB_SHA\" ]; then\n+             echo \"::error::Tag $GITHUB_REF_NAME resolves to $TAG_COMMIT, not the checked-out commit $GITHUB_SHA — refusing to release.\"\n+             exit 1\n+           fi\n+           if ! git merge-base --is-ancestor \"$GITHUB_SHA\" \"refs/remotes/origin/$DEFAULT_BRANCH\"; then\n+             echo \"::error::$GITHUB_SHA is not reachable from origin/$DEFAULT_BRANCH — refusing to release an unreviewed commit.\"\n+             exit 1\n+           fi\n+           echo \"Tag $GITHUB_REF_NAME ($GITHUB_SHA) is reachable from origin/$DEFAULT_BRANCH.\"\n+\n+       - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e  # v7.0.0\n+         with:\n+           go-version-file: go.mod\n+       - run: go build -o dist/app ./...\n+       - name: Package artifact\n+         run: zip -r release-artifact.zip dist/\n+\n+       - name: Compute artifact hash\n+         id: hash\n+         run: |\n+           sha256sum release-artifact.zip > sha256sums.txt\n+           echo \"hashes=$(base64 -w0 sha256sums.txt)\" >> \"$GITHUB_OUTPUT\"\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: release-artifact\n+           path: |\n+             release-artifact.zip\n+             sha256sums.txt\n+           retention-days: 30\n+\n+   trivy-fs-scan:\n+     name: Trivy filesystem scan (CRITICAL block, INV-92)\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read  # checkout only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+       - name: Scan filesystem for CRITICAL vulnerabilities\n+         uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0\n+         with:\n+           scan-type: fs\n+           scan-ref: .\n+           format: table\n+           exit-code: '1'\n+           severity: CRITICAL\n+           trivyignores: .trivyignore\n+\n+   mutation-blocking:\n+     name: Mutation tests (consume nightly, fallback run)\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     # #1505: mutation and fail-on-empty block at every emitted level, including L1.\n+     continue-on-error: false\n+     permissions:\n+       contents: read  # checkout + tool build\n+       actions: read   # gh run list / gh api against the nightly mutation-deep run\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+       # #1502: consume fresh nightly mutation evidence to avoid a synchronous rerun.\n+       # Missing/stale evidence requires a blocking, nonempty fallback (#1505).\n+       - name: Consume latest nightly mutation-deep result\n+         id: nightly\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+           GH_REPO: ${{ github.repository }}\n+           DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}\n+           # Freshness window (days) for accepting a nightly mutation result; repo/org var override.\n+           MUTATION_NIGHTLY_MAX_AGE_DAYS: ${{ vars.MUTATION_NIGHTLY_MAX_AGE_DAYS || '7' }}\n+         run: |\n+           set -uo pipefail\n+           FRESH=false\n+           MAX_AGE=\"${MUTATION_NIGHTLY_MAX_AGE_DAYS:-7}\"\n+           RUN_JSON=$(gh run list --repo \"$GH_REPO\" --workflow 06-nightly.yml \\\n+             --branch \"$DEFAULT_BRANCH\" --status completed --limit 1 \\\n+             --json databaseId,createdAt 2>/dev/null || echo '[]')\n+           RUN_ID=$(printf '%s' \"$RUN_JSON\" | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d[0]['databaseId'] if d else '')\" 2>/dev/null || echo '')\n+           if [ -n \"$RUN_ID\" ]; then\n+             CREATED=$(printf '%s' \"$RUN_JSON\" | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d[0]['createdAt'] if d else '')\" 2>/dev/null || echo '')\n+             CONCLUSION=$(gh api \"repos/$GH_REPO/actions/runs/$RUN_ID/jobs\" \\\n+               --jq '.jobs[] | select(.name==\"Deep mutation testing\") | .conclusion' 2>/dev/null || echo '')\n+             AGE_DAYS=99999\n+             if [ -n \"$CREATED\" ]; then\n+               AGE_DAYS=$(( ( $(date -u +%s) - $(date -u -d \"$CREATED\" +%s) ) / 86400 ))\n+             fi\n+             echo \"Latest nightly run $RUN_ID: mutation-deep=${CONCLUSION:-unknown}, age=${AGE_DAYS}d (max ${MAX_AGE}d)\"\n+             if [ \"$CONCLUSION\" = \"success\" ] && [ \"$AGE_DAYS\" -le \"$MAX_AGE\" ]; then\n+               FRESH=true\n+               echo \"::notice::Release mutation gate satisfied by nightly mutation-deep (run $RUN_ID).\"\n+             fi\n+           else\n+             echo \"No completed nightly run found on $DEFAULT_BRANCH.\"\n+           fi\n+           echo \"fresh=$FRESH\" >> \"$GITHUB_OUTPUT\"\n+           [ \"$FRESH\" = \"true\" ] || echo \"No fresh nightly mutation result — running mutation synchronously as fallback.\"\n+\n+       - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e  # v7.0.0\n+         if: steps.nightly.outputs.fresh != 'true'\n+         with:\n+           go-version-file: go.mod\n+       # #1505: never swallow Go mutation failures; the next guard also rejects empty runs.\n+       - name: go-mutesting (fallback — no fresh nightly)\n+         if: steps.nightly.outputs.fresh != 'true'\n+         run: |\n+           go install github.com/zimmski/go-mutesting/cmd/go-mutesting@latest\n+           go-mutesting ./... 2>&1 | tee mutation-fallback.log\n+\n+       # #1505: require a positive tool-specific mutant count; empty or unrecognized output fails closed.\n+       - name: Fail when the mutation run produced no mutants (#1505)\n+         if: steps.nightly.outputs.fresh != 'true'\n+         run: |\n+           if [ ! -s mutation-fallback.log ]; then\n+             echo \"::error::No mutation output captured — failing closed (an empty run is not a pass).\"\n+             exit 1\n+           fi\n+           if ! grep -Eqi 'tests per mutant|mutation score|[1-9][0-9]* mutants|Generated [1-9][0-9]* mutations|total is [1-9]|Caught [1-9]|KILLED|SURVIVED' mutation-fallback.log; then\n+             echo \"::error::Mutation run generated zero mutants — an empty run is not a pass (#1505).\"\n+             exit 1\n+           fi\n+           echo \"Mutation run produced mutants — fail-on-empty check passed.\"\n+\n+   secret-scan-history:\n+     name: Full-history secret scan\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read  # checkout (full history) only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       # #2100: stale /tmp/gitleaks.tmp on self-hosted runners breaks gitleaks-action retries.\n+       # ponytail: delete assumes one download per runner; concurrent jobs need the\n+       # curl -> $RUNNER_TEMP/bin installation used in 01-pr-fast.\n+       - name: Clear stale gitleaks download temp\n+         run: rm -f /tmp/gitleaks.tmp\n+       # #2673: gitleaks-action always attempts to upload a results.sarif artifact (regardless of\n+       # findings) via actions/upload-artifact's validateRootDirectory, which requires the SARIF\n+       # path to sit under the action runner's rootDirectory. On a self-hosted runner with\n+       # HOME=/root and the workspace under /tmp, that check throws even on a clean scan.\n+       # GITLEAKS_ENABLE_UPLOAD_ARTIFACT=false (verified against the pinned SHA's dist/index.js)\n+       # skips the upload path entirely; the scan itself stays blocking.\n+       - uses: gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e  # v3.0.0\n+         env:\n+           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}\n+           GITLEAKS_ENABLE_UPLOAD_ARTIFACT: 'false'\n+         with:\n+           args: '--log-opts --full-history'\n+\n+   sbom:\n+     name: Generate SBOM\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read  # checkout only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+\n+       - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e  # v7.0.0\n+         with:\n+           go-version-file: go.mod\n+       - name: Generate SBOM via syft\n+         uses: anchore/sbom-action@f6c3d0fe42c3cf876e3462574e4c9416b5e0f07a  # v0.9.0\n+         with:\n+           path: .\n+           output-file: sbom.cdx.json\n+           format: cyclonedx-json\n+\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: sbom\n+           path: sbom.cdx.*\n+           retention-days: 90\n+\n+   cosign-sign:\n+     name: Sign artifact (cosign keyless)\n+     needs: [build-superset, mutation-blocking, secret-scan-history, trivy-fs-scan]\n+     if: github.event_name != 'pull_request'\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       # (M) red-team finding 6: this job only downloads an artifact, signs it and\n+       # uploads a bundle — no checkout, no push. id-token is the only grant it uses.\n+       id-token: write\n+     steps:\n+       - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: release-artifact\n+       - uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6  # v4.1.2\n+       - name: Sigstore preflight check\n+         run: |\n+           STATUS=$(curl -sf https://status.sigstore.dev/api/v2/status.json \\\n+             --max-time 10 | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d.get('status',{}).get('indicator','unknown'))\" 2>/dev/null || echo \"unknown\")\n+           if [[ \"$STATUS\" != \"none\" ]]; then\n+             echo \"::warning::Sigstore status is '${STATUS}' — cosign sign may fail\"\n+           fi\n+           echo \"sigstore-status=$STATUS\"\n+       - name: Sign artifact (keyless OIDC)\n+         run: cosign sign-blob --yes release-artifact.zip --bundle release-artifact.zip.bundle\n+\n+       - name: Verify signature (L2+ strict)\n+         run: |\n+           cosign verify-blob \\\n+             --bundle release-artifact.zip.bundle \\\n+             --certificate-identity-regexp \".*\" \\\n+             --certificate-oidc-issuer \"https://token.actions.githubusercontent.com\" \\\n+             release-artifact.zip\n+\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: cosign-bundle\n+           path: release-artifact.zip.bundle\n+           retention-days: 90\n+\n+   slsa-provenance:\n+     name: SLSA provenance (L2 signed)\n+     needs: [build-superset]\n+     if: github.event_name != 'pull_request'\n+     permissions:\n+       id-token: write\n+       contents: write\n+       actions: read\n+     uses: slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@f7dd8c54c2067bafc12ca7a55595d5ee9b75204a  # v2.1.0\n+     with:\n+       base64-subjects: ${{ needs.build-superset.outputs.hashes }}\n+       # #2673: the generator halts by default on a private repo to avoid exposing the repo name\n+       # in the public Rekor transparency log. Opt in: the name is already public via the npm\n+       # package @getarbiter/cli's trusted-publisher metadata, so the entry reveals nothing new.\n+       private-repository: true\n+       # #2673: compile-generator: false (default) downloads a prebuilt release binary, which\n+       # requires the pinned ref to look like refs/tags/vX.Y.Z (\"Invalid ref: <sha>. Expected ref\n+       # of the form refs/tags/vX.Y.Z\" — generate-builder.sh). Incompatible with pinning this call\n+       # by commit SHA (this repo's convention for every action). Compile from the pinned SHA\n+       # instead (git checkout + go build, ~2 min slower per the input's own description).\n+       compile-generator: true\n+\n+   attest-build-provenance:\n+     name: Attest build provenance\n+     needs: [build-superset]\n+     if: github.event_name != 'pull_request'\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       id-token: write\n+       attestations: write\n+       contents: read\n+     steps:\n+       - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: release-artifact\n+       - uses: actions/attest-build-provenance@e8998f949152b193b063cb0ec769d69d929409be # v2.4.0\n+         with:\n+           subject-path: release-artifact.zip\n+\n+\n+   publish-package:\n+     name: Publish package (lib)\n+     needs: [cosign-sign, attest-build-provenance, slsa-provenance, sbom, doc-freshness-gate]\n+     # #2138/#2624: tag-shaped refs alone also admit manual dispatch; require a push.\n+     if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')\n+\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read\n+       id-token: write  # required by `npm publish --provenance` (OIDC)\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           ref: ${{ github.ref }}\n+       - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: release-artifact\n+       - name: Verify the downloaded artifact is the one that was signed (#2138)\n+         # #2138: reject downloaded bytes that differ from the signed build artifact.\n+         run: sha256sum -c sha256sums.txt\n+\n+       - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e  # v7.0.0\n+         with:\n+           go-version-file: go.mod\n+       - name: Create GitHub release\n+         run: gh release create ${{ github.ref_name }} release-artifact.zip sha256sums.txt --generate-notes\n+         env:\n+           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}\n+\n+\n+\n+   doc-freshness-gate:\n+     name: Per-doc freshness gate (T4, gold-doc-tranches-t3-t5.md §2.3 — release-tag blocking)\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 30\n+     permissions:\n+       contents: read  # checkout (full history) only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       - uses: ./.github/actions/setup-node-pnpm\n+       - name: Per-doc freshness audit — no stale gold doc ships in a tagged release\n+         run: node scripts/check-doc-freshness.mjs\n+\n+   release-required:\n+     name: Release gate\n+     needs:\n+       - mutation-blocking\n+       - secret-scan-history\n+       - trivy-fs-scan\n+       - sbom\n+       - cosign-sign\n+       - slsa-provenance\n+       - attest-build-provenance\n+       - doc-freshness-gate\n+\n+       - publish-package\n+\n+\n+     if: always()\n+\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions: {}  # no checkout; only reads job outcomes via toJSON(needs)\n+     steps:\n+       - name: Release gate summary\n+         id: release-results\n+         env:\n+           NEEDS_JSON: ${{ toJSON(needs) }}\n+         run: |\n+           jq -e 'length > 0 and all(.[]; .result == \"success\")' <<< \"$NEEDS_JSON\" >/dev/null\n+           echo \"Release checks completed.\"\n+           echo \"Signing, provenance, and mutation verified.\"\n+\n\n ❯ __tests__/templates/05-release-render.test.ts:86:24\n     84|     ({ language, buildTool }) => {\n     85|       const rendered = renderRelease({ language, buildTool })\n     86|       expect(rendered).toContain('sbom-attest:')\n       |                        ^\n     87|       const jobSection = rendered.split('sbom-attest:')[1]?.split(/\\n …\n     88|       expect(jobSection).toContain('cosign attest-blob')\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/6]⎯\n\n FAIL  __tests__/templates/05-release-render.test.ts > 05-release.yml.ejs — structural invariants (CANON-18) > 'python': sbom-attest job uses attest-blob, not attest (INV-92)\nAssertionError: expected 'name: Release (T3)\\n\\n# E3: productiv…' to contain 'sbom-attest:'\n\n- Expected\n+ Received\n\n- sbom-attest:\n+ name: Release (T3)\n+\n+ # E3: productive jobs run only on tag pushes; PR builds previously leaked through unguarded roots.\n+ # Manual npm authentication is separate and cannot reach the productive chain.\n+ on:\n+   push:\n+     tags:\n+       - 'v*'\n+       - '!v0.0.0-verify-*'\n+\n+ permissions:\n+   contents: read\n+\n+ concurrency:\n+   group: release-${{ github.ref }}\n+   cancel-in-progress: false\n+\n+ jobs:\n+\n+   build-superset:\n+     name: Build superset\n+\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_BUILD || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     outputs:\n+       hashes: ${{ steps.hash.outputs.hashes }}\n+     permissions:\n+       contents: read  # checkout only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       - name: Verify the tagged commit is reachable from the reviewed default branch (#2679)\n+         # (H) red-team finding 2: any `v*` tag reached this job — a tag pushed at an\n+         # unreviewed commit, or at a re-pointed tag object, still built/signed/published.\n+         # Fail closed unless GITHUB_SHA is an ancestor of origin/<default branch>, and\n+         # unless the pushed tag name itself resolves to that exact commit (works for both\n+         # annotated and lightweight tags — `^{commit}` dereferences an annotated tag\n+         # object, and is a no-op on a lightweight tag that already points at a commit).\n+         #\n+         # HONESTY NOTE (round 2, #2679): this step is DEFENSE IN DEPTH, not the enforcing\n+         # control. A workflow runs at the tagged commit's own content, so a commit that is\n+         # itself off the reviewed branch can simply omit or alter this step before its\n+         # author pushes the tag — the step cannot defend against a workflow file it does\n+         # not control. The actual enforcing control MUST be a repository tag ruleset that\n+         # restricts who may CREATE a `v*` tag to admins/a release team (GitHub rulesets,\n+         # evaluated server-side before the workflow ever runs, independent of what any\n+         # commit's .github/workflows/*.yml says). See docs/internal/release-playbook.md\n+         # for the `gh api` command that creates it; this in-workflow check only catches a\n+         # tag pushed by someone who already has that permission but targeted the wrong\n+         # commit, plus gives a fast, readable failure instead of a silent bypass.\n+         id: ancestry-check\n+         env:\n+           DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}\n+         run: |\n+           git fetch --no-tags origin \"+refs/heads/$DEFAULT_BRANCH:refs/remotes/origin/$DEFAULT_BRANCH\"\n+           git fetch --force origin \"+refs/tags/$GITHUB_REF_NAME:refs/tags/$GITHUB_REF_NAME\"\n+           TAG_COMMIT=$(git rev-parse \"refs/tags/$GITHUB_REF_NAME^{commit}\")\n+           if [ \"$TAG_COMMIT\" != \"$GITHUB_SHA\" ]; then\n+             echo \"::error::Tag $GITHUB_REF_NAME resolves to $TAG_COMMIT, not the checked-out commit $GITHUB_SHA — refusing to release.\"\n+             exit 1\n+           fi\n+           if ! git merge-base --is-ancestor \"$GITHUB_SHA\" \"refs/remotes/origin/$DEFAULT_BRANCH\"; then\n+             echo \"::error::$GITHUB_SHA is not reachable from origin/$DEFAULT_BRANCH — refusing to release an unreviewed commit.\"\n+             exit 1\n+           fi\n+           echo \"Tag $GITHUB_REF_NAME ($GITHUB_SHA) is reachable from origin/$DEFAULT_BRANCH.\"\n+\n+       - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0\n+         with:\n+           python-version-file: .python-version\n+       - run: pip install build\n+       - run: python -m build\n+       - name: Package artifact\n+         run: zip -r release-artifact.zip dist/\n+\n+       - name: Compute artifact hash\n+         id: hash\n+         run: |\n+           sha256sum release-artifact.zip > sha256sums.txt\n+           echo \"hashes=$(base64 -w0 sha256sums.txt)\" >> \"$GITHUB_OUTPUT\"\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: release-artifact\n+           path: |\n+             release-artifact.zip\n+             sha256sums.txt\n+           retention-days: 30\n+\n+   trivy-fs-scan:\n+     name: Trivy filesystem scan (CRITICAL block, INV-92)\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read  # checkout only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+       - name: Scan filesystem for CRITICAL vulnerabilities\n+         uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0\n+         with:\n+           scan-type: fs\n+           scan-ref: .\n+           format: table\n+           exit-code: '1'\n+           severity: CRITICAL\n+           trivyignores: .trivyignore\n+\n+   mutation-blocking:\n+     name: Mutation tests (consume nightly, fallback run)\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     # #1505: mutation and fail-on-empty block at every emitted level, including L1.\n+     continue-on-error: false\n+     permissions:\n+       contents: read  # checkout + tool build\n+       actions: read   # gh run list / gh api against the nightly mutation-deep run\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+       # #1502: consume fresh nightly mutation evidence to avoid a synchronous rerun.\n+       # Missing/stale evidence requires a blocking, nonempty fallback (#1505).\n+       - name: Consume latest nightly mutation-deep result\n+         id: nightly\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+           GH_REPO: ${{ github.repository }}\n+           DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}\n+           # Freshness window (days) for accepting a nightly mutation result; repo/org var override.\n+           MUTATION_NIGHTLY_MAX_AGE_DAYS: ${{ vars.MUTATION_NIGHTLY_MAX_AGE_DAYS || '7' }}\n+         run: |\n+           set -uo pipefail\n+           FRESH=false\n+           MAX_AGE=\"${MUTATION_NIGHTLY_MAX_AGE_DAYS:-7}\"\n+           RUN_JSON=$(gh run list --repo \"$GH_REPO\" --workflow 06-nightly.yml \\\n+             --branch \"$DEFAULT_BRANCH\" --status completed --limit 1 \\\n+             --json databaseId,createdAt 2>/dev/null || echo '[]')\n+           RUN_ID=$(printf '%s' \"$RUN_JSON\" | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d[0]['databaseId'] if d else '')\" 2>/dev/null || echo '')\n+           if [ -n \"$RUN_ID\" ]; then\n+             CREATED=$(printf '%s' \"$RUN_JSON\" | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d[0]['createdAt'] if d else '')\" 2>/dev/null || echo '')\n+             CONCLUSION=$(gh api \"repos/$GH_REPO/actions/runs/$RUN_ID/jobs\" \\\n+               --jq '.jobs[] | select(.name==\"Deep mutation testing\") | .conclusion' 2>/dev/null || echo '')\n+             AGE_DAYS=99999\n+             if [ -n \"$CREATED\" ]; then\n+               AGE_DAYS=$(( ( $(date -u +%s) - $(date -u -d \"$CREATED\" +%s) ) / 86400 ))\n+             fi\n+             echo \"Latest nightly run $RUN_ID: mutation-deep=${CONCLUSION:-unknown}, age=${AGE_DAYS}d (max ${MAX_AGE}d)\"\n+             if [ \"$CONCLUSION\" = \"success\" ] && [ \"$AGE_DAYS\" -le \"$MAX_AGE\" ]; then\n+               FRESH=true\n+               echo \"::notice::Release mutation gate satisfied by nightly mutation-deep (run $RUN_ID).\"\n+             fi\n+           else\n+             echo \"No completed nightly run found on $DEFAULT_BRANCH.\"\n+           fi\n+           echo \"fresh=$FRESH\" >> \"$GITHUB_OUTPUT\"\n+           [ \"$FRESH\" = \"true\" ] || echo \"No fresh nightly mutation result — running mutation synchronously as fallback.\"\n+\n+       - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0\n+         if: steps.nightly.outputs.fresh != 'true'\n+         with:\n+           python-version-file: .python-version\n+       - run: pip install -e \".[dev]\" mutmut\n+         if: steps.nightly.outputs.fresh != 'true'\n+       - name: mutmut mutation (fallback — no fresh nightly)\n+         if: steps.nightly.outputs.fresh != 'true'\n+         run: |\n+           mutmut run 2>&1 | tee mutation-fallback.log\n+           mutmut results 2>&1 | tee -a mutation-fallback.log\n+\n+       # #1505: require a positive tool-specific mutant count; empty or unrecognized output fails closed.\n+       - name: Fail when the mutation run produced no mutants (#1505)\n+         if: steps.nightly.outputs.fresh != 'true'\n+         run: |\n+           if [ ! -s mutation-fallback.log ]; then\n+             echo \"::error::No mutation output captured — failing closed (an empty run is not a pass).\"\n+             exit 1\n+           fi\n+           if ! grep -Eqi 'tests per mutant|mutation score|[1-9][0-9]* mutants|Generated [1-9][0-9]* mutations|total is [1-9]|Caught [1-9]|KILLED|SURVIVED' mutation-fallback.log; then\n+             echo \"::error::Mutation run generated zero mutants — an empty run is not a pass (#1505).\"\n+             exit 1\n+           fi\n+           echo \"Mutation run produced mutants — fail-on-empty check passed.\"\n+\n+   secret-scan-history:\n+     name: Full-history secret scan\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read  # checkout (full history) only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       # #2100: stale /tmp/gitleaks.tmp on self-hosted runners breaks gitleaks-action retries.\n+       # ponytail: delete assumes one download per runner; concurrent jobs need the\n+       # curl -> $RUNNER_TEMP/bin installation used in 01-pr-fast.\n+       - name: Clear stale gitleaks download temp\n+         run: rm -f /tmp/gitleaks.tmp\n+       # #2673: gitleaks-action always attempts to upload a results.sarif artifact (regardless of\n+       # findings) via actions/upload-artifact's validateRootDirectory, which requires the SARIF\n+       # path to sit under the action runner's rootDirectory. On a self-hosted runner with\n+       # HOME=/root and the workspace under /tmp, that check throws even on a clean scan.\n+       # GITLEAKS_ENABLE_UPLOAD_ARTIFACT=false (verified against the pinned SHA's dist/index.js)\n+       # skips the upload path entirely; the scan itself stays blocking.\n+       - uses: gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e  # v3.0.0\n+         env:\n+           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}\n+           GITLEAKS_ENABLE_UPLOAD_ARTIFACT: 'false'\n+         with:\n+           args: '--log-opts --full-history'\n+\n+   sbom:\n+     name: Generate SBOM\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read  # checkout only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+\n+       - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0\n+         with:\n+           python-version-file: .python-version\n+       - run: pip install cyclonedx-bom\n+       - name: Generate CycloneDX SBOM\n+         run: python -m cyclonedx_py auto > sbom.cdx.json || true\n+\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: sbom\n+           path: sbom.cdx.*\n+           retention-days: 90\n+\n+   cosign-sign:\n+     name: Sign artifact (cosign keyless)\n+     needs: [build-superset, mutation-blocking, secret-scan-history, trivy-fs-scan]\n+     if: github.event_name != 'pull_request'\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       # (M) red-team finding 6: this job only downloads an artifact, signs it and\n+       # uploads a bundle — no checkout, no push. id-token is the only grant it uses.\n+       id-token: write\n+     steps:\n+       - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: release-artifact\n+       - uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6  # v4.1.2\n+       - name: Sigstore preflight check\n+         run: |\n+           STATUS=$(curl -sf https://status.sigstore.dev/api/v2/status.json \\\n+             --max-time 10 | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d.get('status',{}).get('indicator','unknown'))\" 2>/dev/null || echo \"unknown\")\n+           if [[ \"$STATUS\" != \"none\" ]]; then\n+             echo \"::warning::Sigstore status is '${STATUS}' — cosign sign may fail\"\n+           fi\n+           echo \"sigstore-status=$STATUS\"\n+       - name: Sign artifact (keyless OIDC)\n+         run: cosign sign-blob --yes release-artifact.zip --bundle release-artifact.zip.bundle\n+\n+       - name: Verify signature (L2+ strict)\n+         run: |\n+           cosign verify-blob \\\n+             --bundle release-artifact.zip.bundle \\\n+             --certificate-identity-regexp \".*\" \\\n+             --certificate-oidc-issuer \"https://token.actions.githubusercontent.com\" \\\n+             release-artifact.zip\n+\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: cosign-bundle\n+           path: release-artifact.zip.bundle\n+           retention-days: 90\n+\n+   slsa-provenance:\n+     name: SLSA provenance (L2 signed)\n+     needs: [build-superset]\n+     if: github.event_name != 'pull_request'\n+     permissions:\n+       id-token: write\n+       contents: write\n+       actions: read\n+     uses: slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@f7dd8c54c2067bafc12ca7a55595d5ee9b75204a  # v2.1.0\n+     with:\n+       base64-subjects: ${{ needs.build-superset.outputs.hashes }}\n+       # #2673: the generator halts by default on a private repo to avoid exposing the repo name\n+       # in the public Rekor transparency log. Opt in: the name is already public via the npm\n+       # package @getarbiter/cli's trusted-publisher metadata, so the entry reveals nothing new.\n+       private-repository: true\n+       # #2673: compile-generator: false (default) downloads a prebuilt release binary, which\n+       # requires the pinned ref to look like refs/tags/vX.Y.Z (\"Invalid ref: <sha>. Expected ref\n+       # of the form refs/tags/vX.Y.Z\" — generate-builder.sh). Incompatible with pinning this call\n+       # by commit SHA (this repo's convention for every action). Compile from the pinned SHA\n+       # instead (git checkout + go build, ~2 min slower per the input's own description).\n+       compile-generator: true\n+\n+   attest-build-provenance:\n+     name: Attest build provenance\n+     needs: [build-superset]\n+     if: github.event_name != 'pull_request'\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       id-token: write\n+       attestations: write\n+       contents: read\n+     steps:\n+       - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: release-artifact\n+       - uses: actions/attest-build-provenance@e8998f949152b193b063cb0ec769d69d929409be # v2.4.0\n+         with:\n+           subject-path: release-artifact.zip\n+\n+\n+   publish-package:\n+     name: Publish package (lib)\n+     needs: [cosign-sign, attest-build-provenance, slsa-provenance, sbom, doc-freshness-gate]\n+     # #2138/#2624: tag-shaped refs alone also admit manual dispatch; require a push.\n+     if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')\n+\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read\n+       id-token: write  # required by `npm publish --provenance` (OIDC)\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           ref: ${{ github.ref }}\n+       - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: release-artifact\n+       - name: Verify the downloaded artifact is the one that was signed (#2138)\n+         # #2138: reject downloaded bytes that differ from the signed build artifact.\n+         run: sha256sum -c sha256sums.txt\n+\n+       - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0\n+         with:\n+           python-version-file: .python-version\n+       - run: pip install build twine\n+       - run: python -m build\n+       - name: Publish to PyPI\n+         run: twine upload dist/*\n+         env:\n+           TWINE_USERNAME: __token__\n+           TWINE_PASSWORD: ${{ secrets.PYPI_TOKEN }}\n+\n+\n+\n+   doc-freshness-gate:\n+     name: Per-doc freshness gate (T4, gold-doc-tranches-t3-t5.md §2.3 — release-tag blocking)\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 30\n+     permissions:\n+       contents: read  # checkout (full history) only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       - uses: ./.github/actions/setup-node-pnpm\n+       - name: Per-doc freshness audit — no stale gold doc ships in a tagged release\n+         run: node scripts/check-doc-freshness.mjs\n+\n+   release-required:\n+     name: Release gate\n+     needs:\n+       - mutation-blocking\n+       - secret-scan-history\n+       - trivy-fs-scan\n+       - sbom\n+       - cosign-sign\n+       - slsa-provenance\n+       - attest-build-provenance\n+       - doc-freshness-gate\n+\n+       - publish-package\n+\n+\n+     if: always()\n+\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions: {}  # no checkout; only reads job outcomes via toJSON(needs)\n+     steps:\n+       - name: Release gate summary\n+         id: release-results\n+         env:\n+           NEEDS_JSON: ${{ toJSON(needs) }}\n+         run: |\n+           jq -e 'length > 0 and all(.[]; .result == \"success\")' <<< \"$NEEDS_JSON\" >/dev/null\n+           echo \"Release checks completed.\"\n+           echo \"Signing, provenance, and mutation verified.\"\n+\n\n ❯ __tests__/templates/05-release-render.test.ts:86:24\n     84|     ({ language, buildTool }) => {\n     85|       const rendered = renderRelease({ language, buildTool })\n     86|       expect(rendered).toContain('sbom-attest:')\n       |                        ^\n     87|       const jobSection = rendered.split('sbom-attest:')[1]?.split(/\\n …\n     88|       expect(jobSection).toContain('cosign attest-blob')\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/6]⎯\n\n FAIL  __tests__/templates/05-release-render.test.ts > 05-release.yml.ejs — structural invariants (CANON-18) > 'rust': sbom-attest job uses attest-blob, not attest (INV-92)\nAssertionError: expected 'name: Release (T3)\\n\\n# E3: productiv…' to contain 'sbom-attest:'\n\n- Expected\n+ Received\n\n- sbom-attest:\n+ name: Release (T3)\n+\n+ # E3: productive jobs run only on tag pushes; PR builds previously leaked through unguarded roots.\n+ # Manual npm authentication is separate and cannot reach the productive chain.\n+ on:\n+   push:\n+     tags:\n+       - 'v*'\n+       - '!v0.0.0-verify-*'\n+\n+ permissions:\n+   contents: read\n+\n+ concurrency:\n+   group: release-${{ github.ref }}\n+   cancel-in-progress: false\n+\n+ jobs:\n+\n+   build-superset:\n+     name: Build superset\n+\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_BUILD || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     outputs:\n+       hashes: ${{ steps.hash.outputs.hashes }}\n+     permissions:\n+       contents: read  # checkout only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       - name: Verify the tagged commit is reachable from the reviewed default branch (#2679)\n+         # (H) red-team finding 2: any `v*` tag reached this job — a tag pushed at an\n+         # unreviewed commit, or at a re-pointed tag object, still built/signed/published.\n+         # Fail closed unless GITHUB_SHA is an ancestor of origin/<default branch>, and\n+         # unless the pushed tag name itself resolves to that exact commit (works for both\n+         # annotated and lightweight tags — `^{commit}` dereferences an annotated tag\n+         # object, and is a no-op on a lightweight tag that already points at a commit).\n+         #\n+         # HONESTY NOTE (round 2, #2679): this step is DEFENSE IN DEPTH, not the enforcing\n+         # control. A workflow runs at the tagged commit's own content, so a commit that is\n+         # itself off the reviewed branch can simply omit or alter this step before its\n+         # author pushes the tag — the step cannot defend against a workflow file it does\n+         # not control. The actual enforcing control MUST be a repository tag ruleset that\n+         # restricts who may CREATE a `v*` tag to admins/a release team (GitHub rulesets,\n+         # evaluated server-side before the workflow ever runs, independent of what any\n+         # commit's .github/workflows/*.yml says). See docs/internal/release-playbook.md\n+         # for the `gh api` command that creates it; this in-workflow check only catches a\n+         # tag pushed by someone who already has that permission but targeted the wrong\n+         # commit, plus gives a fast, readable failure instead of a silent bypass.\n+         id: ancestry-check\n+         env:\n+           DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}\n+         run: |\n+           git fetch --no-tags origin \"+refs/heads/$DEFAULT_BRANCH:refs/remotes/origin/$DEFAULT_BRANCH\"\n+           git fetch --force origin \"+refs/tags/$GITHUB_REF_NAME:refs/tags/$GITHUB_REF_NAME\"\n+           TAG_COMMIT=$(git rev-parse \"refs/tags/$GITHUB_REF_NAME^{commit}\")\n+           if [ \"$TAG_COMMIT\" != \"$GITHUB_SHA\" ]; then\n+             echo \"::error::Tag $GITHUB_REF_NAME resolves to $TAG_COMMIT, not the checked-out commit $GITHUB_SHA — refusing to release.\"\n+             exit 1\n+           fi\n+           if ! git merge-base --is-ancestor \"$GITHUB_SHA\" \"refs/remotes/origin/$DEFAULT_BRANCH\"; then\n+             echo \"::error::$GITHUB_SHA is not reachable from origin/$DEFAULT_BRANCH — refusing to release an unreviewed commit.\"\n+             exit 1\n+           fi\n+           echo \"Tag $GITHUB_REF_NAME ($GITHUB_SHA) is reachable from origin/$DEFAULT_BRANCH.\"\n+\n+       - name: Install Rust toolchain\n+         uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable\n+       - run: cargo build --release\n+       - name: Package artifact\n+         run: zip -r release-artifact.zip target/release/\n+\n+       - name: Compute artifact hash\n+         id: hash\n+         run: |\n+           sha256sum release-artifact.zip > sha256sums.txt\n+           echo \"hashes=$(base64 -w0 sha256sums.txt)\" >> \"$GITHUB_OUTPUT\"\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: release-artifact\n+           path: |\n+             release-artifact.zip\n+             sha256sums.txt\n+           retention-days: 30\n+\n+   trivy-fs-scan:\n+     name: Trivy filesystem scan (CRITICAL block, INV-92)\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read  # checkout only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+       - name: Scan filesystem for CRITICAL vulnerabilities\n+         uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0\n+         with:\n+           scan-type: fs\n+           scan-ref: .\n+           format: table\n+           exit-code: '1'\n+           severity: CRITICAL\n+           trivyignores: .trivyignore\n+\n+   mutation-blocking:\n+     name: Mutation tests (consume nightly, fallback run)\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     # #1505: mutation and fail-on-empty block at every emitted level, including L1.\n+     continue-on-error: false\n+     permissions:\n+       contents: read  # checkout + tool build\n+       actions: read   # gh run list / gh api against the nightly mutation-deep run\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+       # #1502: consume fresh nightly mutation evidence to avoid a synchronous rerun.\n+       # Missing/stale evidence requires a blocking, nonempty fallback (#1505).\n+       - name: Consume latest nightly mutation-deep result\n+         id: nightly\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+           GH_REPO: ${{ github.repository }}\n+           DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}\n+           # Freshness window (days) for accepting a nightly mutation result; repo/org var override.\n+           MUTATION_NIGHTLY_MAX_AGE_DAYS: ${{ vars.MUTATION_NIGHTLY_MAX_AGE_DAYS || '7' }}\n+         run: |\n+           set -uo pipefail\n+           FRESH=false\n+           MAX_AGE=\"${MUTATION_NIGHTLY_MAX_AGE_DAYS:-7}\"\n+           RUN_JSON=$(gh run list --repo \"$GH_REPO\" --workflow 06-nightly.yml \\\n+             --branch \"$DEFAULT_BRANCH\" --status completed --limit 1 \\\n+             --json databaseId,createdAt 2>/dev/null || echo '[]')\n+           RUN_ID=$(printf '%s' \"$RUN_JSON\" | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d[0]['databaseId'] if d else '')\" 2>/dev/null || echo '')\n+           if [ -n \"$RUN_ID\" ]; then\n+             CREATED=$(printf '%s' \"$RUN_JSON\" | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d[0]['createdAt'] if d else '')\" 2>/dev/null || echo '')\n+             CONCLUSION=$(gh api \"repos/$GH_REPO/actions/runs/$RUN_ID/jobs\" \\\n+               --jq '.jobs[] | select(.name==\"Deep mutation testing\") | .conclusion' 2>/dev/null || echo '')\n+             AGE_DAYS=99999\n+             if [ -n \"$CREATED\" ]; then\n+               AGE_DAYS=$(( ( $(date -u +%s) - $(date -u -d \"$CREATED\" +%s) ) / 86400 ))\n+             fi\n+             echo \"Latest nightly run $RUN_ID: mutation-deep=${CONCLUSION:-unknown}, age=${AGE_DAYS}d (max ${MAX_AGE}d)\"\n+             if [ \"$CONCLUSION\" = \"success\" ] && [ \"$AGE_DAYS\" -le \"$MAX_AGE\" ]; then\n+               FRESH=true\n+               echo \"::notice::Release mutation gate satisfied by nightly mutation-deep (run $RUN_ID).\"\n+             fi\n+           else\n+             echo \"No completed nightly run found on $DEFAULT_BRANCH.\"\n+           fi\n+           echo \"fresh=$FRESH\" >> \"$GITHUB_OUTPUT\"\n+           [ \"$FRESH\" = \"true\" ] || echo \"No fresh nightly mutation result — running mutation synchronously as fallback.\"\n+\n+       - name: Install Rust toolchain\n+         if: steps.nightly.outputs.fresh != 'true'\n+         uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable\n+       - name: cargo-mutants mutation (fallback — no fresh nightly)\n+         if: steps.nightly.outputs.fresh != 'true'\n+         run: |\n+           cargo install cargo-mutants --locked\n+           cargo mutants --all-features 2>&1 | tee mutation-fallback.log\n+\n+       # #1505: require a positive tool-specific mutant count; empty or unrecognized output fails closed.\n+       - name: Fail when the mutation run produced no mutants (#1505)\n+         if: steps.nightly.outputs.fresh != 'true'\n+         run: |\n+           if [ ! -s mutation-fallback.log ]; then\n+             echo \"::error::No mutation output captured — failing closed (an empty run is not a pass).\"\n+             exit 1\n+           fi\n+           if ! grep -Eqi 'tests per mutant|mutation score|[1-9][0-9]* mutants|Generated [1-9][0-9]* mutations|total is [1-9]|Caught [1-9]|KILLED|SURVIVED' mutation-fallback.log; then\n+             echo \"::error::Mutation run generated zero mutants — an empty run is not a pass (#1505).\"\n+             exit 1\n+           fi\n+           echo \"Mutation run produced mutants — fail-on-empty check passed.\"\n+\n+   secret-scan-history:\n+     name: Full-history secret scan\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read  # checkout (full history) only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       # #2100: stale /tmp/gitleaks.tmp on self-hosted runners breaks gitleaks-action retries.\n+       # ponytail: delete assumes one download per runner; concurrent jobs need the\n+       # curl -> $RUNNER_TEMP/bin installation used in 01-pr-fast.\n+       - name: Clear stale gitleaks download temp\n+         run: rm -f /tmp/gitleaks.tmp\n+       # #2673: gitleaks-action always attempts to upload a results.sarif artifact (regardless of\n+       # findings) via actions/upload-artifact's validateRootDirectory, which requires the SARIF\n+       # path to sit under the action runner's rootDirectory. On a self-hosted runner with\n+       # HOME=/root and the workspace under /tmp, that check throws even on a clean scan.\n+       # GITLEAKS_ENABLE_UPLOAD_ARTIFACT=false (verified against the pinned SHA's dist/index.js)\n+       # skips the upload path entirely; the scan itself stays blocking.\n+       - uses: gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e  # v3.0.0\n+         env:\n+           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}\n+           GITLEAKS_ENABLE_UPLOAD_ARTIFACT: 'false'\n+         with:\n+           args: '--log-opts --full-history'\n+\n+   sbom:\n+     name: Generate SBOM\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read  # checkout only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+\n+       - name: Install Rust toolchain\n+         uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable\n+       - name: Generate CycloneDX SBOM\n+         run: |\n+           cargo install cargo-cyclonedx --locked || true\n+           cargo cyclonedx --format json || true\n+           mv bom.json sbom.cdx.json 2>/dev/null || echo '{\"bomFormat\":\"CycloneDX\"}' > sbom.cdx.json\n+\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: sbom\n+           path: sbom.cdx.*\n+           retention-days: 90\n+\n+   cosign-sign:\n+     name: Sign artifact (cosign keyless)\n+     needs: [build-superset, mutation-blocking, secret-scan-history, trivy-fs-scan]\n+     if: github.event_name != 'pull_request'\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       # (M) red-team finding 6: this job only downloads an artifact, signs it and\n+       # uploads a bundle — no checkout, no push. id-token is the only grant it uses.\n+       id-token: write\n+     steps:\n+       - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: release-artifact\n+       - uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6  # v4.1.2\n+       - name: Sigstore preflight check\n+         run: |\n+           STATUS=$(curl -sf https://status.sigstore.dev/api/v2/status.json \\\n+             --max-time 10 | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d.get('status',{}).get('indicator','unknown'))\" 2>/dev/null || echo \"unknown\")\n+           if [[ \"$STATUS\" != \"none\" ]]; then\n+             echo \"::warning::Sigstore status is '${STATUS}' — cosign sign may fail\"\n+           fi\n+           echo \"sigstore-status=$STATUS\"\n+       - name: Sign artifact (keyless OIDC)\n+         run: cosign sign-blob --yes release-artifact.zip --bundle release-artifact.zip.bundle\n+\n+       - name: Verify signature (L2+ strict)\n+         run: |\n+           cosign verify-blob \\\n+             --bundle release-artifact.zip.bundle \\\n+             --certificate-identity-regexp \".*\" \\\n+             --certificate-oidc-issuer \"https://token.actions.githubusercontent.com\" \\\n+             release-artifact.zip\n+\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: cosign-bundle\n+           path: release-artifact.zip.bundle\n+           retention-days: 90\n+\n+   slsa-provenance:\n+     name: SLSA provenance (L2 signed)\n+     needs: [build-superset]\n+     if: github.event_name != 'pull_request'\n+     permissions:\n+       id-token: write\n+       contents: write\n+       actions: read\n+     uses: slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@f7dd8c54c2067bafc12ca7a55595d5ee9b75204a  # v2.1.0\n+     with:\n+       base64-subjects: ${{ needs.build-superset.outputs.hashes }}\n+       # #2673: the generator halts by default on a private repo to avoid exposing the repo name\n+       # in the public Rekor transparency log. Opt in: the name is already public via the npm\n+       # package @getarbiter/cli's trusted-publisher metadata, so the entry reveals nothing new.\n+       private-repository: true\n+       # #2673: compile-generator: false (default) downloads a prebuilt release binary, which\n+       # requires the pinned ref to look like refs/tags/vX.Y.Z (\"Invalid ref: <sha>. Expected ref\n+       # of the form refs/tags/vX.Y.Z\" — generate-builder.sh). Incompatible with pinning this call\n+       # by commit SHA (this repo's convention for every action). Compile from the pinned SHA\n+       # instead (git checkout + go build, ~2 min slower per the input's own description).\n+       compile-generator: true\n+\n+   attest-build-provenance:\n+     name: Attest build provenance\n+     needs: [build-superset]\n+     if: github.event_name != 'pull_request'\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       id-token: write\n+       attestations: write\n+       contents: read\n+     steps:\n+       - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: release-artifact\n+       - uses: actions/attest-build-provenance@e8998f949152b193b063cb0ec769d69d929409be # v2.4.0\n+         with:\n+           subject-path: release-artifact.zip\n+\n+\n+   publish-package:\n+     name: Publish package (lib)\n+     needs: [cosign-sign, attest-build-provenance, slsa-provenance, sbom, doc-freshness-gate]\n+     # #2138/#2624: tag-shaped refs alone also admit manual dispatch; require a push.\n+     if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')\n+\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read\n+       id-token: write  # required by `npm publish --provenance` (OIDC)\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           ref: ${{ github.ref }}\n+       - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: release-artifact\n+       - name: Verify the downloaded artifact is the one that was signed (#2138)\n+         # #2138: reject downloaded bytes that differ from the signed build artifact.\n+         run: sha256sum -c sha256sums.txt\n+\n+       - name: Install Rust toolchain\n+         uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable\n+       - name: Publish to crates.io\n+         run: cargo publish --no-verify\n+         env:\n+           CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}\n+\n+\n+\n+   doc-freshness-gate:\n+     name: Per-doc freshness gate (T4, gold-doc-tranches-t3-t5.md §2.3 — release-tag blocking)\n+     needs: [build-superset]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 30\n+     permissions:\n+       contents: read  # checkout (full history) only\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       - uses: ./.github/actions/setup-node-pnpm\n+       - name: Per-doc freshness audit — no stale gold doc ships in a tagged release\n+         run: node scripts/check-doc-freshness.mjs\n+\n+   release-required:\n+     name: Release gate\n+     needs:\n+       - mutation-blocking\n+       - secret-scan-history\n+       - trivy-fs-scan\n+       - sbom\n+       - cosign-sign\n+       - slsa-provenance\n+       - attest-build-provenance\n+       - doc-freshness-gate\n+\n+       - publish-package\n+\n+\n+     if: always()\n+\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions: {}  # no checkout; only reads job outcomes via toJSON(needs)\n+     steps:\n+       - name: Release gate summary\n+         id: release-results\n+         env:\n+           NEEDS_JSON: ${{ toJSON(needs) }}\n+         run: |\n+           jq -e 'length > 0 and all(.[]; .result == \"success\")' <<< \"$NEEDS_JSON\" >/dev/null\n+           echo \"Release checks completed.\"\n+           echo \"Signing, provenance, and mutation verified.\"\n+\n\n ❯ __tests__/templates/05-release-render.test.ts:86:24\n     84|     ({ language, buildTool }) => {\n     85|       const rendered = renderRelease({ language, buildTool })\n     86|       expect(rendered).toContain('sbom-attest:')\n       |                        ^\n     87|       const jobSection = rendered.split('sbom-attest:')[1]?.split(/\\n …\n     88|       expect(jobSection).toContain('cosign attest-blob')\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/6]⎯\n\n FAIL  __tests__/templates/05-release-render.test.ts > 05-release.yml.ejs — structural invariants (CANON-18) > materialized: sbom-attest job uses attest-blob, not attest, and uploads the bundle (INV-92, #2673)\nAssertionError: expected '\\n\\n\\ncosign attest --yes \\\\n  --pred…' to contain 'cosign attest-blob'\n\n- Expected\n+ Received\n\n- cosign attest-blob\n+\n+\n+\n+ cosign attest --yes \\\n+   --predicate \"$SBOM_FILE\" \\\n+   --type cyclonedx \\\n+   \"$ARTIFACT\"\n+\n\n ❯ __tests__/templates/05-release-render.test.ts:98:22\n     96|     const job = (parseYaml(materialized) as ReleaseWorkflow).jobs['sbo…\n     97|     const commands = (job.steps ?? []).map((s) => s.run ?? '').join('\\…\n     98|     expect(commands).toContain('cosign attest-blob')\n       |                      ^\n     99|     expect(commands).toContain('--bundle sbom.attestation.bundle')\n    100|     expect(commands).not.toMatch(/cosign attest --yes/)\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/6]⎯\n\n",
  "observed_failure": "FAIL  __tests__/templates/05-release-render.test.ts",
  "recorded_at": "2026-09-13T17:11:07.022Z",
  "test_command": [
    "npx",
    "vitest",
    "run",
    "__tests__/templates/05-release-render.test.ts"
  ]
}
