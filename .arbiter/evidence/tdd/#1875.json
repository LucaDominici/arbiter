{
  "$schemaVersion": 1,
  "task_id": "#1875",
  "test_path": "__tests__/templates/ci-parallel-test-jobs.test.ts",
  "test_commit_sha": "dc5e329b42ea15b068a7db1f42bcfa2f096579bf",
  "test_run_log": "\n RUN  v4.1.10 /home/luca/work/repos/arbiter.worktrees/1875-pr-fast-tiering\n\n ❯ __tests__/templates/ci-parallel-test-jobs.test.ts (12 tests | 7 failed) 34ms\n       × has contract-tests job 6ms\n       × has integration-tests job with needs: [unit-tests] 3ms\n       × has behavioral-tests job with needs: [unit-tests] 2ms\n       × ci-required needs includes unit-tests, contract-tests, integration-tests, behavioral-tests 4ms\n       × has contract-tests job 2ms\n       × has integration-tests job 2ms\n       × ci-required includes all test jobs 2ms\n\n Test Files  1 failed (1)\n      Tests  7 failed | 5 passed (12)\n   Start at  10:48:32\n   Duration  191ms (transform 65ms, setup 12ms, import 72ms, tests 34ms, environment 0ms)\n\n\n\n⎯⎯⎯⎯⎯⎯⎯ Failed Tests 7 ⎯⎯⎯⎯⎯⎯⎯\n\n FAIL  __tests__/templates/ci-parallel-test-jobs.test.ts > 01-pr-fast.yml.ejs — parallel test category jobs (#219) > TypeScript L2 > has contract-tests job\nAssertionError: expected '# Reference: docs/REFERENCE/workflow-…' to contain 'contract-tests:'\n\n- Expected\n+ Received\n\n- contract-tests:\n+ # Reference: docs/REFERENCE/workflow-pr-fast.md\n+ name: PR Fast (T1)\n+\n+ permissions:\n+   contents: read\n+\n+ on:\n+   push:\n+\n+     branches: [main, \"task/**\"]\n+\n+   pull_request:\n+     types: [opened, synchronize, reopened, labeled, ready_for_review, unlabeled]\n+\n+     branches: [main]\n+\n+     paths-ignore:\n+       - 'docs/**'\n+       - '*.md'\n+       - '.gitignore'\n+       - '.editorconfig'\n+\n+ # Cancel in-progress runs on the same ref; main always completes (#357).\n+ concurrency:\n+   group: pr-fast-${{ github.head_ref || github.ref }}\n+\n+   cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}\n+\n+\n+ jobs:\n+   config-lint:\n+     name: Config Lint (yaml + shell)\n+     # gold-align (#1506 yamllint / #1507 shellcheck): a language-agnostic style/\n+     # structure lint lane. yamllint checks non-workflow YAML (indentation, duplicate\n+     # keys, trailing spaces, document structure) — complementary to actionlint, which\n+     # owns .github/workflows *semantics*. shellcheck checks generated *.sh for unquoted\n+     # expansions / unsafe word-splitting at warning severity. Both BLOCKING; both\n+     # version-pinned for reproducibility. Each linter no-ops cleanly (green) when the\n+     # repo carries no matching files. Committed .yamllint.yml / .shellcheckrc (when\n+     # present) are auto-discovered and make the rules explicit + tunable.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - name: Install shellcheck (pinned)\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/koalaman/shellcheck/releases/download/v0.10.0/shellcheck-v0.10.0.linux.x86_64.tar.xz \\\n+             | tar -xJ -C \"$RUNNER_TEMP/bin\" --strip-components=1 shellcheck-v0.10.0/shellcheck\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: shellcheck (generated shell, blocking at warning severity)\n+         # .shellcheckrc (when present) is auto-discovered. No *.sh ⇒ no-op (green).\n+         run: |\n+           files=$(git ls-files '*.sh')\n+           if [ -n \"$files\" ]; then\n+             echo \"$files\" | xargs shellcheck --severity=warning\n+           else\n+             echo \"shellcheck: no *.sh files — skipping\"\n+           fi\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0\n+         with:\n+           python-version: '3.12'\n+       - name: Install yamllint (pinned)\n+         run: pip install yamllint==1.35.1\n+       - name: yamllint (non-workflow YAML, blocking on errors)\n+         # actionlint owns .github/workflows semantics; exclude them to avoid overlap.\n+         # A committed .yamllint.yml (when present) is auto-discovered and authoritative;\n+         # otherwise fall back to yamllint's built-in `relaxed` preset — both block on\n+         # ERRORS only (line-length etc. stay advisory warnings). No matching YAML ⇒ no-op.\n+         run: |\n+           files=$(git ls-files '*.yml' '*.yaml' ':!.github/workflows/**')\n+           if [ -n \"$files\" ]; then\n+             if [ -f .yamllint.yml ] || [ -f .yamllint.yaml ] || [ -f .yamllint ]; then\n+               echo \"$files\" | xargs yamllint\n+             else\n+               echo \"$files\" | xargs yamllint -d relaxed\n+             fi\n+           else\n+             echo \"yamllint: no non-workflow YAML — skipping\"\n+           fi\n+\n+   security-early-fail:\n+     name: Security (PII + Secrets)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+       - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0\n+         with:\n+           node-version-file: '.nvmrc'\n+       - name: PII scan (INV-12, HARD)\n+         run: node scripts/pii-scan.mjs\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Gitleaks secrets scan (INV-11)\n+         # --log-opts=\"HEAD\" (#1908): without it, gitleaks defaults to scanning\n+         # ALL refs reachable in the checkout's object DB (fetch-depth: 0 above\n+         # fetches every remote branch, not just this one). That let an unrelated,\n+         # unmerged branch's commit cross-contaminate every other PR's scan.\n+         # Scoping to HEAD still walks this ref's FULL history (no depth lost) —\n+         # it just stops other branches' commits from being scanned here. The\n+         # nightly `gitleaks-history` job intentionally keeps `--all --full-history`\n+         # as the deep, cross-branch safety net.\n+         run: |\n+           gitleaks detect --source . --config .gitleaks.toml \\\n+             --gitleaks-ignore-path suppressions/.gitleaksignore \\\n+             --log-opts=\"HEAD\" \\\n+             --exit-code 1 --redact\n+\n+\n+   classify-changes:\n+     name: Classify Changes\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     outputs:\n+       docs_only: ${{ steps.classify.outputs.docs_only }}\n+       backend_changed: ${{ steps.classify.outputs.backend_changed }}\n+       frontend_changed: ${{ steps.classify.outputs.frontend_changed }}\n+       infra_changed: ${{ steps.classify.outputs.infra_changed }}\n+       high_risk: ${{ steps.classify.outputs.high_risk }}\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+       - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0\n+         with:\n+           node-version-file: '.nvmrc'\n+       - id: classify\n+         if: hashFiles('scripts/ci-classify-changes.mjs') != ''\n+         run: node scripts/ci-classify-changes.mjs\n+         env:\n+           BASE_SHA: ${{ github.event.pull_request.base.sha || 'origin/main' }}\n+           HEAD_SHA: ${{ github.sha }}\n+\n+\n+   dependency-review:\n+     name: Dependency Review (supply-chain)\n+     # A1 (#1502): the canonical FAST PR supply-chain gate. dependency-review-action\n+     # diffs the PR's dependency-manifest changes and fails on a newly-introduced\n+     # vulnerable dependency — keeping slow full-tree scanners off the always-path.\n+     # PR-only (the action diffs base...head and errors on push). Fail severity is\n+     # configurable via the DEPENDENCY_REVIEW_FAIL_SEVERITY repo variable (default: high).\n+     # Floor: enableSecurityScanning — the same always-path gate as security-early-fail.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     # PR-only AND GHAS-gated (#1685): dependency-review-action HARD-errors when the\n+     # repo lacks Dependency Graph + GitHub Advanced Security, which reds every PR\n+     # while GHAS is off. A JOB-LEVEL guard makes the job SKIP (result=skipped, which\n+     # the ci-required aggregator treats as non-failure) until GHAS is enabled by\n+     # setting the GHAS_ENABLED repo variable to 'true'. NOT step-level\n+     # continue-on-error: that would violate INV-80 and silently neuter the gate once\n+     # GHAS is on. With the var set, a real finding still fails the job and blocks.\n+     if: github.event_name == 'pull_request' && vars.GHAS_ENABLED == 'true'\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - name: Dependency review (fail on newly-introduced high-severity deps)\n+         uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294  # v5.0.0\n+         with:\n+           fail-on-severity: ${{ vars.DEPENDENCY_REVIEW_FAIL_SEVERITY || 'high' }}\n+\n+   iac-scan:\n+     name: IaC Scan (infra-changed)\n+     # A1 (#1502): consume the classify-changes `infra_changed` signal (previously\n+     # classified but UNUSED) — scan terraform / kubernetes / dockerfile only when the\n+     # PR actually touches infrastructure-as-code, keeping the always-path lean.\n+     # #1785: was pinned to a LITERAL `runs-on: ubuntu-latest` on the (disproven)\n+     # assumption that GitHub-hosted runners are never containerized (#1756).\n+     # Evidence (run 28693108584) showed a job pinned to literal `ubuntu-latest`\n+     # actually serviced by self-hosted `arbiter-slot-build-3` — the label alone\n+     # never guaranteed real GitHub-hosted infra here. Rather than keep trusting\n+     # an unverified runner label, Checkov now runs as a plain pip-installed CLI\n+     # (bridgecrewio/checkov-action was only a Docker convenience wrapper around\n+     # it) — no docker-container action, no workspace bind-mount, the whole\n+     # defect class sidestepped entirely (same pattern as the osv-scanner static-\n+     # binary fix, #1767). Enforced by scripts/check-docker-action-runner-safety.mjs.\n+     # #1894: routed through the same `RUNNER_LABELS_TEST` expression as every other\n+     # job. The former literal `ubuntu-latest` used to be intercepted by the self-\n+     # hosted fleet (#1785 evidence), but the fleet dropped the `ubuntu-latest`\n+     # label, so the literal pin fell through to real GitHub-hosted infra and failed\n+     # (spending-limit block). The expression defaults to `[\"ubuntu-latest\"]` when\n+     # the var is unset (consumers unaffected); arbiter's own repo sets it to the\n+     # self-hosted labels. Checkov/tflint are plain CLIs (no docker-action), so the\n+     # runner class remains moot for the #1785 safety class either way.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [classify-changes]\n+     if: needs.classify-changes.outputs.infra_changed == 'true'\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0\n+         with:\n+           python-version: '3.12'\n+       - name: Install Checkov (pinned, no Docker — #1785)\n+         run: pip install checkov==3.3.7\n+       - name: Checkov IaC scan (all frameworks)\n+         run: |\n+           # #1685: `all` is checkov's documented-valid multi-framework value (also\n+           # its own default scope). Hard-fail stays the default (no soft-fail\n+           # flag passed): a real misconfiguration must block (INV-80).\n+           checkov --directory . --framework all --quiet --compact\n+       # #1509 gold-align: Checkov scans for known misconfigurations; tflint is the\n+       # Terraform-specific *linter* (provider-aware deprecations, invalid arg\n+       # combinations, unused declarations). Reference-grade IaC pipelines run it\n+       # BLOCKING — no soft-fail. `--recursive` exits 0 when the repo carries no\n+       # `.tf` files, so non-Terraform infra changes (k8s/dockerfile) stay green.\n+       - name: Setup tflint (Terraform linter)\n+         uses: terraform-linters/setup-tflint@6e1e0642c0289bd619021bf6b34e3c08ed1e005a  # v6.3.0\n+       - name: tflint (Terraform lint, blocking)\n+         run: tflint --recursive --no-color\n+\n+\n+\n+   gate:\n+     name: Lint + Typecheck\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [security-early-fail, classify-changes]\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: ./.github/actions/setup-node-pnpm\n+       - run: npm run typecheck\n+       - run: npm run lint\n+\n+       - name: Dep audit (INV-13)\n+         run: npm audit --omit=dev --audit-level=high\n+\n+\n+   unit-tests:\n+     name: Unit Tests\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate, classify-changes]\n+     strategy:\n+       max-parallel: 2\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: ./.github/actions/setup-node-pnpm\n+       - run: npm run test:unit -- --reporter=junit --outputFile=test-results/junit.xml\n+\n+       - name: Upload test results\n+         if: always()\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: test-results-unit-${{ github.run_id }}\n+           path: test-results/\n+           retention-days: 7\n+\n+\n+\n+\n+\n+   docs-check:\n+     name: Docs Check\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     if: github.event_name == 'pull_request'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+       - name: Check docs updated with code changes\n+         env:\n+           BASE_SHA: ${{ github.event.pull_request.base.sha }}\n+           HEAD_SHA: ${{ github.event.pull_request.head.sha }}\n+         run: |\n+           # #356: rebased-aware via merge-base; bypass via [skip-docs] commit token.\n+           MB=$(git merge-base \"$BASE_SHA\" \"$HEAD_SHA\" || echo \"$BASE_SHA\")\n+           CHANGED=$(git diff --name-only \"$MB\" \"$HEAD_SHA\")\n+           LOG=$(git log --format=%B \"$MB..$HEAD_SHA\" || true)\n+\n+           HAS_CODE=$(echo \"$CHANGED\" | grep -E '^(src/|__tests__/)' || true)\n+           HAS_DOCS=$(echo \"$CHANGED\" | grep -E '^(docs/|README\\.md)' || true)\n+           BYPASS=$(echo \"$LOG\" | grep -F '[skip-docs]' || true)\n+\n+           if [ -n \"$HAS_CODE\" ] && [ -z \"$HAS_DOCS\" ] && [ -z \"$BYPASS\" ]; then\n+             echo \"::error::Code changed without documentation update\"\n+             echo \"\"\n+             echo \"Files changed in src/ or __tests__/:\"\n+             echo \"$HAS_CODE\"\n+             echo \"\"\n+             echo \"Please update docs/ or README.md, or add [skip-docs] to a commit message to bypass.\"\n+             exit 1\n+           fi\n+           echo \"Docs check passed.\"\n+\n+\n+\n+   debt-gates:\n+     name: Tech Debt Gates\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate]\n+     # E1 (#1502): the PR leg keeps only the FAST static-analysis gates\n+     # (knip/madge/pmd/spotbugs/clippy/ruff/golangci). The whole-suite coverage\n+     # re-run that used to live here was a SECOND full test execution on the PR\n+     # (the unit/integration jobs already ran the suite, and `gate-full` runs\n+     # check-all L2 which enforces the coverage threshold). The threshold floor is\n+     # therefore preserved on PR via gate-full; the heavy coverage report (HTML/lcov\n+     # artifact) is produced at nightly cadence in 06-nightly `coverage-report`.\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+       - run: npx knip\n+       - run: npx madge --circular --extensions ts,tsx,js,jsx src\n+\n+\n+\n+\n+   sonar-scan:\n+     name: SonarCloud Scan\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate]\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+\n+\n+       - name: SonarCloud Scan (PR decoration — incremental new-code analysis)\n+         # E1 (#1502): on PRs Sonar runs in pull-request mode — it decorates the PR\n+         # with new-code findings only (incremental), NOT a full deep scan. The full\n+         # branch baseline runs on merge-to-main below and nightly (06-nightly\n+         # `sonar-deep-scan`), keeping the PR path fast.\n+         if: env.SONAR_TOKEN != '' && github.event_name == 'pull_request'\n+         env:\n+           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}\n+           # untrusted refs pass through env (never inlined into the script body).\n+           PR_KEY: ${{ github.event.pull_request.number }}\n+           PR_BRANCH: ${{ github.head_ref }}\n+           PR_BASE: ${{ github.base_ref }}\n+         run: |\n+           npx sonar-scanner -Dsonar.token=\"$SONAR_TOKEN\" \\\n+             -Dsonar.pullrequest.key=\"$PR_KEY\" \\\n+             -Dsonar.pullrequest.branch=\"$PR_BRANCH\" \\\n+             -Dsonar.pullrequest.base=\"$PR_BASE\"\n+       - name: SonarCloud Scan (full branch baseline)\n+         # Push to a long-lived branch refreshes the full-project baseline so the\n+         # PR decoration above has a fresh reference to diff new code against.\n+         if: env.SONAR_TOKEN != '' && github.event_name != 'pull_request'\n+         env:\n+           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}\n+         run: |\n+           npx sonar-scanner -Dsonar.token=\"$SONAR_TOKEN\"\n+\n+\n+\n+   debt-ratchet:\n+     name: Debt Ratchet\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate]\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+\n+       - name: Run debt ratchet\n+         run: node scripts/debt-report.mjs --gate\n+\n+\n+\n+   gate-full:\n+     name: Full Gate (L2, INV-59)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+\n+     needs: [security-early-fail, classify-changes]\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Install actionlint\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/rhysd/actionlint/releases/download/v1.7.3/actionlint_1.7.3_linux_amd64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" actionlint\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Build\n+         run: npm run build\n+       - name: Run full L2 gate (== pre-push)\n+         run: node scripts/check-all.mjs L2 --json gate-result.json\n+       - name: Upload gate-result artifact (activates INV-59 runtime parity)\n+         if: always()\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: gate-result\n+           path: gate-result.json\n+           retention-days: 7\n+\n+\n+   human-approval-required:\n+     name: Human Approval Required (INV-74)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     if: github.event_name == 'pull_request'\n+     steps:\n+       - name: Assert approved-by-human label (anti-bot gate)\n+         uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3  # v9.0.0\n+         with:\n+           github-token: ${{ secrets.GITHUB_TOKEN }}\n+           script: |\n+             const labels = context.payload.pull_request.labels.map(l => l.name);\n+             if (!labels.includes('approved-by-human')) {\n+               core.setFailed(\n+                 'PR requires the \"approved-by-human\" label applied by a human reviewer. ' +\n+                 'See 03-human-approval.yml — a bot approval does not satisfy this check.'\n+               );\n+             }\n+\n+   ci-required:\n+     name: CI Required\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+\n+     needs: [gate, human-approval-required, unit-tests, security-early-fail, dependency-review, iac-scan, docs-check, sonar-scan, gate-full, debt-gates, debt-ratchet, classify-changes]\n+     if: always()\n+     steps:\n+       - env:\n+           DOCS_ONLY: ${{ needs.classify-changes.outputs.docs_only }}\n+         run: |\n+\n+           if [[ \"${{ needs.classify-changes.result }}\" != \"success\" ]]; then\n+             echo \"classify-changes failed — cannot trust docs_only, failing closed\"\n+             exit 1\n+           fi\n+           # success, or skipped ONLY when the docs-only classification skipped the job\n+           ok() { [[ \"$1\" == \"success\" || ( \"$DOCS_ONLY\" == \"true\" && \"$1\" == \"skipped\" ) ]]; }\n+           if ! ok \"${{ needs.gate.result }}\"; then\n+             echo \"gate failed\"\n+             exit 1\n+           fi\n+\n+           if ! ok \"${{ needs.unit-tests.result }}\"; then\n+             echo \"unit-tests failed\"\n+             exit 1\n+           fi\n+\n+           if [[ \"${{ needs.security-early-fail.result }}\" != \"success\" ]]; then\n+             echo \"security-early-fail failed\"\n+             exit 1\n+           fi\n+           # dependency-review skips on push (PR-only) — only a real finding blocks.\n+           if [[ \"${{ needs.dependency-review.result }}\" == \"failure\" ]]; then\n+             echo \"dependency-review failed — a newly-introduced dependency exceeds the severity floor\"\n+             exit 1\n+           fi\n+           # iac-scan skips when no infra changed — only a real IaC finding blocks.\n+           if [[ \"${{ needs.iac-scan.result }}\" == \"failure\" ]]; then\n+             echo \"iac-scan failed — an infrastructure-as-code misconfiguration was found\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.docs-check.result }}\" == \"failure\" ]]; then\n+             echo \"docs-check failed\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.sonar-scan.result }}\" == \"failure\" ]]; then\n+             echo \"sonar-scan failed\"\n+             exit 1\n+           fi\n+           if ! ok \"${{ needs.debt-gates.result }}\"; then\n+             echo \"debt-gates failed\"\n+             exit 1\n+           fi\n+           if ! ok \"${{ needs.debt-ratchet.result }}\"; then\n+             echo \"debt-ratchet failed\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.gate-full.result }}\" != \"success\" && \"${{ needs.gate-full.result }}\" != \"skipped\" ]]; then\n+             echo \"gate-full (L2) failed or was cancelled\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.human-approval-required.result }}\" == \"failure\" ]]; then\n+             echo \"human-approval-required failed — PR needs approved-by-human label\"\n+             exit 1\n+           fi\n+\n+\n+   post-merge-gate:\n+     name: D4-LITE post-merge gate (main)\n+     needs: [classify-changes]\n+     if: >\n+       github.event_name == 'push' &&\n+       github.ref == 'refs/heads/main' &&\n+       needs.classify-changes.result == 'success' &&\n+       needs.classify-changes.outputs.docs_only != 'true'\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Install actionlint\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/rhysd/actionlint/releases/download/v1.7.3/actionlint_1.7.3_linux_amd64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" actionlint\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Build\n+         run: npm run build\n+       - name: Run full L2 gate (D4-LITE catch-net)\n+         run: node scripts/check-all.mjs L2\n+\n+   post-merge-notify:\n+     name: D4-LITE notify on failure\n+     needs: [post-merge-gate]\n+     if: always() && needs.post-merge-gate.result == 'failure'\n+     permissions:\n+       issues: write\n+     uses: ./.github/workflows/_notify.yml\n+     with:\n+       workflow_name: D4-LITE post-merge gate\n+       area: ci\n+       severity: P1\n+       branch: main\n+       run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\n+       failure_summary: Post-merge L2 gate failed on main — a change merged without full verification.\n+       status: failure\n+\n+\n+\n\n ❯ __tests__/templates/ci-parallel-test-jobs.test.ts:22:24\n     20|       }) as unknown as Record<string, unknown>\n     21|       const rendered = renderTemplate('github/workflows/01-pr-fast.yml…\n     22|       expect(rendered).toContain('contract-tests:')\n       |                        ^\n     23|     })\n     24|\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/7]⎯\n\n FAIL  __tests__/templates/ci-parallel-test-jobs.test.ts > 01-pr-fast.yml.ejs — parallel test category jobs (#219) > TypeScript L2 > has integration-tests job with needs: [unit-tests]\nAssertionError: expected '# Reference: docs/REFERENCE/workflow-…' to contain 'integration-tests:'\n\n- Expected\n+ Received\n\n- integration-tests:\n+ # Reference: docs/REFERENCE/workflow-pr-fast.md\n+ name: PR Fast (T1)\n+\n+ permissions:\n+   contents: read\n+\n+ on:\n+   push:\n+\n+     branches: [main, \"task/**\"]\n+\n+   pull_request:\n+     types: [opened, synchronize, reopened, labeled, ready_for_review, unlabeled]\n+\n+     branches: [main]\n+\n+     paths-ignore:\n+       - 'docs/**'\n+       - '*.md'\n+       - '.gitignore'\n+       - '.editorconfig'\n+\n+ # Cancel in-progress runs on the same ref; main always completes (#357).\n+ concurrency:\n+   group: pr-fast-${{ github.head_ref || github.ref }}\n+\n+   cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}\n+\n+\n+ jobs:\n+   config-lint:\n+     name: Config Lint (yaml + shell)\n+     # gold-align (#1506 yamllint / #1507 shellcheck): a language-agnostic style/\n+     # structure lint lane. yamllint checks non-workflow YAML (indentation, duplicate\n+     # keys, trailing spaces, document structure) — complementary to actionlint, which\n+     # owns .github/workflows *semantics*. shellcheck checks generated *.sh for unquoted\n+     # expansions / unsafe word-splitting at warning severity. Both BLOCKING; both\n+     # version-pinned for reproducibility. Each linter no-ops cleanly (green) when the\n+     # repo carries no matching files. Committed .yamllint.yml / .shellcheckrc (when\n+     # present) are auto-discovered and make the rules explicit + tunable.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - name: Install shellcheck (pinned)\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/koalaman/shellcheck/releases/download/v0.10.0/shellcheck-v0.10.0.linux.x86_64.tar.xz \\\n+             | tar -xJ -C \"$RUNNER_TEMP/bin\" --strip-components=1 shellcheck-v0.10.0/shellcheck\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: shellcheck (generated shell, blocking at warning severity)\n+         # .shellcheckrc (when present) is auto-discovered. No *.sh ⇒ no-op (green).\n+         run: |\n+           files=$(git ls-files '*.sh')\n+           if [ -n \"$files\" ]; then\n+             echo \"$files\" | xargs shellcheck --severity=warning\n+           else\n+             echo \"shellcheck: no *.sh files — skipping\"\n+           fi\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0\n+         with:\n+           python-version: '3.12'\n+       - name: Install yamllint (pinned)\n+         run: pip install yamllint==1.35.1\n+       - name: yamllint (non-workflow YAML, blocking on errors)\n+         # actionlint owns .github/workflows semantics; exclude them to avoid overlap.\n+         # A committed .yamllint.yml (when present) is auto-discovered and authoritative;\n+         # otherwise fall back to yamllint's built-in `relaxed` preset — both block on\n+         # ERRORS only (line-length etc. stay advisory warnings). No matching YAML ⇒ no-op.\n+         run: |\n+           files=$(git ls-files '*.yml' '*.yaml' ':!.github/workflows/**')\n+           if [ -n \"$files\" ]; then\n+             if [ -f .yamllint.yml ] || [ -f .yamllint.yaml ] || [ -f .yamllint ]; then\n+               echo \"$files\" | xargs yamllint\n+             else\n+               echo \"$files\" | xargs yamllint -d relaxed\n+             fi\n+           else\n+             echo \"yamllint: no non-workflow YAML — skipping\"\n+           fi\n+\n+   security-early-fail:\n+     name: Security (PII + Secrets)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+       - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0\n+         with:\n+           node-version-file: '.nvmrc'\n+       - name: PII scan (INV-12, HARD)\n+         run: node scripts/pii-scan.mjs\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Gitleaks secrets scan (INV-11)\n+         # --log-opts=\"HEAD\" (#1908): without it, gitleaks defaults to scanning\n+         # ALL refs reachable in the checkout's object DB (fetch-depth: 0 above\n+         # fetches every remote branch, not just this one). That let an unrelated,\n+         # unmerged branch's commit cross-contaminate every other PR's scan.\n+         # Scoping to HEAD still walks this ref's FULL history (no depth lost) —\n+         # it just stops other branches' commits from being scanned here. The\n+         # nightly `gitleaks-history` job intentionally keeps `--all --full-history`\n+         # as the deep, cross-branch safety net.\n+         run: |\n+           gitleaks detect --source . --config .gitleaks.toml \\\n+             --gitleaks-ignore-path suppressions/.gitleaksignore \\\n+             --log-opts=\"HEAD\" \\\n+             --exit-code 1 --redact\n+\n+\n+   classify-changes:\n+     name: Classify Changes\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     outputs:\n+       docs_only: ${{ steps.classify.outputs.docs_only }}\n+       backend_changed: ${{ steps.classify.outputs.backend_changed }}\n+       frontend_changed: ${{ steps.classify.outputs.frontend_changed }}\n+       infra_changed: ${{ steps.classify.outputs.infra_changed }}\n+       high_risk: ${{ steps.classify.outputs.high_risk }}\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+       - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0\n+         with:\n+           node-version-file: '.nvmrc'\n+       - id: classify\n+         if: hashFiles('scripts/ci-classify-changes.mjs') != ''\n+         run: node scripts/ci-classify-changes.mjs\n+         env:\n+           BASE_SHA: ${{ github.event.pull_request.base.sha || 'origin/main' }}\n+           HEAD_SHA: ${{ github.sha }}\n+\n+\n+   dependency-review:\n+     name: Dependency Review (supply-chain)\n+     # A1 (#1502): the canonical FAST PR supply-chain gate. dependency-review-action\n+     # diffs the PR's dependency-manifest changes and fails on a newly-introduced\n+     # vulnerable dependency — keeping slow full-tree scanners off the always-path.\n+     # PR-only (the action diffs base...head and errors on push). Fail severity is\n+     # configurable via the DEPENDENCY_REVIEW_FAIL_SEVERITY repo variable (default: high).\n+     # Floor: enableSecurityScanning — the same always-path gate as security-early-fail.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     # PR-only AND GHAS-gated (#1685): dependency-review-action HARD-errors when the\n+     # repo lacks Dependency Graph + GitHub Advanced Security, which reds every PR\n+     # while GHAS is off. A JOB-LEVEL guard makes the job SKIP (result=skipped, which\n+     # the ci-required aggregator treats as non-failure) until GHAS is enabled by\n+     # setting the GHAS_ENABLED repo variable to 'true'. NOT step-level\n+     # continue-on-error: that would violate INV-80 and silently neuter the gate once\n+     # GHAS is on. With the var set, a real finding still fails the job and blocks.\n+     if: github.event_name == 'pull_request' && vars.GHAS_ENABLED == 'true'\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - name: Dependency review (fail on newly-introduced high-severity deps)\n+         uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294  # v5.0.0\n+         with:\n+           fail-on-severity: ${{ vars.DEPENDENCY_REVIEW_FAIL_SEVERITY || 'high' }}\n+\n+   iac-scan:\n+     name: IaC Scan (infra-changed)\n+     # A1 (#1502): consume the classify-changes `infra_changed` signal (previously\n+     # classified but UNUSED) — scan terraform / kubernetes / dockerfile only when the\n+     # PR actually touches infrastructure-as-code, keeping the always-path lean.\n+     # #1785: was pinned to a LITERAL `runs-on: ubuntu-latest` on the (disproven)\n+     # assumption that GitHub-hosted runners are never containerized (#1756).\n+     # Evidence (run 28693108584) showed a job pinned to literal `ubuntu-latest`\n+     # actually serviced by self-hosted `arbiter-slot-build-3` — the label alone\n+     # never guaranteed real GitHub-hosted infra here. Rather than keep trusting\n+     # an unverified runner label, Checkov now runs as a plain pip-installed CLI\n+     # (bridgecrewio/checkov-action was only a Docker convenience wrapper around\n+     # it) — no docker-container action, no workspace bind-mount, the whole\n+     # defect class sidestepped entirely (same pattern as the osv-scanner static-\n+     # binary fix, #1767). Enforced by scripts/check-docker-action-runner-safety.mjs.\n+     # #1894: routed through the same `RUNNER_LABELS_TEST` expression as every other\n+     # job. The former literal `ubuntu-latest` used to be intercepted by the self-\n+     # hosted fleet (#1785 evidence), but the fleet dropped the `ubuntu-latest`\n+     # label, so the literal pin fell through to real GitHub-hosted infra and failed\n+     # (spending-limit block). The expression defaults to `[\"ubuntu-latest\"]` when\n+     # the var is unset (consumers unaffected); arbiter's own repo sets it to the\n+     # self-hosted labels. Checkov/tflint are plain CLIs (no docker-action), so the\n+     # runner class remains moot for the #1785 safety class either way.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [classify-changes]\n+     if: needs.classify-changes.outputs.infra_changed == 'true'\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0\n+         with:\n+           python-version: '3.12'\n+       - name: Install Checkov (pinned, no Docker — #1785)\n+         run: pip install checkov==3.3.7\n+       - name: Checkov IaC scan (all frameworks)\n+         run: |\n+           # #1685: `all` is checkov's documented-valid multi-framework value (also\n+           # its own default scope). Hard-fail stays the default (no soft-fail\n+           # flag passed): a real misconfiguration must block (INV-80).\n+           checkov --directory . --framework all --quiet --compact\n+       # #1509 gold-align: Checkov scans for known misconfigurations; tflint is the\n+       # Terraform-specific *linter* (provider-aware deprecations, invalid arg\n+       # combinations, unused declarations). Reference-grade IaC pipelines run it\n+       # BLOCKING — no soft-fail. `--recursive` exits 0 when the repo carries no\n+       # `.tf` files, so non-Terraform infra changes (k8s/dockerfile) stay green.\n+       - name: Setup tflint (Terraform linter)\n+         uses: terraform-linters/setup-tflint@6e1e0642c0289bd619021bf6b34e3c08ed1e005a  # v6.3.0\n+       - name: tflint (Terraform lint, blocking)\n+         run: tflint --recursive --no-color\n+\n+\n+\n+   gate:\n+     name: Lint + Typecheck\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [security-early-fail, classify-changes]\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: ./.github/actions/setup-node-pnpm\n+       - run: npm run typecheck\n+       - run: npm run lint\n+\n+       - name: Dep audit (INV-13)\n+         run: npm audit --omit=dev --audit-level=high\n+\n+\n+   unit-tests:\n+     name: Unit Tests\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate, classify-changes]\n+     strategy:\n+       max-parallel: 2\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: ./.github/actions/setup-node-pnpm\n+       - run: npm run test:unit -- --reporter=junit --outputFile=test-results/junit.xml\n+\n+       - name: Upload test results\n+         if: always()\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: test-results-unit-${{ github.run_id }}\n+           path: test-results/\n+           retention-days: 7\n+\n+\n+\n+\n+\n+   docs-check:\n+     name: Docs Check\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     if: github.event_name == 'pull_request'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+       - name: Check docs updated with code changes\n+         env:\n+           BASE_SHA: ${{ github.event.pull_request.base.sha }}\n+           HEAD_SHA: ${{ github.event.pull_request.head.sha }}\n+         run: |\n+           # #356: rebased-aware via merge-base; bypass via [skip-docs] commit token.\n+           MB=$(git merge-base \"$BASE_SHA\" \"$HEAD_SHA\" || echo \"$BASE_SHA\")\n+           CHANGED=$(git diff --name-only \"$MB\" \"$HEAD_SHA\")\n+           LOG=$(git log --format=%B \"$MB..$HEAD_SHA\" || true)\n+\n+           HAS_CODE=$(echo \"$CHANGED\" | grep -E '^(src/|__tests__/)' || true)\n+           HAS_DOCS=$(echo \"$CHANGED\" | grep -E '^(docs/|README\\.md)' || true)\n+           BYPASS=$(echo \"$LOG\" | grep -F '[skip-docs]' || true)\n+\n+           if [ -n \"$HAS_CODE\" ] && [ -z \"$HAS_DOCS\" ] && [ -z \"$BYPASS\" ]; then\n+             echo \"::error::Code changed without documentation update\"\n+             echo \"\"\n+             echo \"Files changed in src/ or __tests__/:\"\n+             echo \"$HAS_CODE\"\n+             echo \"\"\n+             echo \"Please update docs/ or README.md, or add [skip-docs] to a commit message to bypass.\"\n+             exit 1\n+           fi\n+           echo \"Docs check passed.\"\n+\n+\n+\n+   debt-gates:\n+     name: Tech Debt Gates\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate]\n+     # E1 (#1502): the PR leg keeps only the FAST static-analysis gates\n+     # (knip/madge/pmd/spotbugs/clippy/ruff/golangci). The whole-suite coverage\n+     # re-run that used to live here was a SECOND full test execution on the PR\n+     # (the unit/integration jobs already ran the suite, and `gate-full` runs\n+     # check-all L2 which enforces the coverage threshold). The threshold floor is\n+     # therefore preserved on PR via gate-full; the heavy coverage report (HTML/lcov\n+     # artifact) is produced at nightly cadence in 06-nightly `coverage-report`.\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+       - run: npx knip\n+       - run: npx madge --circular --extensions ts,tsx,js,jsx src\n+\n+\n+\n+\n+   sonar-scan:\n+     name: SonarCloud Scan\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate]\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+\n+\n+       - name: SonarCloud Scan (PR decoration — incremental new-code analysis)\n+         # E1 (#1502): on PRs Sonar runs in pull-request mode — it decorates the PR\n+         # with new-code findings only (incremental), NOT a full deep scan. The full\n+         # branch baseline runs on merge-to-main below and nightly (06-nightly\n+         # `sonar-deep-scan`), keeping the PR path fast.\n+         if: env.SONAR_TOKEN != '' && github.event_name == 'pull_request'\n+         env:\n+           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}\n+           # untrusted refs pass through env (never inlined into the script body).\n+           PR_KEY: ${{ github.event.pull_request.number }}\n+           PR_BRANCH: ${{ github.head_ref }}\n+           PR_BASE: ${{ github.base_ref }}\n+         run: |\n+           npx sonar-scanner -Dsonar.token=\"$SONAR_TOKEN\" \\\n+             -Dsonar.pullrequest.key=\"$PR_KEY\" \\\n+             -Dsonar.pullrequest.branch=\"$PR_BRANCH\" \\\n+             -Dsonar.pullrequest.base=\"$PR_BASE\"\n+       - name: SonarCloud Scan (full branch baseline)\n+         # Push to a long-lived branch refreshes the full-project baseline so the\n+         # PR decoration above has a fresh reference to diff new code against.\n+         if: env.SONAR_TOKEN != '' && github.event_name != 'pull_request'\n+         env:\n+           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}\n+         run: |\n+           npx sonar-scanner -Dsonar.token=\"$SONAR_TOKEN\"\n+\n+\n+\n+   debt-ratchet:\n+     name: Debt Ratchet\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate]\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+\n+       - name: Run debt ratchet\n+         run: node scripts/debt-report.mjs --gate\n+\n+\n+\n+   gate-full:\n+     name: Full Gate (L2, INV-59)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+\n+     needs: [security-early-fail, classify-changes]\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Install actionlint\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/rhysd/actionlint/releases/download/v1.7.3/actionlint_1.7.3_linux_amd64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" actionlint\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Build\n+         run: npm run build\n+       - name: Run full L2 gate (== pre-push)\n+         run: node scripts/check-all.mjs L2 --json gate-result.json\n+       - name: Upload gate-result artifact (activates INV-59 runtime parity)\n+         if: always()\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: gate-result\n+           path: gate-result.json\n+           retention-days: 7\n+\n+\n+   human-approval-required:\n+     name: Human Approval Required (INV-74)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     if: github.event_name == 'pull_request'\n+     steps:\n+       - name: Assert approved-by-human label (anti-bot gate)\n+         uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3  # v9.0.0\n+         with:\n+           github-token: ${{ secrets.GITHUB_TOKEN }}\n+           script: |\n+             const labels = context.payload.pull_request.labels.map(l => l.name);\n+             if (!labels.includes('approved-by-human')) {\n+               core.setFailed(\n+                 'PR requires the \"approved-by-human\" label applied by a human reviewer. ' +\n+                 'See 03-human-approval.yml — a bot approval does not satisfy this check.'\n+               );\n+             }\n+\n+   ci-required:\n+     name: CI Required\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+\n+     needs: [gate, human-approval-required, unit-tests, security-early-fail, dependency-review, iac-scan, docs-check, sonar-scan, gate-full, debt-gates, debt-ratchet, classify-changes]\n+     if: always()\n+     steps:\n+       - env:\n+           DOCS_ONLY: ${{ needs.classify-changes.outputs.docs_only }}\n+         run: |\n+\n+           if [[ \"${{ needs.classify-changes.result }}\" != \"success\" ]]; then\n+             echo \"classify-changes failed — cannot trust docs_only, failing closed\"\n+             exit 1\n+           fi\n+           # success, or skipped ONLY when the docs-only classification skipped the job\n+           ok() { [[ \"$1\" == \"success\" || ( \"$DOCS_ONLY\" == \"true\" && \"$1\" == \"skipped\" ) ]]; }\n+           if ! ok \"${{ needs.gate.result }}\"; then\n+             echo \"gate failed\"\n+             exit 1\n+           fi\n+\n+           if ! ok \"${{ needs.unit-tests.result }}\"; then\n+             echo \"unit-tests failed\"\n+             exit 1\n+           fi\n+\n+           if [[ \"${{ needs.security-early-fail.result }}\" != \"success\" ]]; then\n+             echo \"security-early-fail failed\"\n+             exit 1\n+           fi\n+           # dependency-review skips on push (PR-only) — only a real finding blocks.\n+           if [[ \"${{ needs.dependency-review.result }}\" == \"failure\" ]]; then\n+             echo \"dependency-review failed — a newly-introduced dependency exceeds the severity floor\"\n+             exit 1\n+           fi\n+           # iac-scan skips when no infra changed — only a real IaC finding blocks.\n+           if [[ \"${{ needs.iac-scan.result }}\" == \"failure\" ]]; then\n+             echo \"iac-scan failed — an infrastructure-as-code misconfiguration was found\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.docs-check.result }}\" == \"failure\" ]]; then\n+             echo \"docs-check failed\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.sonar-scan.result }}\" == \"failure\" ]]; then\n+             echo \"sonar-scan failed\"\n+             exit 1\n+           fi\n+           if ! ok \"${{ needs.debt-gates.result }}\"; then\n+             echo \"debt-gates failed\"\n+             exit 1\n+           fi\n+           if ! ok \"${{ needs.debt-ratchet.result }}\"; then\n+             echo \"debt-ratchet failed\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.gate-full.result }}\" != \"success\" && \"${{ needs.gate-full.result }}\" != \"skipped\" ]]; then\n+             echo \"gate-full (L2) failed or was cancelled\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.human-approval-required.result }}\" == \"failure\" ]]; then\n+             echo \"human-approval-required failed — PR needs approved-by-human label\"\n+             exit 1\n+           fi\n+\n+\n+   post-merge-gate:\n+     name: D4-LITE post-merge gate (main)\n+     needs: [classify-changes]\n+     if: >\n+       github.event_name == 'push' &&\n+       github.ref == 'refs/heads/main' &&\n+       needs.classify-changes.result == 'success' &&\n+       needs.classify-changes.outputs.docs_only != 'true'\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Install actionlint\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/rhysd/actionlint/releases/download/v1.7.3/actionlint_1.7.3_linux_amd64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" actionlint\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Build\n+         run: npm run build\n+       - name: Run full L2 gate (D4-LITE catch-net)\n+         run: node scripts/check-all.mjs L2\n+\n+   post-merge-notify:\n+     name: D4-LITE notify on failure\n+     needs: [post-merge-gate]\n+     if: always() && needs.post-merge-gate.result == 'failure'\n+     permissions:\n+       issues: write\n+     uses: ./.github/workflows/_notify.yml\n+     with:\n+       workflow_name: D4-LITE post-merge gate\n+       area: ci\n+       severity: P1\n+       branch: main\n+       run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\n+       failure_summary: Post-merge L2 gate failed on main — a change merged without full verification.\n+       status: failure\n+\n+\n+\n\n ❯ __tests__/templates/ci-parallel-test-jobs.test.ts:31:24\n     29|       }) as unknown as Record<string, unknown>\n     30|       const rendered = renderTemplate('github/workflows/01-pr-fast.yml…\n     31|       expect(rendered).toContain('integration-tests:')\n       |                        ^\n     32|       const integSection = rendered.split('integration-tests:')[1]\n     33|       expect(integSection).toMatch(/needs:.*unit-tests/)\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/7]⎯\n\n FAIL  __tests__/templates/ci-parallel-test-jobs.test.ts > 01-pr-fast.yml.ejs — parallel test category jobs (#219) > TypeScript L2 > has behavioral-tests job with needs: [unit-tests]\nAssertionError: expected '# Reference: docs/REFERENCE/workflow-…' to contain 'behavioral-tests:'\n\n- Expected\n+ Received\n\n- behavioral-tests:\n+ # Reference: docs/REFERENCE/workflow-pr-fast.md\n+ name: PR Fast (T1)\n+\n+ permissions:\n+   contents: read\n+\n+ on:\n+   push:\n+\n+     branches: [main, \"task/**\"]\n+\n+   pull_request:\n+     types: [opened, synchronize, reopened, labeled, ready_for_review, unlabeled]\n+\n+     branches: [main]\n+\n+     paths-ignore:\n+       - 'docs/**'\n+       - '*.md'\n+       - '.gitignore'\n+       - '.editorconfig'\n+\n+ # Cancel in-progress runs on the same ref; main always completes (#357).\n+ concurrency:\n+   group: pr-fast-${{ github.head_ref || github.ref }}\n+\n+   cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}\n+\n+\n+ jobs:\n+   config-lint:\n+     name: Config Lint (yaml + shell)\n+     # gold-align (#1506 yamllint / #1507 shellcheck): a language-agnostic style/\n+     # structure lint lane. yamllint checks non-workflow YAML (indentation, duplicate\n+     # keys, trailing spaces, document structure) — complementary to actionlint, which\n+     # owns .github/workflows *semantics*. shellcheck checks generated *.sh for unquoted\n+     # expansions / unsafe word-splitting at warning severity. Both BLOCKING; both\n+     # version-pinned for reproducibility. Each linter no-ops cleanly (green) when the\n+     # repo carries no matching files. Committed .yamllint.yml / .shellcheckrc (when\n+     # present) are auto-discovered and make the rules explicit + tunable.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - name: Install shellcheck (pinned)\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/koalaman/shellcheck/releases/download/v0.10.0/shellcheck-v0.10.0.linux.x86_64.tar.xz \\\n+             | tar -xJ -C \"$RUNNER_TEMP/bin\" --strip-components=1 shellcheck-v0.10.0/shellcheck\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: shellcheck (generated shell, blocking at warning severity)\n+         # .shellcheckrc (when present) is auto-discovered. No *.sh ⇒ no-op (green).\n+         run: |\n+           files=$(git ls-files '*.sh')\n+           if [ -n \"$files\" ]; then\n+             echo \"$files\" | xargs shellcheck --severity=warning\n+           else\n+             echo \"shellcheck: no *.sh files — skipping\"\n+           fi\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0\n+         with:\n+           python-version: '3.12'\n+       - name: Install yamllint (pinned)\n+         run: pip install yamllint==1.35.1\n+       - name: yamllint (non-workflow YAML, blocking on errors)\n+         # actionlint owns .github/workflows semantics; exclude them to avoid overlap.\n+         # A committed .yamllint.yml (when present) is auto-discovered and authoritative;\n+         # otherwise fall back to yamllint's built-in `relaxed` preset — both block on\n+         # ERRORS only (line-length etc. stay advisory warnings). No matching YAML ⇒ no-op.\n+         run: |\n+           files=$(git ls-files '*.yml' '*.yaml' ':!.github/workflows/**')\n+           if [ -n \"$files\" ]; then\n+             if [ -f .yamllint.yml ] || [ -f .yamllint.yaml ] || [ -f .yamllint ]; then\n+               echo \"$files\" | xargs yamllint\n+             else\n+               echo \"$files\" | xargs yamllint -d relaxed\n+             fi\n+           else\n+             echo \"yamllint: no non-workflow YAML — skipping\"\n+           fi\n+\n+   security-early-fail:\n+     name: Security (PII + Secrets)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+       - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0\n+         with:\n+           node-version-file: '.nvmrc'\n+       - name: PII scan (INV-12, HARD)\n+         run: node scripts/pii-scan.mjs\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Gitleaks secrets scan (INV-11)\n+         # --log-opts=\"HEAD\" (#1908): without it, gitleaks defaults to scanning\n+         # ALL refs reachable in the checkout's object DB (fetch-depth: 0 above\n+         # fetches every remote branch, not just this one). That let an unrelated,\n+         # unmerged branch's commit cross-contaminate every other PR's scan.\n+         # Scoping to HEAD still walks this ref's FULL history (no depth lost) —\n+         # it just stops other branches' commits from being scanned here. The\n+         # nightly `gitleaks-history` job intentionally keeps `--all --full-history`\n+         # as the deep, cross-branch safety net.\n+         run: |\n+           gitleaks detect --source . --config .gitleaks.toml \\\n+             --gitleaks-ignore-path suppressions/.gitleaksignore \\\n+             --log-opts=\"HEAD\" \\\n+             --exit-code 1 --redact\n+\n+\n+   classify-changes:\n+     name: Classify Changes\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     outputs:\n+       docs_only: ${{ steps.classify.outputs.docs_only }}\n+       backend_changed: ${{ steps.classify.outputs.backend_changed }}\n+       frontend_changed: ${{ steps.classify.outputs.frontend_changed }}\n+       infra_changed: ${{ steps.classify.outputs.infra_changed }}\n+       high_risk: ${{ steps.classify.outputs.high_risk }}\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+       - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0\n+         with:\n+           node-version-file: '.nvmrc'\n+       - id: classify\n+         if: hashFiles('scripts/ci-classify-changes.mjs') != ''\n+         run: node scripts/ci-classify-changes.mjs\n+         env:\n+           BASE_SHA: ${{ github.event.pull_request.base.sha || 'origin/main' }}\n+           HEAD_SHA: ${{ github.sha }}\n+\n+\n+   dependency-review:\n+     name: Dependency Review (supply-chain)\n+     # A1 (#1502): the canonical FAST PR supply-chain gate. dependency-review-action\n+     # diffs the PR's dependency-manifest changes and fails on a newly-introduced\n+     # vulnerable dependency — keeping slow full-tree scanners off the always-path.\n+     # PR-only (the action diffs base...head and errors on push). Fail severity is\n+     # configurable via the DEPENDENCY_REVIEW_FAIL_SEVERITY repo variable (default: high).\n+     # Floor: enableSecurityScanning — the same always-path gate as security-early-fail.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     # PR-only AND GHAS-gated (#1685): dependency-review-action HARD-errors when the\n+     # repo lacks Dependency Graph + GitHub Advanced Security, which reds every PR\n+     # while GHAS is off. A JOB-LEVEL guard makes the job SKIP (result=skipped, which\n+     # the ci-required aggregator treats as non-failure) until GHAS is enabled by\n+     # setting the GHAS_ENABLED repo variable to 'true'. NOT step-level\n+     # continue-on-error: that would violate INV-80 and silently neuter the gate once\n+     # GHAS is on. With the var set, a real finding still fails the job and blocks.\n+     if: github.event_name == 'pull_request' && vars.GHAS_ENABLED == 'true'\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - name: Dependency review (fail on newly-introduced high-severity deps)\n+         uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294  # v5.0.0\n+         with:\n+           fail-on-severity: ${{ vars.DEPENDENCY_REVIEW_FAIL_SEVERITY || 'high' }}\n+\n+   iac-scan:\n+     name: IaC Scan (infra-changed)\n+     # A1 (#1502): consume the classify-changes `infra_changed` signal (previously\n+     # classified but UNUSED) — scan terraform / kubernetes / dockerfile only when the\n+     # PR actually touches infrastructure-as-code, keeping the always-path lean.\n+     # #1785: was pinned to a LITERAL `runs-on: ubuntu-latest` on the (disproven)\n+     # assumption that GitHub-hosted runners are never containerized (#1756).\n+     # Evidence (run 28693108584) showed a job pinned to literal `ubuntu-latest`\n+     # actually serviced by self-hosted `arbiter-slot-build-3` — the label alone\n+     # never guaranteed real GitHub-hosted infra here. Rather than keep trusting\n+     # an unverified runner label, Checkov now runs as a plain pip-installed CLI\n+     # (bridgecrewio/checkov-action was only a Docker convenience wrapper around\n+     # it) — no docker-container action, no workspace bind-mount, the whole\n+     # defect class sidestepped entirely (same pattern as the osv-scanner static-\n+     # binary fix, #1767). Enforced by scripts/check-docker-action-runner-safety.mjs.\n+     # #1894: routed through the same `RUNNER_LABELS_TEST` expression as every other\n+     # job. The former literal `ubuntu-latest` used to be intercepted by the self-\n+     # hosted fleet (#1785 evidence), but the fleet dropped the `ubuntu-latest`\n+     # label, so the literal pin fell through to real GitHub-hosted infra and failed\n+     # (spending-limit block). The expression defaults to `[\"ubuntu-latest\"]` when\n+     # the var is unset (consumers unaffected); arbiter's own repo sets it to the\n+     # self-hosted labels. Checkov/tflint are plain CLIs (no docker-action), so the\n+     # runner class remains moot for the #1785 safety class either way.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [classify-changes]\n+     if: needs.classify-changes.outputs.infra_changed == 'true'\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0\n+         with:\n+           python-version: '3.12'\n+       - name: Install Checkov (pinned, no Docker — #1785)\n+         run: pip install checkov==3.3.7\n+       - name: Checkov IaC scan (all frameworks)\n+         run: |\n+           # #1685: `all` is checkov's documented-valid multi-framework value (also\n+           # its own default scope). Hard-fail stays the default (no soft-fail\n+           # flag passed): a real misconfiguration must block (INV-80).\n+           checkov --directory . --framework all --quiet --compact\n+       # #1509 gold-align: Checkov scans for known misconfigurations; tflint is the\n+       # Terraform-specific *linter* (provider-aware deprecations, invalid arg\n+       # combinations, unused declarations). Reference-grade IaC pipelines run it\n+       # BLOCKING — no soft-fail. `--recursive` exits 0 when the repo carries no\n+       # `.tf` files, so non-Terraform infra changes (k8s/dockerfile) stay green.\n+       - name: Setup tflint (Terraform linter)\n+         uses: terraform-linters/setup-tflint@6e1e0642c0289bd619021bf6b34e3c08ed1e005a  # v6.3.0\n+       - name: tflint (Terraform lint, blocking)\n+         run: tflint --recursive --no-color\n+\n+\n+\n+   gate:\n+     name: Lint + Typecheck\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [security-early-fail, classify-changes]\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: ./.github/actions/setup-node-pnpm\n+       - run: npm run typecheck\n+       - run: npm run lint\n+\n+       - name: Dep audit (INV-13)\n+         run: npm audit --omit=dev --audit-level=high\n+\n+\n+   unit-tests:\n+     name: Unit Tests\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate, classify-changes]\n+     strategy:\n+       max-parallel: 2\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: ./.github/actions/setup-node-pnpm\n+       - run: npm run test:unit -- --reporter=junit --outputFile=test-results/junit.xml\n+\n+       - name: Upload test results\n+         if: always()\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: test-results-unit-${{ github.run_id }}\n+           path: test-results/\n+           retention-days: 7\n+\n+\n+\n+\n+\n+   docs-check:\n+     name: Docs Check\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     if: github.event_name == 'pull_request'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+       - name: Check docs updated with code changes\n+         env:\n+           BASE_SHA: ${{ github.event.pull_request.base.sha }}\n+           HEAD_SHA: ${{ github.event.pull_request.head.sha }}\n+         run: |\n+           # #356: rebased-aware via merge-base; bypass via [skip-docs] commit token.\n+           MB=$(git merge-base \"$BASE_SHA\" \"$HEAD_SHA\" || echo \"$BASE_SHA\")\n+           CHANGED=$(git diff --name-only \"$MB\" \"$HEAD_SHA\")\n+           LOG=$(git log --format=%B \"$MB..$HEAD_SHA\" || true)\n+\n+           HAS_CODE=$(echo \"$CHANGED\" | grep -E '^(src/|__tests__/)' || true)\n+           HAS_DOCS=$(echo \"$CHANGED\" | grep -E '^(docs/|README\\.md)' || true)\n+           BYPASS=$(echo \"$LOG\" | grep -F '[skip-docs]' || true)\n+\n+           if [ -n \"$HAS_CODE\" ] && [ -z \"$HAS_DOCS\" ] && [ -z \"$BYPASS\" ]; then\n+             echo \"::error::Code changed without documentation update\"\n+             echo \"\"\n+             echo \"Files changed in src/ or __tests__/:\"\n+             echo \"$HAS_CODE\"\n+             echo \"\"\n+             echo \"Please update docs/ or README.md, or add [skip-docs] to a commit message to bypass.\"\n+             exit 1\n+           fi\n+           echo \"Docs check passed.\"\n+\n+\n+\n+   debt-gates:\n+     name: Tech Debt Gates\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate]\n+     # E1 (#1502): the PR leg keeps only the FAST static-analysis gates\n+     # (knip/madge/pmd/spotbugs/clippy/ruff/golangci). The whole-suite coverage\n+     # re-run that used to live here was a SECOND full test execution on the PR\n+     # (the unit/integration jobs already ran the suite, and `gate-full` runs\n+     # check-all L2 which enforces the coverage threshold). The threshold floor is\n+     # therefore preserved on PR via gate-full; the heavy coverage report (HTML/lcov\n+     # artifact) is produced at nightly cadence in 06-nightly `coverage-report`.\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+       - run: npx knip\n+       - run: npx madge --circular --extensions ts,tsx,js,jsx src\n+\n+\n+\n+\n+   sonar-scan:\n+     name: SonarCloud Scan\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate]\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+\n+\n+       - name: SonarCloud Scan (PR decoration — incremental new-code analysis)\n+         # E1 (#1502): on PRs Sonar runs in pull-request mode — it decorates the PR\n+         # with new-code findings only (incremental), NOT a full deep scan. The full\n+         # branch baseline runs on merge-to-main below and nightly (06-nightly\n+         # `sonar-deep-scan`), keeping the PR path fast.\n+         if: env.SONAR_TOKEN != '' && github.event_name == 'pull_request'\n+         env:\n+           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}\n+           # untrusted refs pass through env (never inlined into the script body).\n+           PR_KEY: ${{ github.event.pull_request.number }}\n+           PR_BRANCH: ${{ github.head_ref }}\n+           PR_BASE: ${{ github.base_ref }}\n+         run: |\n+           npx sonar-scanner -Dsonar.token=\"$SONAR_TOKEN\" \\\n+             -Dsonar.pullrequest.key=\"$PR_KEY\" \\\n+             -Dsonar.pullrequest.branch=\"$PR_BRANCH\" \\\n+             -Dsonar.pullrequest.base=\"$PR_BASE\"\n+       - name: SonarCloud Scan (full branch baseline)\n+         # Push to a long-lived branch refreshes the full-project baseline so the\n+         # PR decoration above has a fresh reference to diff new code against.\n+         if: env.SONAR_TOKEN != '' && github.event_name != 'pull_request'\n+         env:\n+           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}\n+         run: |\n+           npx sonar-scanner -Dsonar.token=\"$SONAR_TOKEN\"\n+\n+\n+\n+   debt-ratchet:\n+     name: Debt Ratchet\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate]\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+\n+       - name: Run debt ratchet\n+         run: node scripts/debt-report.mjs --gate\n+\n+\n+\n+   gate-full:\n+     name: Full Gate (L2, INV-59)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+\n+     needs: [security-early-fail, classify-changes]\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Install actionlint\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/rhysd/actionlint/releases/download/v1.7.3/actionlint_1.7.3_linux_amd64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" actionlint\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Build\n+         run: npm run build\n+       - name: Run full L2 gate (== pre-push)\n+         run: node scripts/check-all.mjs L2 --json gate-result.json\n+       - name: Upload gate-result artifact (activates INV-59 runtime parity)\n+         if: always()\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: gate-result\n+           path: gate-result.json\n+           retention-days: 7\n+\n+\n+   human-approval-required:\n+     name: Human Approval Required (INV-74)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     if: github.event_name == 'pull_request'\n+     steps:\n+       - name: Assert approved-by-human label (anti-bot gate)\n+         uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3  # v9.0.0\n+         with:\n+           github-token: ${{ secrets.GITHUB_TOKEN }}\n+           script: |\n+             const labels = context.payload.pull_request.labels.map(l => l.name);\n+             if (!labels.includes('approved-by-human')) {\n+               core.setFailed(\n+                 'PR requires the \"approved-by-human\" label applied by a human reviewer. ' +\n+                 'See 03-human-approval.yml — a bot approval does not satisfy this check.'\n+               );\n+             }\n+\n+   ci-required:\n+     name: CI Required\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+\n+     needs: [gate, human-approval-required, unit-tests, security-early-fail, dependency-review, iac-scan, docs-check, sonar-scan, gate-full, debt-gates, debt-ratchet, classify-changes]\n+     if: always()\n+     steps:\n+       - env:\n+           DOCS_ONLY: ${{ needs.classify-changes.outputs.docs_only }}\n+         run: |\n+\n+           if [[ \"${{ needs.classify-changes.result }}\" != \"success\" ]]; then\n+             echo \"classify-changes failed — cannot trust docs_only, failing closed\"\n+             exit 1\n+           fi\n+           # success, or skipped ONLY when the docs-only classification skipped the job\n+           ok() { [[ \"$1\" == \"success\" || ( \"$DOCS_ONLY\" == \"true\" && \"$1\" == \"skipped\" ) ]]; }\n+           if ! ok \"${{ needs.gate.result }}\"; then\n+             echo \"gate failed\"\n+             exit 1\n+           fi\n+\n+           if ! ok \"${{ needs.unit-tests.result }}\"; then\n+             echo \"unit-tests failed\"\n+             exit 1\n+           fi\n+\n+           if [[ \"${{ needs.security-early-fail.result }}\" != \"success\" ]]; then\n+             echo \"security-early-fail failed\"\n+             exit 1\n+           fi\n+           # dependency-review skips on push (PR-only) — only a real finding blocks.\n+           if [[ \"${{ needs.dependency-review.result }}\" == \"failure\" ]]; then\n+             echo \"dependency-review failed — a newly-introduced dependency exceeds the severity floor\"\n+             exit 1\n+           fi\n+           # iac-scan skips when no infra changed — only a real IaC finding blocks.\n+           if [[ \"${{ needs.iac-scan.result }}\" == \"failure\" ]]; then\n+             echo \"iac-scan failed — an infrastructure-as-code misconfiguration was found\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.docs-check.result }}\" == \"failure\" ]]; then\n+             echo \"docs-check failed\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.sonar-scan.result }}\" == \"failure\" ]]; then\n+             echo \"sonar-scan failed\"\n+             exit 1\n+           fi\n+           if ! ok \"${{ needs.debt-gates.result }}\"; then\n+             echo \"debt-gates failed\"\n+             exit 1\n+           fi\n+           if ! ok \"${{ needs.debt-ratchet.result }}\"; then\n+             echo \"debt-ratchet failed\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.gate-full.result }}\" != \"success\" && \"${{ needs.gate-full.result }}\" != \"skipped\" ]]; then\n+             echo \"gate-full (L2) failed or was cancelled\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.human-approval-required.result }}\" == \"failure\" ]]; then\n+             echo \"human-approval-required failed — PR needs approved-by-human label\"\n+             exit 1\n+           fi\n+\n+\n+   post-merge-gate:\n+     name: D4-LITE post-merge gate (main)\n+     needs: [classify-changes]\n+     if: >\n+       github.event_name == 'push' &&\n+       github.ref == 'refs/heads/main' &&\n+       needs.classify-changes.result == 'success' &&\n+       needs.classify-changes.outputs.docs_only != 'true'\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Install actionlint\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/rhysd/actionlint/releases/download/v1.7.3/actionlint_1.7.3_linux_amd64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" actionlint\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Build\n+         run: npm run build\n+       - name: Run full L2 gate (D4-LITE catch-net)\n+         run: node scripts/check-all.mjs L2\n+\n+   post-merge-notify:\n+     name: D4-LITE notify on failure\n+     needs: [post-merge-gate]\n+     if: always() && needs.post-merge-gate.result == 'failure'\n+     permissions:\n+       issues: write\n+     uses: ./.github/workflows/_notify.yml\n+     with:\n+       workflow_name: D4-LITE post-merge gate\n+       area: ci\n+       severity: P1\n+       branch: main\n+       run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\n+       failure_summary: Post-merge L2 gate failed on main — a change merged without full verification.\n+       status: failure\n+\n+\n+\n\n ❯ __tests__/templates/ci-parallel-test-jobs.test.ts:42:24\n     40|       }) as unknown as Record<string, unknown>\n     41|       const rendered = renderTemplate('github/workflows/01-pr-fast.yml…\n     42|       expect(rendered).toContain('behavioral-tests:')\n       |                        ^\n     43|       const behavSection = rendered.split('behavioral-tests:')[1]\n     44|       expect(behavSection).toMatch(/needs:.*unit-tests/)\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/7]⎯\n\n FAIL  __tests__/templates/ci-parallel-test-jobs.test.ts > 01-pr-fast.yml.ejs — parallel test category jobs (#219) > TypeScript L2 > ci-required needs includes unit-tests, contract-tests, integration-tests, behavioral-tests\nAssertionError: expected '\\n    name: CI Required\\n    runs-on:…' to contain 'contract-tests'\n\n- Expected\n+ Received\n\n- contract-tests\n+\n+     name: CI Required\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+\n+     needs: [gate, human-approval-required, unit-tests, security-early-fail, dependency-review, iac-scan, docs-check, sonar-scan, gate-full, debt-gates, debt-ratchet, classify-changes]\n+     if: always()\n+     steps:\n+       - env:\n+           DOCS_ONLY: ${{ needs.classify-changes.outputs.docs_only }}\n+         run: |\n+\n+           if [[ \"${{ needs.classify-changes.result }}\" != \"success\" ]]; then\n+             echo \"classify-changes failed — cannot trust docs_only, failing closed\"\n+             exit 1\n+           fi\n+           # success, or skipped ONLY when the docs-only classification skipped the job\n+           ok() { [[ \"$1\" == \"success\" || ( \"$DOCS_ONLY\" == \"true\" && \"$1\" == \"skipped\" ) ]]; }\n+           if ! ok \"${{ needs.gate.result }}\"; then\n+             echo \"gate failed\"\n+             exit 1\n+           fi\n+\n+           if ! ok \"${{ needs.unit-tests.result }}\"; then\n+             echo \"unit-tests failed\"\n+             exit 1\n+           fi\n+\n+           if [[ \"${{ needs.security-early-fail.result }}\" != \"success\" ]]; then\n+             echo \"security-early-fail failed\"\n+             exit 1\n+           fi\n+           # dependency-review skips on push (PR-only) — only a real finding blocks.\n+           if [[ \"${{ needs.dependency-review.result }}\" == \"failure\" ]]; then\n+             echo \"dependency-review failed — a newly-introduced dependency exceeds the severity floor\"\n+             exit 1\n+           fi\n+           # iac-scan skips when no infra changed — only a real IaC finding blocks.\n+           if [[ \"${{ needs.iac-scan.result }}\" == \"failure\" ]]; then\n+             echo \"iac-scan failed — an infrastructure-as-code misconfiguration was found\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.docs-check.result }}\" == \"failure\" ]]; then\n+             echo \"docs-check failed\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.sonar-scan.result }}\" == \"failure\" ]]; then\n+             echo \"sonar-scan failed\"\n+             exit 1\n+           fi\n+           if ! ok \"${{ needs.debt-gates.result }}\"; then\n+             echo \"debt-gates failed\"\n+             exit 1\n+           fi\n+           if ! ok \"${{ needs.debt-ratchet.result }}\"; then\n+             echo \"debt-ratchet failed\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.gate-full.result }}\" != \"success\" && \"${{ needs.gate-full.result }}\" != \"skipped\" ]]; then\n+             echo \"gate-full (L2) failed or was cancelled\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.human-approval-required.result }}\" == \"failure\" ]]; then\n+             echo \"human-approval-required failed — PR needs approved-by-human label\"\n+             exit 1\n+           fi\n+\n+\n+   post-merge-gate:\n+     name: D4-LITE post-merge gate (main)\n+     needs: [classify-changes]\n+     if: >\n+       github.event_name == 'push' &&\n+       github.ref == 'refs/heads/main' &&\n+       needs.classify-changes.result == 'success' &&\n+       needs.classify-changes.outputs.docs_only != 'true'\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Install actionlint\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/rhysd/actionlint/releases/download/v1.7.3/actionlint_1.7.3_linux_amd64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" actionlint\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Build\n+         run: npm run build\n+       - name: Run full L2 gate (D4-LITE catch-net)\n+         run: node scripts/check-all.mjs L2\n+\n+   post-merge-notify:\n+     name: D4-LITE notify on failure\n+     needs: [post-merge-gate]\n+     if: always() && needs.post-merge-gate.result == 'failure'\n+     permissions:\n+       issues: write\n+     uses: ./.github/workflows/_notify.yml\n+     with:\n+       workflow_name: D4-LITE post-merge gate\n+       area: ci\n+       severity: P1\n+       branch: main\n+       run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\n+       failure_summary: Post-merge L2 gate failed on main — a change merged without full verification.\n+       status: failure\n+\n+\n+\n\n ❯ __tests__/templates/ci-parallel-test-jobs.test.ts:55:26\n     53|       const ciRequired = rendered.split('ci-required:')[1]\n     54|       expect(ciRequired).toContain('unit-tests')\n     55|       expect(ciRequired).toContain('contract-tests')\n       |                          ^\n     56|       expect(ciRequired).toContain('integration-tests')\n     57|       expect(ciRequired).toContain('behavioral-tests')\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/7]⎯\n\n FAIL  __tests__/templates/ci-parallel-test-jobs.test.ts > 01-pr-fast.yml.ejs — parallel test category jobs (#219) > Java L2 (Maven) > has contract-tests job\nAssertionError: expected '# Reference: docs/REFERENCE/workflow-…' to contain 'contract-tests:'\n\n- Expected\n+ Received\n\n- contract-tests:\n+ # Reference: docs/REFERENCE/workflow-pr-fast.md\n+ name: PR Fast (T1)\n+\n+ permissions:\n+   contents: read\n+\n+ on:\n+   push:\n+\n+     branches: [main, \"task/**\"]\n+\n+   pull_request:\n+     types: [opened, synchronize, reopened, labeled, ready_for_review, unlabeled]\n+\n+     branches: [main]\n+\n+     paths-ignore:\n+       - 'docs/**'\n+       - '*.md'\n+       - '.gitignore'\n+       - '.editorconfig'\n+\n+ # Cancel in-progress runs on the same ref; main always completes (#357).\n+ concurrency:\n+   group: pr-fast-${{ github.head_ref || github.ref }}\n+\n+   cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}\n+\n+\n+ jobs:\n+   config-lint:\n+     name: Config Lint (yaml + shell)\n+     # gold-align (#1506 yamllint / #1507 shellcheck): a language-agnostic style/\n+     # structure lint lane. yamllint checks non-workflow YAML (indentation, duplicate\n+     # keys, trailing spaces, document structure) — complementary to actionlint, which\n+     # owns .github/workflows *semantics*. shellcheck checks generated *.sh for unquoted\n+     # expansions / unsafe word-splitting at warning severity. Both BLOCKING; both\n+     # version-pinned for reproducibility. Each linter no-ops cleanly (green) when the\n+     # repo carries no matching files. Committed .yamllint.yml / .shellcheckrc (when\n+     # present) are auto-discovered and make the rules explicit + tunable.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - name: Install shellcheck (pinned)\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/koalaman/shellcheck/releases/download/v0.10.0/shellcheck-v0.10.0.linux.x86_64.tar.xz \\\n+             | tar -xJ -C \"$RUNNER_TEMP/bin\" --strip-components=1 shellcheck-v0.10.0/shellcheck\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: shellcheck (generated shell, blocking at warning severity)\n+         # .shellcheckrc (when present) is auto-discovered. No *.sh ⇒ no-op (green).\n+         run: |\n+           files=$(git ls-files '*.sh')\n+           if [ -n \"$files\" ]; then\n+             echo \"$files\" | xargs shellcheck --severity=warning\n+           else\n+             echo \"shellcheck: no *.sh files — skipping\"\n+           fi\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0\n+         with:\n+           python-version: '3.12'\n+       - name: Install yamllint (pinned)\n+         run: pip install yamllint==1.35.1\n+       - name: yamllint (non-workflow YAML, blocking on errors)\n+         # actionlint owns .github/workflows semantics; exclude them to avoid overlap.\n+         # A committed .yamllint.yml (when present) is auto-discovered and authoritative;\n+         # otherwise fall back to yamllint's built-in `relaxed` preset — both block on\n+         # ERRORS only (line-length etc. stay advisory warnings). No matching YAML ⇒ no-op.\n+         run: |\n+           files=$(git ls-files '*.yml' '*.yaml' ':!.github/workflows/**')\n+           if [ -n \"$files\" ]; then\n+             if [ -f .yamllint.yml ] || [ -f .yamllint.yaml ] || [ -f .yamllint ]; then\n+               echo \"$files\" | xargs yamllint\n+             else\n+               echo \"$files\" | xargs yamllint -d relaxed\n+             fi\n+           else\n+             echo \"yamllint: no non-workflow YAML — skipping\"\n+           fi\n+\n+   security-early-fail:\n+     name: Security (PII + Secrets)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+       - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0\n+         with:\n+           node-version-file: '.nvmrc'\n+       - name: PII scan (INV-12, HARD)\n+         run: node scripts/pii-scan.mjs\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Gitleaks secrets scan (INV-11)\n+         # --log-opts=\"HEAD\" (#1908): without it, gitleaks defaults to scanning\n+         # ALL refs reachable in the checkout's object DB (fetch-depth: 0 above\n+         # fetches every remote branch, not just this one). That let an unrelated,\n+         # unmerged branch's commit cross-contaminate every other PR's scan.\n+         # Scoping to HEAD still walks this ref's FULL history (no depth lost) —\n+         # it just stops other branches' commits from being scanned here. The\n+         # nightly `gitleaks-history` job intentionally keeps `--all --full-history`\n+         # as the deep, cross-branch safety net.\n+         run: |\n+           gitleaks detect --source . --config .gitleaks.toml \\\n+             --gitleaks-ignore-path suppressions/.gitleaksignore \\\n+             --log-opts=\"HEAD\" \\\n+             --exit-code 1 --redact\n+\n+\n+   classify-changes:\n+     name: Classify Changes\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     outputs:\n+       docs_only: ${{ steps.classify.outputs.docs_only }}\n+       backend_changed: ${{ steps.classify.outputs.backend_changed }}\n+       frontend_changed: ${{ steps.classify.outputs.frontend_changed }}\n+       infra_changed: ${{ steps.classify.outputs.infra_changed }}\n+       high_risk: ${{ steps.classify.outputs.high_risk }}\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+       - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0\n+         with:\n+           node-version-file: '.nvmrc'\n+       - id: classify\n+         if: hashFiles('scripts/ci-classify-changes.mjs') != ''\n+         run: node scripts/ci-classify-changes.mjs\n+         env:\n+           BASE_SHA: ${{ github.event.pull_request.base.sha || 'origin/main' }}\n+           HEAD_SHA: ${{ github.sha }}\n+\n+\n+   dependency-review:\n+     name: Dependency Review (supply-chain)\n+     # A1 (#1502): the canonical FAST PR supply-chain gate. dependency-review-action\n+     # diffs the PR's dependency-manifest changes and fails on a newly-introduced\n+     # vulnerable dependency — keeping slow full-tree scanners off the always-path.\n+     # PR-only (the action diffs base...head and errors on push). Fail severity is\n+     # configurable via the DEPENDENCY_REVIEW_FAIL_SEVERITY repo variable (default: high).\n+     # Floor: enableSecurityScanning — the same always-path gate as security-early-fail.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     # PR-only AND GHAS-gated (#1685): dependency-review-action HARD-errors when the\n+     # repo lacks Dependency Graph + GitHub Advanced Security, which reds every PR\n+     # while GHAS is off. A JOB-LEVEL guard makes the job SKIP (result=skipped, which\n+     # the ci-required aggregator treats as non-failure) until GHAS is enabled by\n+     # setting the GHAS_ENABLED repo variable to 'true'. NOT step-level\n+     # continue-on-error: that would violate INV-80 and silently neuter the gate once\n+     # GHAS is on. With the var set, a real finding still fails the job and blocks.\n+     if: github.event_name == 'pull_request' && vars.GHAS_ENABLED == 'true'\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - name: Dependency review (fail on newly-introduced high-severity deps)\n+         uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294  # v5.0.0\n+         with:\n+           fail-on-severity: ${{ vars.DEPENDENCY_REVIEW_FAIL_SEVERITY || 'high' }}\n+\n+   iac-scan:\n+     name: IaC Scan (infra-changed)\n+     # A1 (#1502): consume the classify-changes `infra_changed` signal (previously\n+     # classified but UNUSED) — scan terraform / kubernetes / dockerfile only when the\n+     # PR actually touches infrastructure-as-code, keeping the always-path lean.\n+     # #1785: was pinned to a LITERAL `runs-on: ubuntu-latest` on the (disproven)\n+     # assumption that GitHub-hosted runners are never containerized (#1756).\n+     # Evidence (run 28693108584) showed a job pinned to literal `ubuntu-latest`\n+     # actually serviced by self-hosted `arbiter-slot-build-3` — the label alone\n+     # never guaranteed real GitHub-hosted infra here. Rather than keep trusting\n+     # an unverified runner label, Checkov now runs as a plain pip-installed CLI\n+     # (bridgecrewio/checkov-action was only a Docker convenience wrapper around\n+     # it) — no docker-container action, no workspace bind-mount, the whole\n+     # defect class sidestepped entirely (same pattern as the osv-scanner static-\n+     # binary fix, #1767). Enforced by scripts/check-docker-action-runner-safety.mjs.\n+     # #1894: routed through the same `RUNNER_LABELS_TEST` expression as every other\n+     # job. The former literal `ubuntu-latest` used to be intercepted by the self-\n+     # hosted fleet (#1785 evidence), but the fleet dropped the `ubuntu-latest`\n+     # label, so the literal pin fell through to real GitHub-hosted infra and failed\n+     # (spending-limit block). The expression defaults to `[\"ubuntu-latest\"]` when\n+     # the var is unset (consumers unaffected); arbiter's own repo sets it to the\n+     # self-hosted labels. Checkov/tflint are plain CLIs (no docker-action), so the\n+     # runner class remains moot for the #1785 safety class either way.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [classify-changes]\n+     if: needs.classify-changes.outputs.infra_changed == 'true'\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0\n+         with:\n+           python-version: '3.12'\n+       - name: Install Checkov (pinned, no Docker — #1785)\n+         run: pip install checkov==3.3.7\n+       - name: Checkov IaC scan (all frameworks)\n+         run: |\n+           # #1685: `all` is checkov's documented-valid multi-framework value (also\n+           # its own default scope). Hard-fail stays the default (no soft-fail\n+           # flag passed): a real misconfiguration must block (INV-80).\n+           checkov --directory . --framework all --quiet --compact\n+       # #1509 gold-align: Checkov scans for known misconfigurations; tflint is the\n+       # Terraform-specific *linter* (provider-aware deprecations, invalid arg\n+       # combinations, unused declarations). Reference-grade IaC pipelines run it\n+       # BLOCKING — no soft-fail. `--recursive` exits 0 when the repo carries no\n+       # `.tf` files, so non-Terraform infra changes (k8s/dockerfile) stay green.\n+       - name: Setup tflint (Terraform linter)\n+         uses: terraform-linters/setup-tflint@6e1e0642c0289bd619021bf6b34e3c08ed1e005a  # v6.3.0\n+       - name: tflint (Terraform lint, blocking)\n+         run: tflint --recursive --no-color\n+\n+\n+\n+\n+   build-reactor:\n+     name: Build Maven Reactor (#1226)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     permissions:\n+       contents: read\n+     needs: [classify-changes]\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: ./.github/actions/setup-java-maven\n+       - name: Pre-fetch plugins and dependencies (offline cache)\n+         run: mvn dependency:go-offline --batch-mode -q\n+       - name: Build reactor (compile main + test classes, skip test execution)\n+         # -DskipTests runs the test-compile phase but skips execution, so\n+         # target/classes and target/test-classes are produced once here and\n+         # reused by every test job below — no per-job recompile (#1278).\n+         run: mvn --batch-mode -DskipTests install\n+       - name: Pack M2 repository + compiled target/ trees\n+         # m2-reactor.tar stays byte-identical to its prior form (rooted at $HOME/.m2).\n+         # target-reactor.tar is additive (#1278): every module's compiled target/\n+         # (main + test classes) in its own tar rooted at the workspace, so downstream\n+         # run-only test goals (surefire:test) never recompile.\n+         run: |\n+           mkdir -p \"${{ runner.temp }}/.m2-reactor\"\n+           tar -cf \"${{ runner.temp }}/.m2-reactor/m2-reactor.tar\" -C \"$HOME/.m2\" repository\n+           find . -type d -name target -prune -print0 \\\n+             | tar -cf \"${{ runner.temp }}/.m2-reactor/target-reactor.tar\" --null -T -\n+       - name: Upload reactor M2 + target artifact\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: reactor-m2-${{ github.run_id }}\n+           path: ${{ runner.temp }}/.m2-reactor/*.tar\n+           retention-days: 7\n+           overwrite: true\n+\n+   gate:\n+     name: Lint (Java)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     needs: [security-early-fail, classify-changes, build-reactor]\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: ./.github/actions/setup-java-maven\n+       - name: Download reactor M2 artifact\n+         uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: reactor-m2-${{ github.run_id }}\n+           path: ${{ runner.temp }}/.m2-reactor\n+       - run: mvn checkstyle:check --offline\n+\n+\n+   unit-tests:\n+     name: Unit Tests (Java)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [build-reactor, classify-changes]\n+     strategy:\n+       max-parallel: 2\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: ./.github/actions/setup-java-maven\n+       - name: Download reactor M2 artifact\n+         uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: reactor-m2-${{ github.run_id }}\n+           path: ${{ runner.temp }}/.m2-reactor\n+       - run: mvn surefire:test -Punit --offline\n+\n+       - name: Upload test results\n+         if: always()\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: test-results-unit-${{ github.run_id }}\n+           path: target/surefire-reports/\n+           retention-days: 7\n+\n+\n+\n+   archunit-tests:\n+     name: ArchUnit Architecture Tests (Java)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 30\n+     # #1512 gold-align: the lightweight *ArchTest JUnit5 architecture rules\n+     # (dependency-direction / layering / package-boundary) run BLOCKING on EVERY PR.\n+     # Previously they ran only on the weekly 13-archunit-extended cron, leaving up to a\n+     # 7-day fake-green window for a merged architecture regression. The heavier\n+     # full module-graph analysis (the Modulith suite) stays on that weekly cron.\n+     needs: [build-reactor, classify-changes]\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: ./.github/actions/setup-java-maven\n+       - name: Download reactor M2 artifact\n+         uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: reactor-m2-${{ github.run_id }}\n+           path: ${{ runner.temp }}/.m2-reactor\n+       - run: mvn surefire:test -Dtest=\"*ArchTest\" --offline\n+\n+\n+\n+\n+\n+\n+   docs-check:\n+     name: Docs Check\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     if: github.event_name == 'pull_request'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+       - name: Check docs updated with code changes\n+         env:\n+           BASE_SHA: ${{ github.event.pull_request.base.sha }}\n+           HEAD_SHA: ${{ github.event.pull_request.head.sha }}\n+         run: |\n+           # #356: rebased-aware via merge-base; bypass via [skip-docs] commit token.\n+           MB=$(git merge-base \"$BASE_SHA\" \"$HEAD_SHA\" || echo \"$BASE_SHA\")\n+           CHANGED=$(git diff --name-only \"$MB\" \"$HEAD_SHA\")\n+           LOG=$(git log --format=%B \"$MB..$HEAD_SHA\" || true)\n+\n+           HAS_CODE=$(echo \"$CHANGED\" | grep -E '^(src/|__tests__/)' || true)\n+           HAS_DOCS=$(echo \"$CHANGED\" | grep -E '^(docs/|README\\.md)' || true)\n+           BYPASS=$(echo \"$LOG\" | grep -F '[skip-docs]' || true)\n+\n+           if [ -n \"$HAS_CODE\" ] && [ -z \"$HAS_DOCS\" ] && [ -z \"$BYPASS\" ]; then\n+             echo \"::error::Code changed without documentation update\"\n+             echo \"\"\n+             echo \"Files changed in src/ or __tests__/:\"\n+             echo \"$HAS_CODE\"\n+             echo \"\"\n+             echo \"Please update docs/ or README.md, or add [skip-docs] to a commit message to bypass.\"\n+             exit 1\n+           fi\n+           echo \"Docs check passed.\"\n+\n+\n+\n+   debt-gates:\n+     name: Tech Debt Gates\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate]\n+     # E1 (#1502): the PR leg keeps only the FAST static-analysis gates\n+     # (knip/madge/pmd/spotbugs/clippy/ruff/golangci). The whole-suite coverage\n+     # re-run that used to live here was a SECOND full test execution on the PR\n+     # (the unit/integration jobs already ran the suite, and `gate-full` runs\n+     # check-all L2 which enforces the coverage threshold). The threshold floor is\n+     # therefore preserved on PR via gate-full; the heavy coverage report (HTML/lcov\n+     # artifact) is produced at nightly cadence in 06-nightly `coverage-report`.\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+\n+       - uses: ./.github/actions/setup-java-maven\n+       - name: Download reactor M2 artifact\n+         uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: reactor-m2-${{ github.run_id }}\n+           path: ${{ runner.temp }}/.m2-reactor\n+       - run: mvn pmd:check --offline\n+       - run: mvn spotbugs:check --offline\n+\n+\n+\n+\n+   sonar-scan:\n+     name: SonarCloud Scan\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate]\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: ./.github/actions/setup-java-maven\n+\n+\n+       - name: SonarCloud Scan (PR decoration — incremental new-code analysis)\n+         # E1 (#1502): on PRs Sonar runs in pull-request mode — it decorates the PR\n+         # with new-code findings only (incremental), NOT a full deep scan. The full\n+         # branch baseline runs on merge-to-main below and nightly (06-nightly\n+         # `sonar-deep-scan`), keeping the PR path fast.\n+         if: env.SONAR_TOKEN != '' && github.event_name == 'pull_request'\n+         env:\n+           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}\n+           # untrusted refs pass through env (never inlined into the script body).\n+           PR_KEY: ${{ github.event.pull_request.number }}\n+           PR_BRANCH: ${{ github.head_ref }}\n+           PR_BASE: ${{ github.base_ref }}\n+         run: |\n+           mvn sonar:sonar -Dsonar.token=\"$SONAR_TOKEN\" \\\n+             -Dsonar.pullrequest.key=\"$PR_KEY\" \\\n+             -Dsonar.pullrequest.branch=\"$PR_BRANCH\" \\\n+             -Dsonar.pullrequest.base=\"$PR_BASE\"\n+       - name: SonarCloud Scan (full branch baseline)\n+         # Push to a long-lived branch refreshes the full-project baseline so the\n+         # PR decoration above has a fresh reference to diff new code against.\n+         if: env.SONAR_TOKEN != '' && github.event_name != 'pull_request'\n+         env:\n+           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}\n+         run: |\n+           mvn sonar:sonar -Dsonar.token=\"$SONAR_TOKEN\"\n+\n+\n+\n+   debt-ratchet:\n+     name: Debt Ratchet\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate]\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+\n+       - uses: ./.github/actions/setup-java-maven\n+\n+       - name: Run debt ratchet\n+         run: node scripts/debt-report.mjs --gate\n+\n+\n+\n+   gate-full:\n+     name: Full Gate (L2, INV-59)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+\n+     needs: [security-early-fail, classify-changes]\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0\n+         with:\n+           node-version-file: '.nvmrc'\n+           cache: 'npm'\n+       - run: npm ci\n+\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Install actionlint\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/rhysd/actionlint/releases/download/v1.7.3/actionlint_1.7.3_linux_amd64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" actionlint\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Build\n+         run: npm run build\n+       - name: Run full L2 gate (== pre-push)\n+         run: node scripts/check-all.mjs L2 --json gate-result.json\n+       - name: Upload gate-result artifact (activates INV-59 runtime parity)\n+         if: always()\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: gate-result\n+           path: gate-result.json\n+           retention-days: 7\n+\n+\n+   human-approval-required:\n+     name: Human Approval Required (INV-74)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     if: github.event_name == 'pull_request'\n+     steps:\n+       - name: Assert approved-by-human label (anti-bot gate)\n+         uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3  # v9.0.0\n+         with:\n+           github-token: ${{ secrets.GITHUB_TOKEN }}\n+           script: |\n+             const labels = context.payload.pull_request.labels.map(l => l.name);\n+             if (!labels.includes('approved-by-human')) {\n+               core.setFailed(\n+                 'PR requires the \"approved-by-human\" label applied by a human reviewer. ' +\n+                 'See 03-human-approval.yml — a bot approval does not satisfy this check.'\n+               );\n+             }\n+\n+   ci-required:\n+     name: CI Required\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+\n+     needs: [gate, human-approval-required, build-reactor, unit-tests, archunit-tests, security-early-fail, dependency-review, iac-scan, docs-check, sonar-scan, gate-full, debt-gates, debt-ratchet, classify-changes]\n+     if: always()\n+     steps:\n+       - env:\n+           DOCS_ONLY: ${{ needs.classify-changes.outputs.docs_only }}\n+         run: |\n+\n+           if [[ \"${{ needs.classify-changes.result }}\" != \"success\" ]]; then\n+             echo \"classify-changes failed — cannot trust docs_only, failing closed\"\n+             exit 1\n+           fi\n+           # success, or skipped ONLY when the docs-only classification skipped the job\n+           ok() { [[ \"$1\" == \"success\" || ( \"$DOCS_ONLY\" == \"true\" && \"$1\" == \"skipped\" ) ]]; }\n+           if ! ok \"${{ needs.gate.result }}\"; then\n+             echo \"gate failed\"\n+             exit 1\n+           fi\n+\n+           if ! ok \"${{ needs.build-reactor.result }}\"; then\n+             echo \"build-reactor failed\"\n+             exit 1\n+           fi\n+\n+           if ! ok \"${{ needs.unit-tests.result }}\"; then\n+             echo \"unit-tests failed\"\n+             exit 1\n+           fi\n+\n+           if ! ok \"${{ needs.archunit-tests.result }}\"; then\n+             echo \"archunit-tests failed\"\n+             exit 1\n+           fi\n+\n+           if [[ \"${{ needs.security-early-fail.result }}\" != \"success\" ]]; then\n+             echo \"security-early-fail failed\"\n+             exit 1\n+           fi\n+           # dependency-review skips on push (PR-only) — only a real finding blocks.\n+           if [[ \"${{ needs.dependency-review.result }}\" == \"failure\" ]]; then\n+             echo \"dependency-review failed — a newly-introduced dependency exceeds the severity floor\"\n+             exit 1\n+           fi\n+           # iac-scan skips when no infra changed — only a real IaC finding blocks.\n+           if [[ \"${{ needs.iac-scan.result }}\" == \"failure\" ]]; then\n+             echo \"iac-scan failed — an infrastructure-as-code misconfiguration was found\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.docs-check.result }}\" == \"failure\" ]]; then\n+             echo \"docs-check failed\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.sonar-scan.result }}\" == \"failure\" ]]; then\n+             echo \"sonar-scan failed\"\n+             exit 1\n+           fi\n+           if ! ok \"${{ needs.debt-gates.result }}\"; then\n+             echo \"debt-gates failed\"\n+             exit 1\n+           fi\n+           if ! ok \"${{ needs.debt-ratchet.result }}\"; then\n+             echo \"debt-ratchet failed\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.gate-full.result }}\" != \"success\" && \"${{ needs.gate-full.result }}\" != \"skipped\" ]]; then\n+             echo \"gate-full (L2) failed or was cancelled\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.human-approval-required.result }}\" == \"failure\" ]]; then\n+             echo \"human-approval-required failed — PR needs approved-by-human label\"\n+             exit 1\n+           fi\n+\n+\n+   post-merge-gate:\n+     name: D4-LITE post-merge gate (main)\n+     needs: [classify-changes]\n+     if: >\n+       github.event_name == 'push' &&\n+       github.ref == 'refs/heads/main' &&\n+       needs.classify-changes.result == 'success' &&\n+       needs.classify-changes.outputs.docs_only != 'true'\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0\n+         with:\n+           node-version-file: '.nvmrc'\n+           cache: 'npm'\n+       - run: npm ci\n+\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Install actionlint\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/rhysd/actionlint/releases/download/v1.7.3/actionlint_1.7.3_linux_amd64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" actionlint\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Build\n+         run: npm run build\n+       - name: Run full L2 gate (D4-LITE catch-net)\n+         run: node scripts/check-all.mjs L2\n+\n+   post-merge-notify:\n+     name: D4-LITE notify on failure\n+     needs: [post-merge-gate]\n+     if: always() && needs.post-merge-gate.result == 'failure'\n+     permissions:\n+       issues: write\n+     uses: ./.github/workflows/_notify.yml\n+     with:\n+       workflow_name: D4-LITE post-merge gate\n+       area: ci\n+       severity: P1\n+       branch: main\n+       run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\n+       failure_summary: Post-merge L2 gate failed on main — a change merged without full verification.\n+       status: failure\n+\n+\n+\n\n ❯ __tests__/templates/ci-parallel-test-jobs.test.ts:115:24\n    113|       }) as unknown as Record<string, unknown>\n    114|       const rendered = renderTemplate('github/workflows/01-pr-fast.yml…\n    115|       expect(rendered).toContain('contract-tests:')\n       |                        ^\n    116|     })\n    117|\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/7]⎯\n\n FAIL  __tests__/templates/ci-parallel-test-jobs.test.ts > 01-pr-fast.yml.ejs — parallel test category jobs (#219) > Java L2 (Maven) > has integration-tests job\nAssertionError: expected '# Reference: docs/REFERENCE/workflow-…' to contain 'integration-tests:'\n\n- Expected\n+ Received\n\n- integration-tests:\n+ # Reference: docs/REFERENCE/workflow-pr-fast.md\n+ name: PR Fast (T1)\n+\n+ permissions:\n+   contents: read\n+\n+ on:\n+   push:\n+\n+     branches: [main, \"task/**\"]\n+\n+   pull_request:\n+     types: [opened, synchronize, reopened, labeled, ready_for_review, unlabeled]\n+\n+     branches: [main]\n+\n+     paths-ignore:\n+       - 'docs/**'\n+       - '*.md'\n+       - '.gitignore'\n+       - '.editorconfig'\n+\n+ # Cancel in-progress runs on the same ref; main always completes (#357).\n+ concurrency:\n+   group: pr-fast-${{ github.head_ref || github.ref }}\n+\n+   cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}\n+\n+\n+ jobs:\n+   config-lint:\n+     name: Config Lint (yaml + shell)\n+     # gold-align (#1506 yamllint / #1507 shellcheck): a language-agnostic style/\n+     # structure lint lane. yamllint checks non-workflow YAML (indentation, duplicate\n+     # keys, trailing spaces, document structure) — complementary to actionlint, which\n+     # owns .github/workflows *semantics*. shellcheck checks generated *.sh for unquoted\n+     # expansions / unsafe word-splitting at warning severity. Both BLOCKING; both\n+     # version-pinned for reproducibility. Each linter no-ops cleanly (green) when the\n+     # repo carries no matching files. Committed .yamllint.yml / .shellcheckrc (when\n+     # present) are auto-discovered and make the rules explicit + tunable.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - name: Install shellcheck (pinned)\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/koalaman/shellcheck/releases/download/v0.10.0/shellcheck-v0.10.0.linux.x86_64.tar.xz \\\n+             | tar -xJ -C \"$RUNNER_TEMP/bin\" --strip-components=1 shellcheck-v0.10.0/shellcheck\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: shellcheck (generated shell, blocking at warning severity)\n+         # .shellcheckrc (when present) is auto-discovered. No *.sh ⇒ no-op (green).\n+         run: |\n+           files=$(git ls-files '*.sh')\n+           if [ -n \"$files\" ]; then\n+             echo \"$files\" | xargs shellcheck --severity=warning\n+           else\n+             echo \"shellcheck: no *.sh files — skipping\"\n+           fi\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0\n+         with:\n+           python-version: '3.12'\n+       - name: Install yamllint (pinned)\n+         run: pip install yamllint==1.35.1\n+       - name: yamllint (non-workflow YAML, blocking on errors)\n+         # actionlint owns .github/workflows semantics; exclude them to avoid overlap.\n+         # A committed .yamllint.yml (when present) is auto-discovered and authoritative;\n+         # otherwise fall back to yamllint's built-in `relaxed` preset — both block on\n+         # ERRORS only (line-length etc. stay advisory warnings). No matching YAML ⇒ no-op.\n+         run: |\n+           files=$(git ls-files '*.yml' '*.yaml' ':!.github/workflows/**')\n+           if [ -n \"$files\" ]; then\n+             if [ -f .yamllint.yml ] || [ -f .yamllint.yaml ] || [ -f .yamllint ]; then\n+               echo \"$files\" | xargs yamllint\n+             else\n+               echo \"$files\" | xargs yamllint -d relaxed\n+             fi\n+           else\n+             echo \"yamllint: no non-workflow YAML — skipping\"\n+           fi\n+\n+   security-early-fail:\n+     name: Security (PII + Secrets)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+       - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0\n+         with:\n+           node-version-file: '.nvmrc'\n+       - name: PII scan (INV-12, HARD)\n+         run: node scripts/pii-scan.mjs\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Gitleaks secrets scan (INV-11)\n+         # --log-opts=\"HEAD\" (#1908): without it, gitleaks defaults to scanning\n+         # ALL refs reachable in the checkout's object DB (fetch-depth: 0 above\n+         # fetches every remote branch, not just this one). That let an unrelated,\n+         # unmerged branch's commit cross-contaminate every other PR's scan.\n+         # Scoping to HEAD still walks this ref's FULL history (no depth lost) —\n+         # it just stops other branches' commits from being scanned here. The\n+         # nightly `gitleaks-history` job intentionally keeps `--all --full-history`\n+         # as the deep, cross-branch safety net.\n+         run: |\n+           gitleaks detect --source . --config .gitleaks.toml \\\n+             --gitleaks-ignore-path suppressions/.gitleaksignore \\\n+             --log-opts=\"HEAD\" \\\n+             --exit-code 1 --redact\n+\n+\n+   classify-changes:\n+     name: Classify Changes\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     outputs:\n+       docs_only: ${{ steps.classify.outputs.docs_only }}\n+       backend_changed: ${{ steps.classify.outputs.backend_changed }}\n+       frontend_changed: ${{ steps.classify.outputs.frontend_changed }}\n+       infra_changed: ${{ steps.classify.outputs.infra_changed }}\n+       high_risk: ${{ steps.classify.outputs.high_risk }}\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+       - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0\n+         with:\n+           node-version-file: '.nvmrc'\n+       - id: classify\n+         if: hashFiles('scripts/ci-classify-changes.mjs') != ''\n+         run: node scripts/ci-classify-changes.mjs\n+         env:\n+           BASE_SHA: ${{ github.event.pull_request.base.sha || 'origin/main' }}\n+           HEAD_SHA: ${{ github.sha }}\n+\n+\n+   dependency-review:\n+     name: Dependency Review (supply-chain)\n+     # A1 (#1502): the canonical FAST PR supply-chain gate. dependency-review-action\n+     # diffs the PR's dependency-manifest changes and fails on a newly-introduced\n+     # vulnerable dependency — keeping slow full-tree scanners off the always-path.\n+     # PR-only (the action diffs base...head and errors on push). Fail severity is\n+     # configurable via the DEPENDENCY_REVIEW_FAIL_SEVERITY repo variable (default: high).\n+     # Floor: enableSecurityScanning — the same always-path gate as security-early-fail.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     # PR-only AND GHAS-gated (#1685): dependency-review-action HARD-errors when the\n+     # repo lacks Dependency Graph + GitHub Advanced Security, which reds every PR\n+     # while GHAS is off. A JOB-LEVEL guard makes the job SKIP (result=skipped, which\n+     # the ci-required aggregator treats as non-failure) until GHAS is enabled by\n+     # setting the GHAS_ENABLED repo variable to 'true'. NOT step-level\n+     # continue-on-error: that would violate INV-80 and silently neuter the gate once\n+     # GHAS is on. With the var set, a real finding still fails the job and blocks.\n+     if: github.event_name == 'pull_request' && vars.GHAS_ENABLED == 'true'\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - name: Dependency review (fail on newly-introduced high-severity deps)\n+         uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294  # v5.0.0\n+         with:\n+           fail-on-severity: ${{ vars.DEPENDENCY_REVIEW_FAIL_SEVERITY || 'high' }}\n+\n+   iac-scan:\n+     name: IaC Scan (infra-changed)\n+     # A1 (#1502): consume the classify-changes `infra_changed` signal (previously\n+     # classified but UNUSED) — scan terraform / kubernetes / dockerfile only when the\n+     # PR actually touches infrastructure-as-code, keeping the always-path lean.\n+     # #1785: was pinned to a LITERAL `runs-on: ubuntu-latest` on the (disproven)\n+     # assumption that GitHub-hosted runners are never containerized (#1756).\n+     # Evidence (run 28693108584) showed a job pinned to literal `ubuntu-latest`\n+     # actually serviced by self-hosted `arbiter-slot-build-3` — the label alone\n+     # never guaranteed real GitHub-hosted infra here. Rather than keep trusting\n+     # an unverified runner label, Checkov now runs as a plain pip-installed CLI\n+     # (bridgecrewio/checkov-action was only a Docker convenience wrapper around\n+     # it) — no docker-container action, no workspace bind-mount, the whole\n+     # defect class sidestepped entirely (same pattern as the osv-scanner static-\n+     # binary fix, #1767). Enforced by scripts/check-docker-action-runner-safety.mjs.\n+     # #1894: routed through the same `RUNNER_LABELS_TEST` expression as every other\n+     # job. The former literal `ubuntu-latest` used to be intercepted by the self-\n+     # hosted fleet (#1785 evidence), but the fleet dropped the `ubuntu-latest`\n+     # label, so the literal pin fell through to real GitHub-hosted infra and failed\n+     # (spending-limit block). The expression defaults to `[\"ubuntu-latest\"]` when\n+     # the var is unset (consumers unaffected); arbiter's own repo sets it to the\n+     # self-hosted labels. Checkov/tflint are plain CLIs (no docker-action), so the\n+     # runner class remains moot for the #1785 safety class either way.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [classify-changes]\n+     if: needs.classify-changes.outputs.infra_changed == 'true'\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0\n+         with:\n+           python-version: '3.12'\n+       - name: Install Checkov (pinned, no Docker — #1785)\n+         run: pip install checkov==3.3.7\n+       - name: Checkov IaC scan (all frameworks)\n+         run: |\n+           # #1685: `all` is checkov's documented-valid multi-framework value (also\n+           # its own default scope). Hard-fail stays the default (no soft-fail\n+           # flag passed): a real misconfiguration must block (INV-80).\n+           checkov --directory . --framework all --quiet --compact\n+       # #1509 gold-align: Checkov scans for known misconfigurations; tflint is the\n+       # Terraform-specific *linter* (provider-aware deprecations, invalid arg\n+       # combinations, unused declarations). Reference-grade IaC pipelines run it\n+       # BLOCKING — no soft-fail. `--recursive` exits 0 when the repo carries no\n+       # `.tf` files, so non-Terraform infra changes (k8s/dockerfile) stay green.\n+       - name: Setup tflint (Terraform linter)\n+         uses: terraform-linters/setup-tflint@6e1e0642c0289bd619021bf6b34e3c08ed1e005a  # v6.3.0\n+       - name: tflint (Terraform lint, blocking)\n+         run: tflint --recursive --no-color\n+\n+\n+\n+\n+   build-reactor:\n+     name: Build Maven Reactor (#1226)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     permissions:\n+       contents: read\n+     needs: [classify-changes]\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: ./.github/actions/setup-java-maven\n+       - name: Pre-fetch plugins and dependencies (offline cache)\n+         run: mvn dependency:go-offline --batch-mode -q\n+       - name: Build reactor (compile main + test classes, skip test execution)\n+         # -DskipTests runs the test-compile phase but skips execution, so\n+         # target/classes and target/test-classes are produced once here and\n+         # reused by every test job below — no per-job recompile (#1278).\n+         run: mvn --batch-mode -DskipTests install\n+       - name: Pack M2 repository + compiled target/ trees\n+         # m2-reactor.tar stays byte-identical to its prior form (rooted at $HOME/.m2).\n+         # target-reactor.tar is additive (#1278): every module's compiled target/\n+         # (main + test classes) in its own tar rooted at the workspace, so downstream\n+         # run-only test goals (surefire:test) never recompile.\n+         run: |\n+           mkdir -p \"${{ runner.temp }}/.m2-reactor\"\n+           tar -cf \"${{ runner.temp }}/.m2-reactor/m2-reactor.tar\" -C \"$HOME/.m2\" repository\n+           find . -type d -name target -prune -print0 \\\n+             | tar -cf \"${{ runner.temp }}/.m2-reactor/target-reactor.tar\" --null -T -\n+       - name: Upload reactor M2 + target artifact\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: reactor-m2-${{ github.run_id }}\n+           path: ${{ runner.temp }}/.m2-reactor/*.tar\n+           retention-days: 7\n+           overwrite: true\n+\n+   gate:\n+     name: Lint (Java)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     needs: [security-early-fail, classify-changes, build-reactor]\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: ./.github/actions/setup-java-maven\n+       - name: Download reactor M2 artifact\n+         uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: reactor-m2-${{ github.run_id }}\n+           path: ${{ runner.temp }}/.m2-reactor\n+       - run: mvn checkstyle:check --offline\n+\n+\n+   unit-tests:\n+     name: Unit Tests (Java)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [build-reactor, classify-changes]\n+     strategy:\n+       max-parallel: 2\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: ./.github/actions/setup-java-maven\n+       - name: Download reactor M2 artifact\n+         uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: reactor-m2-${{ github.run_id }}\n+           path: ${{ runner.temp }}/.m2-reactor\n+       - run: mvn surefire:test -Punit --offline\n+\n+       - name: Upload test results\n+         if: always()\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: test-results-unit-${{ github.run_id }}\n+           path: target/surefire-reports/\n+           retention-days: 7\n+\n+\n+\n+   archunit-tests:\n+     name: ArchUnit Architecture Tests (Java)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 30\n+     # #1512 gold-align: the lightweight *ArchTest JUnit5 architecture rules\n+     # (dependency-direction / layering / package-boundary) run BLOCKING on EVERY PR.\n+     # Previously they ran only on the weekly 13-archunit-extended cron, leaving up to a\n+     # 7-day fake-green window for a merged architecture regression. The heavier\n+     # full module-graph analysis (the Modulith suite) stays on that weekly cron.\n+     needs: [build-reactor, classify-changes]\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+       - uses: ./.github/actions/setup-java-maven\n+       - name: Download reactor M2 artifact\n+         uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: reactor-m2-${{ github.run_id }}\n+           path: ${{ runner.temp }}/.m2-reactor\n+       - run: mvn surefire:test -Dtest=\"*ArchTest\" --offline\n+\n+\n+\n+\n+\n+\n+   docs-check:\n+     name: Docs Check\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     if: github.event_name == 'pull_request'\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+       - name: Check docs updated with code changes\n+         env:\n+           BASE_SHA: ${{ github.event.pull_request.base.sha }}\n+           HEAD_SHA: ${{ github.event.pull_request.head.sha }}\n+         run: |\n+           # #356: rebased-aware via merge-base; bypass via [skip-docs] commit token.\n+           MB=$(git merge-base \"$BASE_SHA\" \"$HEAD_SHA\" || echo \"$BASE_SHA\")\n+           CHANGED=$(git diff --name-only \"$MB\" \"$HEAD_SHA\")\n+           LOG=$(git log --format=%B \"$MB..$HEAD_SHA\" || true)\n+\n+           HAS_CODE=$(echo \"$CHANGED\" | grep -E '^(src/|__tests__/)' || true)\n+           HAS_DOCS=$(echo \"$CHANGED\" | grep -E '^(docs/|README\\.md)' || true)\n+           BYPASS=$(echo \"$LOG\" | grep -F '[skip-docs]' || true)\n+\n+           if [ -n \"$HAS_CODE\" ] && [ -z \"$HAS_DOCS\" ] && [ -z \"$BYPASS\" ]; then\n+             echo \"::error::Code changed without documentation update\"\n+             echo \"\"\n+             echo \"Files changed in src/ or __tests__/:\"\n+             echo \"$HAS_CODE\"\n+             echo \"\"\n+             echo \"Please update docs/ or README.md, or add [skip-docs] to a commit message to bypass.\"\n+             exit 1\n+           fi\n+           echo \"Docs check passed.\"\n+\n+\n+\n+   debt-gates:\n+     name: Tech Debt Gates\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate]\n+     # E1 (#1502): the PR leg keeps only the FAST static-analysis gates\n+     # (knip/madge/pmd/spotbugs/clippy/ruff/golangci). The whole-suite coverage\n+     # re-run that used to live here was a SECOND full test execution on the PR\n+     # (the unit/integration jobs already ran the suite, and `gate-full` runs\n+     # check-all L2 which enforces the coverage threshold). The threshold floor is\n+     # therefore preserved on PR via gate-full; the heavy coverage report (HTML/lcov\n+     # artifact) is produced at nightly cadence in 06-nightly `coverage-report`.\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+\n+       - uses: ./.github/actions/setup-java-maven\n+       - name: Download reactor M2 artifact\n+         uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1\n+         with:\n+           name: reactor-m2-${{ github.run_id }}\n+           path: ${{ runner.temp }}/.m2-reactor\n+       - run: mvn pmd:check --offline\n+       - run: mvn spotbugs:check --offline\n+\n+\n+\n+\n+   sonar-scan:\n+     name: SonarCloud Scan\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate]\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: ./.github/actions/setup-java-maven\n+\n+\n+       - name: SonarCloud Scan (PR decoration — incremental new-code analysis)\n+         # E1 (#1502): on PRs Sonar runs in pull-request mode — it decorates the PR\n+         # with new-code findings only (incremental), NOT a full deep scan. The full\n+         # branch baseline runs on merge-to-main below and nightly (06-nightly\n+         # `sonar-deep-scan`), keeping the PR path fast.\n+         if: env.SONAR_TOKEN != '' && github.event_name == 'pull_request'\n+         env:\n+           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}\n+           # untrusted refs pass through env (never inlined into the script body).\n+           PR_KEY: ${{ github.event.pull_request.number }}\n+           PR_BRANCH: ${{ github.head_ref }}\n+           PR_BASE: ${{ github.base_ref }}\n+         run: |\n+           mvn sonar:sonar -Dsonar.token=\"$SONAR_TOKEN\" \\\n+             -Dsonar.pullrequest.key=\"$PR_KEY\" \\\n+             -Dsonar.pullrequest.branch=\"$PR_BRANCH\" \\\n+             -Dsonar.pullrequest.base=\"$PR_BASE\"\n+       - name: SonarCloud Scan (full branch baseline)\n+         # Push to a long-lived branch refreshes the full-project baseline so the\n+         # PR decoration above has a fresh reference to diff new code against.\n+         if: env.SONAR_TOKEN != '' && github.event_name != 'pull_request'\n+         env:\n+           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}\n+         run: |\n+           mvn sonar:sonar -Dsonar.token=\"$SONAR_TOKEN\"\n+\n+\n+\n+   debt-ratchet:\n+     name: Debt Ratchet\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate]\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+\n+       - uses: ./.github/actions/setup-java-maven\n+\n+       - name: Run debt ratchet\n+         run: node scripts/debt-report.mjs --gate\n+\n+\n+\n+   gate-full:\n+     name: Full Gate (L2, INV-59)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+\n+     needs: [security-early-fail, classify-changes]\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0\n+         with:\n+           node-version-file: '.nvmrc'\n+           cache: 'npm'\n+       - run: npm ci\n+\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Install actionlint\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/rhysd/actionlint/releases/download/v1.7.3/actionlint_1.7.3_linux_amd64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" actionlint\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Build\n+         run: npm run build\n+       - name: Run full L2 gate (== pre-push)\n+         run: node scripts/check-all.mjs L2 --json gate-result.json\n+       - name: Upload gate-result artifact (activates INV-59 runtime parity)\n+         if: always()\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: gate-result\n+           path: gate-result.json\n+           retention-days: 7\n+\n+\n+   human-approval-required:\n+     name: Human Approval Required (INV-74)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     if: github.event_name == 'pull_request'\n+     steps:\n+       - name: Assert approved-by-human label (anti-bot gate)\n+         uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3  # v9.0.0\n+         with:\n+           github-token: ${{ secrets.GITHUB_TOKEN }}\n+           script: |\n+             const labels = context.payload.pull_request.labels.map(l => l.name);\n+             if (!labels.includes('approved-by-human')) {\n+               core.setFailed(\n+                 'PR requires the \"approved-by-human\" label applied by a human reviewer. ' +\n+                 'See 03-human-approval.yml — a bot approval does not satisfy this check.'\n+               );\n+             }\n+\n+   ci-required:\n+     name: CI Required\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+\n+     needs: [gate, human-approval-required, build-reactor, unit-tests, archunit-tests, security-early-fail, dependency-review, iac-scan, docs-check, sonar-scan, gate-full, debt-gates, debt-ratchet, classify-changes]\n+     if: always()\n+     steps:\n+       - env:\n+           DOCS_ONLY: ${{ needs.classify-changes.outputs.docs_only }}\n+         run: |\n+\n+           if [[ \"${{ needs.classify-changes.result }}\" != \"success\" ]]; then\n+             echo \"classify-changes failed — cannot trust docs_only, failing closed\"\n+             exit 1\n+           fi\n+           # success, or skipped ONLY when the docs-only classification skipped the job\n+           ok() { [[ \"$1\" == \"success\" || ( \"$DOCS_ONLY\" == \"true\" && \"$1\" == \"skipped\" ) ]]; }\n+           if ! ok \"${{ needs.gate.result }}\"; then\n+             echo \"gate failed\"\n+             exit 1\n+           fi\n+\n+           if ! ok \"${{ needs.build-reactor.result }}\"; then\n+             echo \"build-reactor failed\"\n+             exit 1\n+           fi\n+\n+           if ! ok \"${{ needs.unit-tests.result }}\"; then\n+             echo \"unit-tests failed\"\n+             exit 1\n+           fi\n+\n+           if ! ok \"${{ needs.archunit-tests.result }}\"; then\n+             echo \"archunit-tests failed\"\n+             exit 1\n+           fi\n+\n+           if [[ \"${{ needs.security-early-fail.result }}\" != \"success\" ]]; then\n+             echo \"security-early-fail failed\"\n+             exit 1\n+           fi\n+           # dependency-review skips on push (PR-only) — only a real finding blocks.\n+           if [[ \"${{ needs.dependency-review.result }}\" == \"failure\" ]]; then\n+             echo \"dependency-review failed — a newly-introduced dependency exceeds the severity floor\"\n+             exit 1\n+           fi\n+           # iac-scan skips when no infra changed — only a real IaC finding blocks.\n+           if [[ \"${{ needs.iac-scan.result }}\" == \"failure\" ]]; then\n+             echo \"iac-scan failed — an infrastructure-as-code misconfiguration was found\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.docs-check.result }}\" == \"failure\" ]]; then\n+             echo \"docs-check failed\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.sonar-scan.result }}\" == \"failure\" ]]; then\n+             echo \"sonar-scan failed\"\n+             exit 1\n+           fi\n+           if ! ok \"${{ needs.debt-gates.result }}\"; then\n+             echo \"debt-gates failed\"\n+             exit 1\n+           fi\n+           if ! ok \"${{ needs.debt-ratchet.result }}\"; then\n+             echo \"debt-ratchet failed\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.gate-full.result }}\" != \"success\" && \"${{ needs.gate-full.result }}\" != \"skipped\" ]]; then\n+             echo \"gate-full (L2) failed or was cancelled\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.human-approval-required.result }}\" == \"failure\" ]]; then\n+             echo \"human-approval-required failed — PR needs approved-by-human label\"\n+             exit 1\n+           fi\n+\n+\n+   post-merge-gate:\n+     name: D4-LITE post-merge gate (main)\n+     needs: [classify-changes]\n+     if: >\n+       github.event_name == 'push' &&\n+       github.ref == 'refs/heads/main' &&\n+       needs.classify-changes.result == 'success' &&\n+       needs.classify-changes.outputs.docs_only != 'true'\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0\n+         with:\n+           node-version-file: '.nvmrc'\n+           cache: 'npm'\n+       - run: npm ci\n+\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Install actionlint\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/rhysd/actionlint/releases/download/v1.7.3/actionlint_1.7.3_linux_amd64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" actionlint\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Build\n+         run: npm run build\n+       - name: Run full L2 gate (D4-LITE catch-net)\n+         run: node scripts/check-all.mjs L2\n+\n+   post-merge-notify:\n+     name: D4-LITE notify on failure\n+     needs: [post-merge-gate]\n+     if: always() && needs.post-merge-gate.result == 'failure'\n+     permissions:\n+       issues: write\n+     uses: ./.github/workflows/_notify.yml\n+     with:\n+       workflow_name: D4-LITE post-merge gate\n+       area: ci\n+       severity: P1\n+       branch: main\n+       run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\n+       failure_summary: Post-merge L2 gate failed on main — a change merged without full verification.\n+       status: failure\n+\n+\n+\n\n ❯ __tests__/templates/ci-parallel-test-jobs.test.ts:125:24\n    123|       }) as unknown as Record<string, unknown>\n    124|       const rendered = renderTemplate('github/workflows/01-pr-fast.yml…\n    125|       expect(rendered).toContain('integration-tests:')\n       |                        ^\n    126|     })\n    127|\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/7]⎯\n\n FAIL  __tests__/templates/ci-parallel-test-jobs.test.ts > 01-pr-fast.yml.ejs — parallel test category jobs (#219) > Java L2 (Maven) > ci-required includes all test jobs\nAssertionError: expected '\\n    name: CI Required\\n    runs-on:…' to contain 'integration-tests'\n\n- Expected\n+ Received\n\n- integration-tests\n+\n+     name: CI Required\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+\n+     needs: [gate, human-approval-required, build-reactor, unit-tests, archunit-tests, security-early-fail, dependency-review, iac-scan, docs-check, sonar-scan, gate-full, debt-gates, debt-ratchet, classify-changes]\n+     if: always()\n+     steps:\n+       - env:\n+           DOCS_ONLY: ${{ needs.classify-changes.outputs.docs_only }}\n+         run: |\n+\n+           if [[ \"${{ needs.classify-changes.result }}\" != \"success\" ]]; then\n+             echo \"classify-changes failed — cannot trust docs_only, failing closed\"\n+             exit 1\n+           fi\n+           # success, or skipped ONLY when the docs-only classification skipped the job\n+           ok() { [[ \"$1\" == \"success\" || ( \"$DOCS_ONLY\" == \"true\" && \"$1\" == \"skipped\" ) ]]; }\n+           if ! ok \"${{ needs.gate.result }}\"; then\n+             echo \"gate failed\"\n+             exit 1\n+           fi\n+\n+           if ! ok \"${{ needs.build-reactor.result }}\"; then\n+             echo \"build-reactor failed\"\n+             exit 1\n+           fi\n+\n+           if ! ok \"${{ needs.unit-tests.result }}\"; then\n+             echo \"unit-tests failed\"\n+             exit 1\n+           fi\n+\n+           if ! ok \"${{ needs.archunit-tests.result }}\"; then\n+             echo \"archunit-tests failed\"\n+             exit 1\n+           fi\n+\n+           if [[ \"${{ needs.security-early-fail.result }}\" != \"success\" ]]; then\n+             echo \"security-early-fail failed\"\n+             exit 1\n+           fi\n+           # dependency-review skips on push (PR-only) — only a real finding blocks.\n+           if [[ \"${{ needs.dependency-review.result }}\" == \"failure\" ]]; then\n+             echo \"dependency-review failed — a newly-introduced dependency exceeds the severity floor\"\n+             exit 1\n+           fi\n+           # iac-scan skips when no infra changed — only a real IaC finding blocks.\n+           if [[ \"${{ needs.iac-scan.result }}\" == \"failure\" ]]; then\n+             echo \"iac-scan failed — an infrastructure-as-code misconfiguration was found\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.docs-check.result }}\" == \"failure\" ]]; then\n+             echo \"docs-check failed\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.sonar-scan.result }}\" == \"failure\" ]]; then\n+             echo \"sonar-scan failed\"\n+             exit 1\n+           fi\n+           if ! ok \"${{ needs.debt-gates.result }}\"; then\n+             echo \"debt-gates failed\"\n+             exit 1\n+           fi\n+           if ! ok \"${{ needs.debt-ratchet.result }}\"; then\n+             echo \"debt-ratchet failed\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.gate-full.result }}\" != \"success\" && \"${{ needs.gate-full.result }}\" != \"skipped\" ]]; then\n+             echo \"gate-full (L2) failed or was cancelled\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.human-approval-required.result }}\" == \"failure\" ]]; then\n+             echo \"human-approval-required failed — PR needs approved-by-human label\"\n+             exit 1\n+           fi\n+\n+\n+   post-merge-gate:\n+     name: D4-LITE post-merge gate (main)\n+     needs: [classify-changes]\n+     if: >\n+       github.event_name == 'push' &&\n+       github.ref == 'refs/heads/main' &&\n+       needs.classify-changes.result == 'success' &&\n+       needs.classify-changes.outputs.docs_only != 'true'\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0\n+         with:\n+           node-version-file: '.nvmrc'\n+           cache: 'npm'\n+       - run: npm ci\n+\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Install actionlint\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/rhysd/actionlint/releases/download/v1.7.3/actionlint_1.7.3_linux_amd64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" actionlint\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Build\n+         run: npm run build\n+       - name: Run full L2 gate (D4-LITE catch-net)\n+         run: node scripts/check-all.mjs L2\n+\n+   post-merge-notify:\n+     name: D4-LITE notify on failure\n+     needs: [post-merge-gate]\n+     if: always() && needs.post-merge-gate.result == 'failure'\n+     permissions:\n+       issues: write\n+     uses: ./.github/workflows/_notify.yml\n+     with:\n+       workflow_name: D4-LITE post-merge gate\n+       area: ci\n+       severity: P1\n+       branch: main\n+       run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\n+       failure_summary: Post-merge L2 gate failed on main — a change merged without full verification.\n+       status: failure\n+\n+\n+\n\n ❯ __tests__/templates/ci-parallel-test-jobs.test.ts:137:26\n    135|       const ciRequired = rendered.split('ci-required:')[1]\n    136|       expect(ciRequired).toContain('unit-tests')\n    137|       expect(ciRequired).toContain('integration-tests')\n       |                          ^\n    138|     })\n    139|   })\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[7/7]⎯\n\n",
  "observed_failure": "FAIL  __tests__/templates/ci-parallel-test-jobs.test.ts",
  "recorded_at": "2026-07-13T08:48:32.726Z"
}
