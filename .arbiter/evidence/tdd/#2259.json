{
  "$schemaVersion": 1,
  "task_id": "#2259",
  "test_path": "__tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts",
  "test_commit_sha": "75ac4e2160ddfac88f81e5413e1006ab3bd35354",
  "test_blob_sha": "ba9922ca4543787838f1bb8a014b078cc3f7fe82",
  "test_run_log": "\n RUN  v4.1.10 .\n\n ❯ __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts (8 tests | 8 failed) 22ms\n     × github/actions/setup-node-pnpm/action.yml.ejs has a Node setup path independent of a root lockfile 5ms\n     × github/workflows/01-pr-fast.yml.ejs has a Node setup path independent of a root lockfile 4ms\n     × github/workflows/04-deploy-test.yml.ejs has a Node setup path independent of a root lockfile 4ms\n     × github/workflows/_contract-postman.yml.ejs has a Node setup path independent of a root lockfile 1ms\n     × github/workflows/_monthly.yml.ejs has a Node setup path independent of a root lockfile 2ms\n     × github/workflows/_nightly.yml.ejs has a Node setup path independent of a root lockfile 3ms\n     × github/workflows/16-frontend-quality.yml.ejs has a Node setup path independent of a root lockfile 1ms\n     × github/workflows/18-frontend-lane.yml.ejs has a Node setup path independent of a root lockfile 1ms\n\n Test Files  1 failed (1)\n      Tests  8 failed (8)\n   Start at  20:55:51\n   Duration  285ms (transform 105ms, setup 14ms, import 126ms, tests 22ms, environment 0ms)\n\n\n\n⎯⎯⎯⎯⎯⎯⎯ Failed Tests 8 ⎯⎯⎯⎯⎯⎯⎯\n\n FAIL  __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts > non-TypeScript workflow Node tooling (#2259) > github/actions/setup-node-pnpm/action.yml.ejs has a Node setup path independent of a root lockfile\nAssertionError: expected '# NOTE: directory is named `setup-nod…' to contain 'node-version: \\'22\\''\n\n- Expected\n+ Received\n\n- node-version: '22'\n+ # NOTE: directory is named `setup-node-pnpm` for historical reasons; it sets up\n+ # Node + npm (not pnpm). The name is retained to avoid orphaning the dir in\n+ # already-generated repos on their next `arbiter update` (#1131).\n+ name: Setup Node.js + npm ci\n+ description: >-\n+   Sets up Node.js from .nvmrc with the npm cache, then runs `npm ci`.\n+   Set the `install` input to 'false' for setup-only jobs.\n+\n+ inputs:\n+   node-version-file:\n+     description: Path to the node version file\n+     required: false\n+     default: .nvmrc\n+   install:\n+     description: Run `npm ci` after setup (set 'false' to skip install)\n+     required: false\n+     default: 'true'\n+\n+ runs:\n+   using: composite\n+   steps:\n+     - name: Setup Node.js\n+       uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0\n+       with:\n+         node-version-file: ${{ inputs.node-version-file }}\n+         cache: npm\n+     - name: Install dependencies (npm ci)\n+       if: ${{ inputs.install == 'true' }}\n+       shell: bash\n+       run: npm ci\n+\n\n ❯ expectBareNodeSetup __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts:17:18\n     15|\n     16| function expectBareNodeSetup(output: string): void {\n     17|   expect(output).toContain(\"node-version: '22'\")\n       |                  ^\n     18|   expect(output).toContain('package-manager-cache: false')\n     19|   expect(output).not.toContain('node-version-file: .nvmrc')\n ❯ __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts:40:5\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/8]⎯\n\n FAIL  __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts > non-TypeScript workflow Node tooling (#2259) > github/workflows/01-pr-fast.yml.ejs has a Node setup path independent of a root lockfile\nAssertionError: expected '# Reference: docs/REFERENCE/workflow-…' to contain 'node-version: \\'22\\''\n\n- Expected\n+ Received\n\n- node-version: '22'\n+ # Reference: docs/REFERENCE/workflow-pr-fast.md\n+ name: PR Fast (T1)\n+\n+ permissions:\n+   contents: read\n+\n+ on:\n+   push:\n+\n+     branches: [main]\n+\n+   workflow_dispatch:\n+   pull_request:\n+     types: [opened, synchronize, reopened, labeled, ready_for_review, unlabeled]\n+\n+     branches: [main]\n+\n+ # Cancel in-progress runs on the same ref; main always completes (#357).\n+ concurrency:\n+   group: pr-fast-${{ github.head_ref || github.ref_name }}\n+\n+   cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}\n+\n+\n+ jobs:\n+   config-lint:\n+     name: Config Lint (yaml + shell)\n+     # gold-align (#1506 yamllint / #1507 shellcheck): a language-agnostic style/\n+     # structure lint lane. yamllint checks non-workflow YAML (indentation, duplicate\n+     # keys, trailing spaces, document structure) — complementary to actionlint, which\n+     # owns .github/workflows *semantics*. shellcheck checks generated *.sh for unquoted\n+     # expansions / unsafe word-splitting at warning severity. Both BLOCKING; both\n+     # version-pinned for reproducibility. Each linter no-ops cleanly (green) when the\n+     # repo carries no matching files. Committed .yamllint.yml / .shellcheckrc (when\n+     # present) are auto-discovered and make the rules explicit + tunable.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+       - name: Install shellcheck (pinned)\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/koalaman/shellcheck/releases/download/v0.10.0/shellcheck-v0.10.0.linux.x86_64.tar.xz \\\n+             | tar -xJ -C \"$RUNNER_TEMP/bin\" --strip-components=1 shellcheck-v0.10.0/shellcheck\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: shellcheck (generated shell, blocking at warning severity)\n+         # .shellcheckrc (when present) is auto-discovered. No *.sh ⇒ no-op (green).\n+         run: |\n+           files=$(git ls-files '*.sh')\n+           if [ -n \"$files\" ]; then\n+             echo \"$files\" | xargs shellcheck --severity=warning\n+           else\n+             echo \"shellcheck: no *.sh files — skipping\"\n+           fi\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0\n+         with:\n+           python-version: '3.12'\n+       - name: Install yamllint (pinned)\n+         run: pip install yamllint==1.35.1\n+       - name: yamllint (non-workflow YAML, blocking on errors)\n+         # actionlint owns .github/workflows semantics; exclude them to avoid overlap.\n+         # A committed .yamllint.yml (when present) is auto-discovered and authoritative;\n+         # otherwise fall back to yamllint's built-in `relaxed` preset — both block on\n+         # ERRORS only (line-length etc. stay advisory warnings). No matching YAML ⇒ no-op.\n+         run: |\n+           files=$(git ls-files '*.yml' '*.yaml' ':!.github/workflows/**')\n+           if [ -n \"$files\" ]; then\n+             if [ -f .yamllint.yml ] || [ -f .yamllint.yaml ] || [ -f .yamllint ]; then\n+               echo \"$files\" | xargs yamllint\n+             else\n+               echo \"$files\" | xargs yamllint -d relaxed\n+             fi\n+           else\n+             echo \"yamllint: no non-workflow YAML — skipping\"\n+           fi\n+\n+   security-early-fail:\n+     name: Security (PII + Secrets)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0\n+         with:\n+           node-version-file: '.nvmrc'\n+       - name: PII scan (INV-12, HARD)\n+         run: node scripts/pii-scan.mjs\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Gitleaks secrets scan (INV-11)\n+         # --log-opts=\"HEAD\" (#1908): without it, gitleaks defaults to scanning\n+         # ALL refs reachable in the checkout's object DB (fetch-depth: 0 above\n+         # fetches every remote branch, not just this one). That let an unrelated,\n+         # unmerged branch's commit cross-contaminate every other PR's scan.\n+         # Scoping to HEAD still walks this ref's FULL history (no depth lost) —\n+         # it just stops other branches' commits from being scanned here. The\n+         # nightly `gitleaks-history` job intentionally keeps `--all --full-history`\n+         # as the deep, cross-branch safety net.\n+         run: |\n+           gitleaks detect --source . --config .gitleaks.toml \\\n+             --gitleaks-ignore-path suppressions/.gitleaksignore \\\n+             --log-opts=\"HEAD\" \\\n+             --exit-code 1 --redact\n+\n+\n+   classify-changes:\n+     name: Classify Changes\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     outputs:\n+       docs_only: ${{ steps.classify.outputs.docs_only }}\n+       backend_changed: ${{ steps.classify.outputs.backend_changed }}\n+       frontend_changed: ${{ steps.classify.outputs.frontend_changed }}\n+       infra_changed: ${{ steps.classify.outputs.infra_changed }}\n+       high_risk: ${{ steps.classify.outputs.high_risk }}\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0\n+         with:\n+           node-version-file: '.nvmrc'\n+       - id: classify\n+         if: hashFiles('scripts/ci-classify-changes.mjs') != ''\n+         run: node scripts/ci-classify-changes.mjs\n+         env:\n+           BASE_SHA: ${{ github.event.pull_request.base.sha || 'origin/main' }}\n+           HEAD_SHA: ${{ github.sha }}\n+\n+\n+   dependency-review:\n+     name: Dependency Review (supply-chain)\n+     # A1 (#1502): the canonical FAST PR supply-chain gate. dependency-review-action\n+     # diffs the PR's dependency-manifest changes and fails on a newly-introduced\n+     # vulnerable dependency — keeping slow full-tree scanners off the always-path.\n+     # PR-only (the action diffs base...head and errors on push). Fail severity is\n+     # configurable via the DEPENDENCY_REVIEW_FAIL_SEVERITY repo variable (default: high).\n+     # Floor: enableSecurityScanning — the same always-path gate as security-early-fail.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     # PR-only AND GHAS-gated (#1685): dependency-review-action HARD-errors when the\n+     # repo lacks Dependency Graph + GitHub Advanced Security, which reds every PR\n+     # while GHAS is off. A JOB-LEVEL guard makes the job SKIP (result=skipped, which\n+     # the ci-required aggregator treats as non-failure) until GHAS is enabled by\n+     # setting the GHAS_ENABLED repo variable to 'true'. NOT step-level\n+     # continue-on-error: that would violate INV-80 and silently neuter the gate once\n+     # GHAS is on. With the var set, a real finding still fails the job and blocks.\n+     if: github.event_name == 'pull_request' && vars.GHAS_ENABLED == 'true'\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+       - name: Dependency review (fail on newly-introduced high-severity deps)\n+         uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294  # v5.0.0\n+         with:\n+           fail-on-severity: ${{ vars.DEPENDENCY_REVIEW_FAIL_SEVERITY || 'high' }}\n+\n+   iac-scan:\n+     name: IaC Scan (infra-changed)\n+     # A1 (#1502): consume the classify-changes `infra_changed` signal (previously\n+     # classified but UNUSED) — scan terraform / kubernetes / dockerfile only when the\n+     # PR actually touches infrastructure-as-code, keeping the always-path lean.\n+     # #1785: was pinned to a LITERAL `runs-on: ubuntu-latest` on the (disproven)\n+     # assumption that GitHub-hosted runners are never containerized (#1756).\n+     # Evidence (run 28693108584) showed a job pinned to literal `ubuntu-latest`\n+     # actually serviced by self-hosted `arbiter-slot-build-3` — the label alone\n+     # never guaranteed real GitHub-hosted infra here. Rather than keep trusting\n+     # an unverified runner label, Checkov now runs as a plain pip-installed CLI\n+     # (bridgecrewio/checkov-action was only a Docker convenience wrapper around\n+     # it) — no docker-container action, no workspace bind-mount, the whole\n+     # defect class sidestepped entirely (same pattern as the osv-scanner static-\n+     # binary fix, #1767). Enforced by scripts/check-docker-action-runner-safety.mjs.\n+     # #1894: routed through the same `RUNNER_LABELS_TEST` expression as every other\n+     # job. The former literal `ubuntu-latest` used to be intercepted by the self-\n+     # hosted fleet (#1785 evidence), but the fleet dropped the `ubuntu-latest`\n+     # label, so the literal pin fell through to real GitHub-hosted infra and failed\n+     # (spending-limit block). The expression defaults to `[\"ubuntu-latest\"]` when\n+     # the var is unset (consumers unaffected); arbiter's own repo sets it to the\n+     # self-hosted labels. Checkov/tflint are plain CLIs (no docker-action), so the\n+     # runner class remains moot for the #1785 safety class either way.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [classify-changes]\n+     if: needs.classify-changes.outputs.infra_changed == 'true'\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0\n+         with:\n+           python-version: '3.12'\n+       - name: Install Checkov (pinned, no Docker — #1785)\n+         run: pip install checkov==3.3.7\n+       - name: Checkov IaC scan (all frameworks)\n+         run: |\n+           # #1685: `all` is checkov's documented-valid multi-framework value (also\n+           # its own default scope). Hard-fail stays the default (no soft-fail\n+           # flag passed): a real misconfiguration must block (INV-80).\n+           checkov --directory . --framework all --quiet --compact\n+       # #1509 gold-align: Checkov scans for known misconfigurations; tflint is the\n+       # Terraform-specific *linter* (provider-aware deprecations, invalid arg\n+       # combinations, unused declarations). Reference-grade IaC pipelines run it\n+       # BLOCKING — no soft-fail. `--recursive` exits 0 when the repo carries no\n+       # `.tf` files, so non-Terraform infra changes (k8s/dockerfile) stay green.\n+       - name: Setup tflint (Terraform linter)\n+         uses: terraform-linters/setup-tflint@6e1e0642c0289bd619021bf6b34e3c08ed1e005a  # v6.3.0\n+       - name: tflint (Terraform lint, blocking)\n+         run: tflint --recursive --no-color\n+\n+\n+   gate:\n+     name: Lint + Test (Python)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [security-early-fail, classify-changes]\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0\n+         with:\n+           python-version: '3.12'\n+           cache: 'pip'\n+       - run: pip install -e \".[dev]\"\n+       - run: ruff check .\n+       - run: ruff format --check .\n+       - run: pytest --cov --junitxml=test-results/junit.xml\n+\n+       - name: Dep audit (INV-13)\n+         run: pip-audit\n+\n+\n+       - name: Upload test results\n+         id: upload-test-results\n+         if: always()\n+         # Supplementary artifact — never let an Artifacts-quota hiccup fail the\n+         # job whose actual signal (the test run above) already passed.\n+         continue-on-error: true\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: test-results-${{ github.run_id }}\n+           path: test-results/\n+           retention-days: 7\n+\n+\n+\n+\n+   docs-check:\n+     name: Docs Check\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     if: github.event_name == 'pull_request'\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       - name: Check docs updated with code changes\n+         env:\n+           BASE_SHA: ${{ github.event.pull_request.base.sha }}\n+           HEAD_SHA: ${{ github.event.pull_request.head.sha }}\n+         run: |\n+           # #356: rebased-aware via merge-base; bypass via [skip-docs] commit token.\n+           MB=$(git merge-base \"$BASE_SHA\" \"$HEAD_SHA\" || echo \"$BASE_SHA\")\n+           CHANGED=$(git diff --name-only \"$MB\" \"$HEAD_SHA\")\n+           LOG=$(git log --format=%B \"$MB..$HEAD_SHA\" || true)\n+\n+           HAS_CODE=$(echo \"$CHANGED\" | grep -E '^(src/|__tests__/)' || true)\n+           HAS_DOCS=$(echo \"$CHANGED\" | grep -E '^(docs/|README\\.md)' || true)\n+           BYPASS=$(echo \"$LOG\" | grep -F '[skip-docs]' || true)\n+\n+           if [ -n \"$HAS_CODE\" ] && [ -z \"$HAS_DOCS\" ] && [ -z \"$BYPASS\" ]; then\n+             echo \"::error::Code changed without documentation update\"\n+             echo \"\"\n+             echo \"Files changed in src/ or __tests__/:\"\n+             echo \"$HAS_CODE\"\n+             echo \"\"\n+             echo \"Please update docs/ or README.md, or add [skip-docs] to a commit message to bypass.\"\n+             exit 1\n+           fi\n+           echo \"Docs check passed.\"\n+\n+\n+\n+   debt-gates:\n+     name: Tech Debt Gates\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate]\n+     # E1 (#1502): the PR leg keeps only the FAST static-analysis gates\n+     # (knip/madge/pmd/spotbugs/clippy/ruff/golangci). The whole-suite coverage\n+     # re-run that used to live here was a SECOND full test execution on the PR\n+     # (the unit/integration jobs already ran the suite, and `gate-full` runs\n+     # check-all L2 which enforces the coverage threshold). The threshold floor is\n+     # therefore preserved on PR via gate-full; the heavy coverage report (HTML/lcov\n+     # artifact) is produced at nightly cadence in 06-nightly `coverage-report`.\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0\n+         with:\n+           python-version: '3.12'\n+           cache: 'pip'\n+       - run: pip install -e \".[dev]\"\n+       - run: ruff check --select C901,PLR0911,PLR0912,PLR0913,PLR0915 .\n+       - run: ruff check --select F401,F811 .\n+\n+\n+\n+\n+   sonar-scan:\n+     name: SonarCloud Scan\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate]\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+\n+\n+       - name: SonarCloud Scan (PR decoration — incremental new-code analysis)\n+         # E1 (#1502): on PRs Sonar runs in pull-request mode — it decorates the PR\n+         # with new-code findings only (incremental), NOT a full deep scan. The full\n+         # branch baseline runs on merge-to-main below and nightly (06-nightly\n+         # `sonar-deep-scan`), keeping the PR path fast.\n+         if: env.SONAR_TOKEN != '' && github.event_name == 'pull_request'\n+         env:\n+           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}\n+           # untrusted refs pass through env (never inlined into the script body).\n+           PR_KEY: ${{ github.event.pull_request.number }}\n+           PR_BRANCH: ${{ github.head_ref }}\n+           PR_BASE: ${{ github.base_ref }}\n+         run: |\n+           npx sonar-scanner -Dsonar.token=\"$SONAR_TOKEN\" \\\n+             -Dsonar.pullrequest.key=\"$PR_KEY\" \\\n+             -Dsonar.pullrequest.branch=\"$PR_BRANCH\" \\\n+             -Dsonar.pullrequest.base=\"$PR_BASE\"\n+       - name: SonarCloud Scan (full branch baseline)\n+         # Push to a long-lived branch refreshes the full-project baseline so the\n+         # PR decoration above has a fresh reference to diff new code against.\n+         if: env.SONAR_TOKEN != '' && github.event_name != 'pull_request'\n+         env:\n+           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}\n+         run: |\n+           npx sonar-scanner -Dsonar.token=\"$SONAR_TOKEN\"\n+\n+\n+\n+   debt-ratchet:\n+     name: Debt Ratchet\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     needs: [gate]\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0\n+         with:\n+           python-version: '3.12'\n+           cache: 'pip'\n+       - run: pip install -e \".[dev]\"\n+\n+       - name: Run debt ratchet\n+         run: node scripts/debt-report.mjs --gate\n+\n+\n+\n+   gate-full:\n+     name: Full Gate (L2, INV-59)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+\n+     needs: [security-early-fail, classify-changes]\n+     if: needs.classify-changes.outputs.docs_only != 'true'\n+\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0\n+         with:\n+           node-version-file: '.nvmrc'\n+           cache: 'npm'\n+       - run: npm ci\n+\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Install actionlint\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/rhysd/actionlint/releases/download/v1.7.3/actionlint_1.7.3_linux_amd64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" actionlint\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Install psmisc (fuser — #2098 gate-exec queue-depth advisory)\n+         # Mirrors the identical step in the Unit Tests job (see there for the\n+         # full rationale) so this job's own gate-exec queue-depth advisory\n+         # works too, independent of whether Unit Tests already ran on this\n+         # runner. For arbiter's own repo, `check-all.mjs L2` below also runs\n+         # the waiter-count/gate-exec test suite directly.\n+         run: |\n+           if ! command -v fuser >/dev/null 2>&1; then\n+             if [ \"$(id -u)\" -eq 0 ]; then\n+               apt-get update -qq && apt-get install -y -qq psmisc\n+             else\n+               sudo apt-get update -qq && sudo apt-get install -y -qq psmisc\n+             fi\n+           fi\n+       - name: Build\n+         run: npm run build\n+       - name: Run full L2 gate (== pre-push)\n+         run: node scripts/check-all.mjs L2 --json gate-result.json\n+       - name: Upload gate-result artifact (activates INV-59 runtime parity)\n+         id: upload-gate-result\n+         if: always()\n+         # check-local-ci-parity.mjs already treats a missing gate-result artifact\n+         # as a graceful skip, not a hard failure — an Artifacts-quota hiccup on\n+         # this upload must not fail the job the real gate already ran in.\n+         continue-on-error: true\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: gate-result\n+           path: gate-result.json\n+           retention-days: 7\n+\n+\n+   human-approval-required:\n+     name: Human Approval Required (INV-74)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     if: github.event_name == 'pull_request'\n+     steps:\n+       - name: Assert approved-by-human label (anti-bot gate)\n+         uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3  # v9.0.0\n+         with:\n+           github-token: ${{ secrets.GITHUB_TOKEN }}\n+           script: |\n+             const labels = context.payload.pull_request.labels.map(l => l.name);\n+             if (!labels.includes('approved-by-human')) {\n+               core.setFailed(\n+                 'PR requires the \"approved-by-human\" label applied by a human reviewer. ' +\n+                 'See 03-human-approval.yml — a bot approval does not satisfy this check.'\n+               );\n+             }\n+\n+   ci-required:\n+     name: CI Required\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+\n+     needs: [gate, human-approval-required, security-early-fail, dependency-review, iac-scan, docs-check, sonar-scan, gate-full, debt-gates, debt-ratchet, classify-changes]\n+     if: always()\n+     steps:\n+       - env:\n+           DOCS_ONLY: ${{ needs.classify-changes.outputs.docs_only }}\n+         run: |\n+\n+           if [[ \"${{ needs.classify-changes.result }}\" != \"success\" ]]; then\n+             echo \"classify-changes failed — cannot trust docs_only, failing closed\"\n+             exit 1\n+           fi\n+           # success, or skipped ONLY when the docs-only classification skipped the job\n+           ok() { [[ \"$1\" == \"success\" || ( \"$DOCS_ONLY\" == \"true\" && \"$1\" == \"skipped\" ) ]]; }\n+           if ! ok \"${{ needs.gate.result }}\"; then\n+             echo \"gate failed\"\n+             exit 1\n+           fi\n+\n+           if [[ \"${{ needs.security-early-fail.result }}\" != \"success\" ]]; then\n+             echo \"security-early-fail failed\"\n+             exit 1\n+           fi\n+           # dependency-review skips on push (PR-only) — only a real finding blocks.\n+           if [[ \"${{ needs.dependency-review.result }}\" == \"failure\" ]]; then\n+             echo \"dependency-review failed — a newly-introduced dependency exceeds the severity floor\"\n+             exit 1\n+           fi\n+           # iac-scan skips when no infra changed — only a real IaC finding blocks.\n+           if [[ \"${{ needs.iac-scan.result }}\" == \"failure\" ]]; then\n+             echo \"iac-scan failed — an infrastructure-as-code misconfiguration was found\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.docs-check.result }}\" == \"failure\" ]]; then\n+             echo \"docs-check failed\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.sonar-scan.result }}\" == \"failure\" ]]; then\n+             echo \"sonar-scan failed\"\n+             exit 1\n+           fi\n+           if ! ok \"${{ needs.debt-gates.result }}\"; then\n+             echo \"debt-gates failed\"\n+             exit 1\n+           fi\n+           if ! ok \"${{ needs.debt-ratchet.result }}\"; then\n+             echo \"debt-ratchet failed\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.gate-full.result }}\" != \"success\" && \"${{ needs.gate-full.result }}\" != \"skipped\" ]]; then\n+             echo \"gate-full (L2) failed or was cancelled\"\n+             exit 1\n+           fi\n+           if [[ \"${{ needs.human-approval-required.result }}\" == \"failure\" ]]; then\n+             echo \"human-approval-required failed — PR needs approved-by-human label\"\n+             exit 1\n+           fi\n+\n+\n+   post-merge-gate:\n+     name: D4-LITE post-merge gate (main)\n+     needs: [classify-changes, gate-full]\n+     if: >\n+       always() &&\n+       github.event_name == 'push' &&\n+       github.ref == 'refs/heads/main' &&\n+       needs.classify-changes.result == 'success' &&\n+       needs.classify-changes.outputs.docs_only != 'true'\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0\n+         with:\n+           node-version-file: '.nvmrc'\n+           cache: 'npm'\n+       - run: npm ci\n+\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Install actionlint\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/rhysd/actionlint/releases/download/v1.7.3/actionlint_1.7.3_linux_amd64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" actionlint\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Build\n+         run: npm run build\n+       - name: Run full L2 gate (D4-LITE catch-net)\n+         run: node scripts/check-all.mjs L2\n+\n+   post-merge-notify:\n+     name: D4-LITE notify on failure\n+     needs: [post-merge-gate]\n+     if: always() && needs.post-merge-gate.result == 'failure'\n+     permissions:\n+       issues: write\n+     uses: ./.github/workflows/_notify.yml\n+     with:\n+       workflow_name: D4-LITE post-merge gate\n+       area: ci\n+       severity: P1\n+       branch: main\n+       run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\n+       failure_summary: Post-merge L2 gate failed on main — a change merged without full verification.\n+       status: failure\n+\n+\n\n ❯ expectBareNodeSetup __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts:17:18\n     15|\n     16| function expectBareNodeSetup(output: string): void {\n     17|   expect(output).toContain(\"node-version: '22'\")\n       |                  ^\n     18|   expect(output).toContain('package-manager-cache: false')\n     19|   expect(output).not.toContain('node-version-file: .nvmrc')\n ❯ __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts:40:5\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/8]⎯\n\n FAIL  __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts > non-TypeScript workflow Node tooling (#2259) > github/workflows/04-deploy-test.yml.ejs has a Node setup path independent of a root lockfile\nAssertionError: expected '# 04-deploy-test.yml — generated by a…' to contain 'node-version: \\'22\\''\n\n- Expected\n+ Received\n\n- node-version: '22'\n+ # 04-deploy-test.yml — generated by arbiter — Deploy to TEST environment\n+ # Trigger: push to main branch (or develop when branchingStrategy=github-flow-with-develop)\n+ # Slot 04 reserved in CI-TIER-MODEL.md §2 — opt-in via deployTarget !== 'none'.\n+ #\n+ # Job graph:\n+ #   build-and-sign ──── deploy-test ──── post-deploy-smoke\n+ #       (failure)──── notify\n+ #\n+ # Security invariants:\n+ #   - Trivy HIGH/CRITICAL scan MUST precede container registry push\n+ #   - Cosign keyless signing (OIDC) after push\n+ #   - SBOM attestation (CycloneDX) linked to image digest\n+ #   - ACR idempotent retry tag: <sha>-<run_attempt> (H2-M3)\n+ #   - Sigstore preflight check before signing (H3-S1)\n+ #\n+ # Ref: docs/SYSTEM/CI-TIER-MODEL.md §2 (slot 04, deploy-test)\n+\n+\n+ name: Deploy Test\n+\n+ on:\n+   push:\n+\n+     branches: [main]\n+\n+     paths-ignore:\n+       - 'docs/**'\n+       - '*.md'\n+       - '.gitignore'\n+       - '.editorconfig'\n+\n+ concurrency:\n+   group: ${{ github.workflow }}-${{ github.ref }}\n+   cancel-in-progress: false\n+\n+ permissions:\n+   contents: read\n+   id-token: write\n+   packages: write\n+\n+ jobs:\n+   # === GROUP: build ===\n+\n+   build-and-sign:\n+     name: Build, Scan, Sign & SBOM\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_BUILD || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 40\n+     outputs:\n+       image-digest: ${{ steps.push.outputs.digest }}\n+       image-tag: ${{ steps.tag.outputs.image_tag }}\n+     steps:\n+       - name: Checkout\n+         uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+\n+\n+       - name: Setup Node.js\n+         uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0\n+         with:\n+           node-version-file: .nvmrc\n+           cache: npm\n+\n+\n+       - name: Set up Docker Buildx\n+         uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f  # v3\n+         with:\n+           driver: docker\n+\n+       # #1650: GitHub Actions does NOT auto-authenticate the docker CLI to the\n+       # container registry; `packages: write` only scopes GITHUB_TOKEN. Without an\n+       # explicit login, every `docker push` / `cosign sign` below returns\n+       # `denied: requested access to the resource is denied`. Mirrors 05-release.\n+       - name: Log in to container registry\n+         uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0  # v4.4.0\n+         with:\n+           registry: ${{ vars.CONTAINER_REGISTRY || 'ghcr.io' }}\n+           username: ${{ github.actor }}\n+           password: ${{ secrets.GITHUB_TOKEN }}\n+\n+       - name: Restore Trivy DB cache\n+         uses: actions/cache/restore@5a3ec84eff668545956fd18022155c47e93e2684  # v4.2.3\n+         with:\n+           path: ${{ runner.temp }}/trivy-db\n+           key: ${{ runner.os }}-trivy-db-v1-${{ github.run_id }}\n+           restore-keys: |\n+             ${{ runner.os }}-trivy-db-v1-\n+\n+       - name: Build Docker image\n+         run: |\n+           docker build --load \\\n+             -t \"${{ vars.IMAGE_NAME || 'app' }}:ci-${{ github.sha }}\" \\\n+             .\n+\n+       - name: Export image to tar\n+         run: docker save \"${{ vars.IMAGE_NAME || 'app' }}:ci-${{ github.sha }}\" -o \"${{ runner.temp }}/image.tar\"\n+\n+       - name: Trivy scan — block on HIGH/CRITICAL (security invariant before push)\n+         id: trivy_scan\n+         uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0\n+         with:\n+           input: ${{ runner.temp }}/image.tar\n+           format: table\n+           exit-code: '1'\n+           severity: 'HIGH,CRITICAL'\n+           trivyignores: .trivyignore\n+           cache-dir: ${{ runner.temp }}/trivy-db\n+\n+       - name: Trivy full report (informational)\n+         if: always() && steps.trivy_scan.conclusion != 'skipped'\n+         uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0\n+         with:\n+           input: ${{ runner.temp }}/image.tar\n+           format: table\n+           exit-code: '0'\n+           severity: 'CRITICAL,HIGH,MEDIUM,LOW'\n+           trivyignores: .trivyignore\n+           cache-dir: ${{ runner.temp }}/trivy-db\n+           output: trivy-report.txt\n+\n+       - name: Save Trivy DB cache\n+         if: always()\n+         uses: actions/cache/save@5a3ec84eff668545956fd18022155c47e93e2684  # v4.2.3\n+         with:\n+           path: ${{ runner.temp }}/trivy-db\n+           key: ${{ runner.os }}-trivy-db-v1-${{ github.run_id }}\n+\n+       - name: Tag image for registry (idempotent retry tag H2-M3)\n+         id: tag\n+         env:\n+           RUN_ATTEMPT: ${{ github.run_attempt }}\n+           REGISTRY: ${{ vars.CONTAINER_REGISTRY || 'ghcr.io' }}\n+           IMAGE_NAME: ${{ vars.IMAGE_NAME || 'app' }}\n+         run: |\n+           IMAGE_TAG=\"${REGISTRY}/${IMAGE_NAME}:${{ github.sha }}\"\n+           IMAGE_TAG_RETRY=\"${REGISTRY}/${IMAGE_NAME}:${{ github.sha }}-${RUN_ATTEMPT}\"\n+           docker tag \"${{ vars.IMAGE_NAME || 'app' }}:ci-${{ github.sha }}\" \"$IMAGE_TAG\"\n+           docker tag \"${{ vars.IMAGE_NAME || 'app' }}:ci-${{ github.sha }}\" \"$IMAGE_TAG_RETRY\"\n+           echo \"image_tag=$IMAGE_TAG\" >> \"$GITHUB_OUTPUT\"\n+\n+       - name: Push to container registry\n+         id: push\n+         env:\n+           REGISTRY: ${{ vars.CONTAINER_REGISTRY || 'ghcr.io' }}\n+           IMAGE_NAME: ${{ vars.IMAGE_NAME || 'app' }}\n+         run: |\n+           docker push \"${REGISTRY}/${IMAGE_NAME}:${{ github.sha }}\"\n+           docker push \"${REGISTRY}/${IMAGE_NAME}:${{ github.sha }}-${{ github.run_attempt }}\"\n+           DIGEST=$(docker inspect --format='{{index .RepoDigests 0}}' \\\n+             \"${REGISTRY}/${IMAGE_NAME}:${{ github.sha }}\" 2>/dev/null | cut -d@ -f2 || echo \"\")\n+           echo \"digest=$DIGEST\" >> \"$GITHUB_OUTPUT\"\n+\n+       - name: Install cosign (keyless OIDC)\n+         uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6  # v4.1.2\n+\n+       - name: Sigstore preflight check (H3-S1)\n+         run: |\n+           STATUS=$(curl -sf https://status.sigstore.dev/api/v2/status.json \\\n+             --max-time 10 | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d.get('status',{}).get('indicator','unknown'))\" 2>/dev/null || echo \"unknown\")\n+           if [[ \"$STATUS\" != \"none\" ]]; then\n+             echo \"::warning::Sigstore status is '${STATUS}' — cosign sign may fail\"\n+           fi\n+           echo \"sigstore-status=$STATUS\"\n+\n+\n+       - name: cosign sign image (OCI manifest, H3-M2)\n+         env:\n+           IMAGE_DIGEST: ${{ steps.push.outputs.digest }}\n+           REGISTRY: ${{ vars.CONTAINER_REGISTRY || 'ghcr.io' }}\n+           IMAGE_NAME: ${{ vars.IMAGE_NAME || 'app' }}\n+         run: |\n+           [[ -z \"$IMAGE_DIGEST\" ]] && { echo \"::warning::No digest — skip cosign sign\"; exit 0; }\n+           cosign sign --yes \"${REGISTRY}/${IMAGE_NAME}@${IMAGE_DIGEST}\"\n+\n+\n+       - name: Generate SBOM (CycloneDX)\n+         uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610  # v0.24.0\n+         with:\n+           image: ${{ steps.tag.outputs.image_tag }}\n+           format: cyclonedx-json\n+           output-file: bom.json\n+\n+\n+       - name: cosign attest SBOM (CycloneDX, H3-M2)\n+         env:\n+           IMAGE_DIGEST: ${{ steps.push.outputs.digest }}\n+           REGISTRY: ${{ vars.CONTAINER_REGISTRY || 'ghcr.io' }}\n+           IMAGE_NAME: ${{ vars.IMAGE_NAME || 'app' }}\n+         run: |\n+           [[ -z \"$IMAGE_DIGEST\" ]] && { echo \"::warning::No digest — skip cosign attest\"; exit 0; }\n+           BOM_FILE=$([ -f \"target/bom.json\" ] && echo \"target/bom.json\" || echo \"bom.json\")\n+           cosign attest --yes \\\n+             --predicate \"$BOM_FILE\" \\\n+             --type cyclonedx \\\n+             \"${REGISTRY}/${IMAGE_NAME}@${IMAGE_DIGEST}\"\n+\n+       - name: Upload SBOM artifact (14d retention)\n+         # INTENTIONAL-NON-BLOCKING: artifact upload failures must not block CI\n+         continue-on-error: true\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: sbom-develop-${{ github.sha }}\n+           path: |\n+             target/bom.json\n+             bom.json\n+           if-no-files-found: warn\n+           retention-days: 14\n+\n+       - name: Upload Trivy report\n+         if: always() && steps.trivy_scan.conclusion != 'skipped'\n+         # INTENTIONAL-NON-BLOCKING: artifact upload failures must not block CI\n+         continue-on-error: true\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: trivy-report-develop-${{ github.sha }}\n+           path: trivy-report.txt\n+           if-no-files-found: ignore\n+           retention-days: 7\n+\n+       - name: Cleanup Docker image and tar\n+         if: always()\n+         run: |\n+           docker rmi \"${{ vars.IMAGE_NAME || 'app' }}:ci-${{ github.sha }}\" 2>/dev/null || true\n+           rm -f \"${{ runner.temp }}/image.tar\"\n+\n+   # === GROUP: deploy ===\n+\n+   deploy-test:\n+     name: Deploy to TEST environment\n+     needs: [build-and-sign]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_DEPLOY || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 20\n+     steps:\n+       - name: Checkout\n+         uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+\n+\n+\n+\n+   # === GROUP: smoke ===\n+\n+   post-deploy-smoke:\n+     name: Post-Deploy Smoke (k6 LT1)\n+     needs: [deploy-test]\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_BUILD || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 20\n+     steps:\n+       - name: Checkout\n+         uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+\n+       - name: k6 smoke (LT1 — post-deploy gate)\n+         env:\n+           BASE_URL: ${{ secrets.TEST_BASE_URL }}\n+           AUTH_TOKEN: ${{ secrets.K6_TEST_AUTH_TOKEN }}\n+           # Sanctioned opt-out: set repo/org variable SKIP_SMOKE_K6=true to deliberately skip this\n+           # gate. With it unset, an empty TEST_BASE_URL secret FAILS loud — never a silent green.\n+           SKIP_SMOKE_K6: ${{ vars.SKIP_SMOKE_K6 }}\n+           VUS: \"1\"\n+           DURATION: \"30s\"\n+         run: |\n+           test -n \"$BASE_URL\" || { [ \"${SKIP_SMOKE_K6}\" = \"true\" ] \\\n+             && { echo \"::notice::SKIP_SMOKE_K6=true — k6 smoke explicitly skipped\"; exit 0; } \\\n+             || { echo \"::error::TEST_BASE_URL secret is empty and SKIP_SMOKE_K6 is not 'true'\"; exit 1; }; }\n+           k6 run \\\n+             --env BASE_URL=\"$BASE_URL\" \\\n+             --env AUTH_TOKEN=\"$AUTH_TOKEN\" \\\n+             --env VUS=\"$VUS\" \\\n+             --env DURATION=\"$DURATION\" \\\n+             tests/load/smoke.js\n+\n+       - name: Upload k6 summary\n+         if: always()\n+         # INTENTIONAL-NON-BLOCKING: artifact upload failures must not block CI\n+         continue-on-error: true\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1\n+         with:\n+           name: k6-smoke-post-deploy-${{ github.sha }}\n+           path: k6-smoke-post-deploy.json\n+           if-no-files-found: ignore\n+           retention-days: 7\n+\n+   # === GROUP: dast ===\n+\n+   # E4 (#1502): DAST baseline moved off the PR path (02-pr-extended) to here, where\n+   # it runs against the ephemeral TEST environment that deploy-test just provisioned —\n+   # a live URL actually exists at this cadence. Skips gracefully (like post-deploy-smoke)\n+   # when TEST_BASE_URL is unset, so a non-web deploy never fails on a missing target.\n+   # The deep counterpart (full active scan) lives at nightly cadence (06-nightly dast-full).\n+   dast-baseline:\n+     name: DAST Baseline (ZAP — ephemeral TEST env)\n+     needs: [deploy-test]\n+     # #1756: pinned to a LITERAL GitHub-hosted runner, not the usual\n+     # `vars.RUNNER_LABELS_BUILD` self-hosted-capable fallback. zaproxy/action-baseline\n+     # is a Docker-outside-of-Docker action (it internally runs `docker run` against the\n+     # ZAP image) — on a self-hosted runner whose own process runs inside a container,\n+     # the workspace bind-mount resolves against the DOCKER HOST path, not the runner's\n+     # own checkout. GitHub-hosted runners are not containerized this way and are\n+     # unaffected. Enforced by scripts/check-docker-action-runner-safety.mjs.\n+     runs-on: ubuntu-latest\n+     timeout-minutes: 30\n+     permissions:\n+       contents: read\n+     steps:\n+       - name: Checkout\n+         uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1\n+\n+       - name: Resolve DAST target\n+         id: target\n+         env:\n+           BASE_URL: ${{ secrets.TEST_BASE_URL }}\n+           # Sanctioned opt-out: set repo/org variable SKIP_DAST_BASELINE=true to skip.\n+           # With it unset, an empty TEST_BASE_URL FAILS loud — never a silent green.\n+           SKIP_DAST_BASELINE: ${{ vars.SKIP_DAST_BASELINE }}\n+         run: |\n+           if [ -z \"$BASE_URL\" ]; then\n+             if [ \"${SKIP_DAST_BASELINE}\" = \"true\" ]; then\n+               echo \"::notice::SKIP_DAST_BASELINE=true — DAST baseline explicitly skipped\"\n+               echo \"run=false\" >> \"$GITHUB_OUTPUT\"\n+             else\n+               echo \"::error::TEST_BASE_URL secret is empty and SKIP_DAST_BASELINE is not 'true'\"\n+               exit 1\n+             fi\n+           else\n+             echo \"run=true\" >> \"$GITHUB_OUTPUT\"\n+           fi\n+\n+       - name: OWASP ZAP baseline scan (ephemeral TEST env)\n+         if: steps.target.outputs.run == 'true'\n+         uses: zaproxy/action-baseline@66042c8e7e24680119199a017e5b0e8603bf4dae  # v0.12.0\n+         with:\n+           target: ${{ secrets.TEST_BASE_URL }}\n+           fail_action: true\n+           rules_file_name: '.zap/rules.tsv'\n+           # #1887-E: authenticated-scan context — baseline-auth.context is emitted by\n+           # generateSecurity (security.ts) for the user to fill in login URL/credentials.\n+           context_file: '.zap/baseline-auth.context'\n+           cmd_options: '-J zap-report.json'\n+           allow_issue_writing: false\n+\n+   # === GROUP: notify ===\n+\n+   notify:\n+     name: Notify on Failure\n+     needs: [build-and-sign, deploy-test, post-deploy-smoke, dast-baseline]\n+     if: failure()\n+     uses: ./.github/workflows/_notify.yml\n+     with:\n+       workflow_name: \"Deploy to TEST\"\n+       area: deploy\n+       severity: P1\n+       branch: ${{ github.ref_name }}\n+       run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\n+       failure_summary: \"04-deploy-test.yml failed on develop branch.\"\n+       status: failure\n+     permissions:\n+       contents: read\n+       issues: write\n+     secrets: inherit\n+\n\n ❯ expectBareNodeSetup __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts:17:18\n     15|\n     16| function expectBareNodeSetup(output: string): void {\n     17|   expect(output).toContain(\"node-version: '22'\")\n       |                  ^\n     18|   expect(output).toContain('package-manager-cache: false')\n     19|   expect(output).not.toContain('node-version-file: .nvmrc')\n ❯ __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts:40:5\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/8]⎯\n\n FAIL  __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts > non-TypeScript workflow Node tooling (#2259) > github/workflows/_contract-postman.yml.ejs has a Node setup path independent of a root lockfile\nAssertionError: expected 'name: Contract Tests — Postman/Newman…' to contain 'node-version: \\'22\\''\n\n- Expected\n+ Received\n\n- node-version: '22'\n+ name: Contract Tests — Postman/Newman\n+\n+ permissions:\n+   contents: read\n+   checks: write\n+\n+ on:\n+   pull_request:\n+     branches: [main]\n+     paths:\n+       - 'src/**'\n+       - 'postman/**'\n+       - 'pacts/**'\n+       - 'scripts/run-postman-tests.sh'\n+   workflow_dispatch:\n+     inputs:\n+       base_url:\n+         description: 'Service base URL to test against'\n+         required: false\n+         default: 'http://localhost:8080'\n+\n+ # Cancel in-progress contract runs on the same branch — re-runs on each push.\n+ concurrency:\n+   group: contract-postman-${{ github.head_ref || github.ref_name }}\n+   cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}\n+\n+ jobs:\n+   contract-postman:\n+     name: Postman/Newman Contract Tests (INV-35)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+\n+\n+     services:\n+       app:\n+         image: ${{ vars.APP_IMAGE || format('ghcr.io/{0}:pr-{1}', github.repository, github.event.pull_request.number) }}\n+         ports:\n+           - 8080:8080\n+         env:\n+           SPRING_PROFILES_ACTIVE: contract-test\n+\n+\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+         with:\n+           fetch-depth: 1\n+\n+       - name: Setup Node.js (for Newman)\n+         uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0\n+         with:\n+           node-version-file: '.nvmrc'\n+           cache: 'npm'\n+\n+       - name: Install Newman\n+         run: npm install -g newman newman-reporter-junitfull\n+\n+       - name: Prepare results directory\n+         run: mkdir -p .arbiter/evidence/contract\n+\n+       - name: Inject Pact samples (optional)\n+         env:\n+           SERVICE_BASE_URL: ${{ github.event.inputs.base_url || 'http://localhost:8080' }}\n+           PACTS_DIR: pacts\n+         run: |\n+           if [[ -d pacts ]] && ls pacts/*.json &>/dev/null; then\n+             bash scripts/inject-pact-samples.sh\n+           else\n+             echo \"No pacts directory — skipping sample injection.\"\n+           fi\n+\n+       - name: Run Newman contract tests\n+         env:\n+           SERVICE_BASE_URL: ${{ github.event.inputs.base_url || 'http://localhost:8080' }}\n+           POSTMAN_COLLECTION: ${{ vars.POSTMAN_COLLECTION || 'postman/test-project.postman_collection.json' }}\n+           RESULTS_DIR: .arbiter/evidence/contract\n+           NEWMAN_TIMEOUT_MS: '30000'\n+           NEWMAN_MAX_RETRIES: '1'\n+         run: bash scripts/run-postman-tests.sh\n+\n+       - name: Upload test results\n+         if: always()\n+         uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4\n+         with:\n+           name: newman-results-${{ github.run_id }}\n+           path: .arbiter/evidence/contract/newman-results.xml\n+           if-no-files-found: warn\n+\n+       - name: Publish JUnit results\n+         if: always()\n+         uses: mikepenz/action-junit-report@3585f732c9e1f2c1b7a7c99b27ddd7b85082c04d # v5.2.0\n+         with:\n+           report_paths: '.arbiter/evidence/contract/newman-results.xml'\n+           check_name: 'Postman/Newman Contract Tests'\n+           fail_on_failure: true\n+\n\n ❯ expectBareNodeSetup __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts:17:18\n     15|\n     16| function expectBareNodeSetup(output: string): void {\n     17|   expect(output).toContain(\"node-version: '22'\")\n       |                  ^\n     18|   expect(output).toContain('package-manager-cache: false')\n     19|   expect(output).not.toContain('node-version-file: .nvmrc')\n ❯ __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts:40:5\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/8]⎯\n\n FAIL  __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts > non-TypeScript workflow Node tooling (#2259) > github/workflows/_monthly.yml.ejs has a Node setup path independent of a root lockfile\nAssertionError: expected 'name: Monthly jobs (reusable)\\n\\non:\\…' to contain 'node-version: \\'22\\''\n\n- Expected\n+ Received\n\n- node-version: '22'\n+ name: Monthly jobs (reusable)\n+\n+ on:\n+   workflow_call:\n+\n+ permissions:\n+   contents: read\n+   issues: write\n+\n+ jobs:\n+   dep-age-audit:\n+     name: Dependency age audit (>180 days)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+         with:\n+           fetch-depth: 0\n+\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0\n+         with:\n+           python-version-file: .python-version\n+       - run: pip install pip-outdated\n+       - name: pip outdated report\n+         run: pip list --outdated --format=json > dep-age-report.json || true\n+\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1\n+         with:\n+           name: dep-age-report-${{ github.run_id }}\n+           path: dep-age-report.*\n+           retention-days: 365\n+           if-no-files-found: warn\n+\n+   license-full-audit:\n+     name: Full license compliance scan\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0\n+         with:\n+           python-version-file: .python-version\n+       - run: pip install pip-licenses\n+       - name: pip-licenses full report\n+         run: pip-licenses --format=json --output-file=license-report.json\n+\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1\n+         with:\n+           name: license-report-${{ github.run_id }}\n+           path: license-report.*\n+           retention-days: 365\n+           if-no-files-found: warn\n+\n+   action-pins-stale-audit:\n+     name: GitHub Actions SHA pin staleness (>180d)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     permissions:\n+       contents: read\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+       - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0\n+         with:\n+           node-version-file: .nvmrc\n+           cache: npm\n+       - name: Audit action pin ages\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+         run: |\n+           STALE=0\n+           UNREACHABLE=0\n+           THRESHOLD_DAYS=180\n+           NOW_EPOCH=$(date +%s)\n+           while IFS= read -r line; do\n+             REPO=$(echo \"$line\" | sed -E 's/.*uses: ([^@]+)@[a-f0-9]{40}.*/\\1/')\n+             SHA=$(echo \"$line\" | sed -E 's/.*@([a-f0-9]{40}).*/\\1/')\n+             if [[ \"$REPO\" == \"$line\" || \"$SHA\" == \"$line\" ]]; then continue; fi\n+             COMMIT_DATE=$(gh api \"repos/$REPO/commits/$SHA\" --jq '.commit.committer.date' 2>/dev/null || echo \"\")\n+             if [ -z \"$COMMIT_DATE\" ]; then\n+               echo \"::warning::$REPO@$SHA is unreachable (deleted/private/renamed?)\"\n+               UNREACHABLE=$((UNREACHABLE + 1))\n+               continue\n+             fi\n+             COMMIT_EPOCH=$(date -d \"$COMMIT_DATE\" +%s 2>/dev/null || echo 0)\n+             AGE_DAYS=$(( (NOW_EPOCH - COMMIT_EPOCH) / 86400 ))\n+             if [ \"$AGE_DAYS\" -gt \"$THRESHOLD_DAYS\" ]; then\n+               echo \"::warning::$REPO@$SHA is ${AGE_DAYS}d old (>${THRESHOLD_DAYS}d)\"\n+               STALE=$((STALE + 1))\n+             fi\n+           done < <(grep -rEh 'uses: [^@]+@[a-f0-9]{40}' .github/workflows/ 2>/dev/null | sort -u)\n+           echo \"Stale action pins: $STALE\"\n+           echo \"Unreachable action pins: $UNREACHABLE\"\n+           echo \"stale_count=$STALE\" >> \"$GITHUB_OUTPUT\"\n+           echo \"unreachable_count=$UNREACHABLE\" >> \"$GITHUB_OUTPUT\"\n+\n+   sbom-archive:\n+     name: SBOM full snapshot\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0\n+         with:\n+           python-version-file: .python-version\n+       - run: pip install cyclonedx-bom\n+       - name: cyclonedx-py SBOM\n+         run: cyclonedx-py environment -o sbom.cdx.json || true\n+\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1\n+         with:\n+           name: sbom-snapshot-${{ github.run_id }}\n+           path: sbom.cdx.json\n+           retention-days: 365\n+           if-no-files-found: warn\n+\n+   doc-freshness-audit:\n+     name: Per-doc freshness gate (T4, gold-doc-tranches-t3-t5.md §2, #H4)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 30\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+         with:\n+           # Change-coupling (§2.2 point 3) needs full history — a depth-1 checkout's\n+           # `git log -- path` lies, so the script itself detects and skips coupling on a\n+           # shallow clone; this checkout is what keeps that signal live in CI.\n+           fetch-depth: 0\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+       - name: Per-doc freshness audit (HARD — binary, no advisory mode inside the script)\n+         id: freshness\n+         run: |\n+           node scripts/check-doc-freshness.mjs --json > doc-freshness-report.json || echo \"stale=true\" >> \"$GITHUB_OUTPUT\"\n+           cat doc-freshness-report.json\n+\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1\n+         if: always()\n+         with:\n+           name: doc-freshness-report-${{ github.run_id }}\n+           path: doc-freshness-report.json\n+           retention-days: 365\n+           if-no-files-found: warn\n+\n+       - name: Fail the job on any stale doc\n+         if: steps.freshness.outputs.stale == 'true'\n+         run: exit 1\n+\n+   evidence-collect:\n+     name: Upload monthly evidence summary\n+     needs:\n+       - dep-age-audit\n+       - license-full-audit\n+       - action-pins-stale-audit\n+       - sbom-archive\n+       - doc-freshness-audit\n+     if: always()\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+       - name: Collect evidence summary\n+         run: |\n+           mkdir -p monthly-evidence\n+           echo \"Monthly run: $(date -u +%Y-%m-%dT%H:%M:%SZ)\" > monthly-evidence/summary.txt\n+           echo \"Ref: ${{ github.sha }}\" >> monthly-evidence/summary.txt\n+           echo \"Run ID: ${{ github.run_id }}\" >> monthly-evidence/summary.txt\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1\n+         with:\n+           name: monthly-evidence-${{ github.run_id }}\n+           path: monthly-evidence/\n+           retention-days: 365\n+\n+   monthly-required:\n+     name: Monthly gate\n+     needs:\n+       - dep-age-audit\n+       - license-full-audit\n+       - action-pins-stale-audit\n+       - sbom-archive\n+       - doc-freshness-audit\n+     if: always()\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - name: Assert all jobs succeeded\n+         run: |\n+           declare -A RESULTS=(\n+             [dep-age-audit]=\"${{ needs.dep-age-audit.result }}\"\n+             [license-full-audit]=\"${{ needs.license-full-audit.result }}\"\n+             [action-pins-stale-audit]=\"${{ needs.action-pins-stale-audit.result }}\"\n+             [sbom-archive]=\"${{ needs.sbom-archive.result }}\"\n+             [doc-freshness-audit]=\"${{ needs.doc-freshness-audit.result }}\"\n+           )\n+           FAILED=0\n+           for job in \"${!RESULTS[@]}\"; do\n+             r=\"${RESULTS[$job]}\"\n+             # Treat anything other than \"success\" as failure (cancelled, timed_out,\n+             # skipped, failure, or empty from typos in needs:).\n+             if [ \"$r\" != \"success\" ]; then\n+               echo \"::error::$job result=$r (expected: success)\"\n+               FAILED=1\n+             fi\n+           done\n+           if [ \"$FAILED\" = \"1\" ]; then\n+             echo \"::error::One or more monthly jobs did not succeed.\"\n+             exit 1\n+           fi\n+           echo \"Monthly gate passed.\"\n+\n+       - name: File issue on failure\n+         if: failure()\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+           GH_REPO: ${{ github.repository }}\n+         run: |\n+           EXISTING=$(gh issue list \\\n+             --label monthly-regression \\\n+             --state open \\\n+             --limit 1 \\\n+             --json number \\\n+             --repo \"$GH_REPO\" \\\n+             --jq '.[0].number // empty')\n+           if [ -z \"$EXISTING\" ]; then\n+             gh issue create \\\n+               --title \"[monthly] regression detected (${{ github.run_id }})\" \\\n+               --body \"Monthly workflow failed. Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\" \\\n+               --label monthly-regression \\\n+               --repo \"$GH_REPO\"\n+           fi\n+\n\n ❯ expectBareNodeSetup __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts:17:18\n     15|\n     16| function expectBareNodeSetup(output: string): void {\n     17|   expect(output).toContain(\"node-version: '22'\")\n       |                  ^\n     18|   expect(output).toContain('package-manager-cache: false')\n     19|   expect(output).not.toContain('node-version-file: .nvmrc')\n ❯ __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts:40:5\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/8]⎯\n\n FAIL  __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts > non-TypeScript workflow Node tooling (#2259) > github/workflows/_nightly.yml.ejs has a Node setup path independent of a root lockfile\nAssertionError: expected 'name: Nightly jobs (reusable)\\n\\n\\non…' to contain 'node-version: \\'22\\''\n\n- Expected\n+ Received\n\n- node-version: '22'\n+ name: Nightly jobs (reusable)\n+\n+\n+ on:\n+   workflow_call:\n+\n+ permissions:\n+   contents: read\n+   issues: write\n+   actions: write\n+\n+ jobs:\n+   shared-security:\n+     uses: ./.github/workflows/_shared-security.yml\n+\n+\n+   semgrep-sast:\n+     name: Semgrep SAST (2nd SAST — covers Rust + trunk-solo)\n+     # PORT A2 (#1502): Semgrep is a second SAST alongside CodeQL (15-codeql). It\n+     # covers the two cases CodeQL misses: Rust (unsupported by CodeQL) and any\n+     # config that never emits CodeQL. Semgrep is language-agnostic — it scans the\n+     # source tree directly, so no per-language toolchain branch is needed. Run via\n+     # the pinned CLI rather than an unverified third-party action (INV-76 / the\n+     # fabricated-pin lesson). Ruleset configurable via the SEMGREP_RULES variable.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 30\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0\n+         with:\n+           python-version: '3.12'\n+       - name: Install Semgrep (pinned CLI)\n+         run: pip install semgrep\n+       - name: Semgrep SAST (configurable ruleset)\n+         env:\n+           SEMGREP_RULES: ${{ vars.SEMGREP_RULES || 'auto' }}\n+           # Informational by default; set repo/org var SEMGREP_STRICT=true to block.\n+           SEMGREP_STRICT: ${{ vars.SEMGREP_STRICT }}\n+         run: |\n+           set -uo pipefail\n+           ARGS=\"--config ${SEMGREP_RULES} --sarif --output semgrep.sarif --metrics=off\"\n+           if [ \"${SEMGREP_STRICT:-}\" = \"true\" ]; then\n+             semgrep scan ${ARGS} --error\n+           else\n+             semgrep scan ${ARGS} || echo \"::warning::Semgrep reported findings (informational; set SEMGREP_STRICT=true to block)\"\n+           fi\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1\n+         if: always()\n+         with:\n+           name: semgrep-sarif\n+           path: semgrep.sarif\n+           retention-days: 30\n+\n+   fuzz:\n+     name: Fuzz testing\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 30\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0\n+         with:\n+           python-version-file: .python-version\n+       - run: pip install atheris\n+       - name: atheris fuzz (30 min budget)\n+         run: |\n+           FUZZ_SCRIPTS=$(find . -name 'fuzz_*.py' | head -5)\n+           if [ -n \"$FUZZ_SCRIPTS\" ]; then\n+             for s in $FUZZ_SCRIPTS; do\n+               timeout 360 python \"$s\" -atheris_runs=100000 || true\n+             done\n+           fi\n+\n+\n+   soak-e2e:\n+     name: Soak / E2E full suite\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0\n+         with:\n+           python-version-file: .python-version\n+       - run: pip install -e \".[dev]\"\n+       - name: pytest soak (60 min budget)\n+         run: pytest -m 'e2e or soak' --timeout=3600 || true\n+\n+\n+\n+\n+\n+   gitleaks-history:\n+     name: Secret scan (full history)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       # gitleaks-action downloads to a HARDCODED /tmp/gitleaks.tmp. On a self-hosted\n+       # runner /tmp survives between jobs, so a leftover from an interrupted download\n+       # fails every later install after both internal retries (\"Destination file path\n+       # /tmp/gitleaks.tmp already exists\" then \"parameter 'file' is required\") — the\n+       # cause of #2100.\n+       # ponytail: unconditional delete — one gitleaks job per workflow run, so nothing\n+       # else is mid-download. If gitleaks ever runs in parallel jobs on one runner,\n+       # switch to the curl -> $RUNNER_TEMP/bin install shape used in 01-pr-fast.\n+       - name: Clear stale gitleaks download temp\n+         run: rm -f /tmp/gitleaks.tmp\n+       - name: Gitleaks full history scan\n+         uses: gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e  # v3.0.0\n+         env:\n+           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}\n+           GITLEAKS_ENABLE_UPLOAD_ARTIFACT: \"false\"\n+         with:\n+           args: detect --source . --log-opts=\"--all --full-history\"\n+\n+\n+   gate-full-nightly:\n+     name: Full Gate nightly (L2+, INV-59)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 90\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+         with:\n+           fetch-depth: 0\n+       - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0\n+         with:\n+           node-version-file: '.nvmrc'\n+           cache: 'npm'\n+       - run: npm ci\n+       - name: Install gitleaks\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" gitleaks\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Install actionlint\n+         run: |\n+           mkdir -p \"$RUNNER_TEMP/bin\"\n+           curl -sSfL https://github.com/rhysd/actionlint/releases/download/v1.7.3/actionlint_1.7.3_linux_amd64.tar.gz | tar -xz -C \"$RUNNER_TEMP/bin\" actionlint\n+           echo \"$RUNNER_TEMP/bin\" >> \"$GITHUB_PATH\"\n+       - name: Build\n+         run: npm run build\n+       - name: Run full L2 gate (== pre-push, nightly)\n+         run: node scripts/check-all.mjs L2 --json gate-result-nightly.json\n+       - name: Upload nightly gate-result artifact\n+         if: always()\n+         # Supplementary artifact — never let an Artifacts-quota hiccup fail the\n+         # job whose actual signal (the L2 gate above) already passed (#2058-class).\n+         continue-on-error: true\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1\n+         with:\n+           name: gate-result-nightly\n+           path: gate-result-nightly.json\n+           retention-days: 30\n+\n+\n+\n+\n+   coverage-report:\n+     name: Coverage report + threshold (full suite)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+         with:\n+           # The full suite includes TDD-evidence checks that resolve an older\n+           # commit; reachability needs history, not a shallow clone (#2100).\n+           fetch-depth: 0\n+\n+       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0\n+         with:\n+           python-version-file: .python-version\n+       - run: pip install -e \".[dev]\"\n+       - run: pytest --cov --cov-fail-under=80 --cov-report=html:coverage/html --cov-report=xml:coverage/coverage.xml\n+\n+       - name: Upload coverage reports\n+         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1\n+         if: always()\n+         # Supplementary artifact — never let an Artifacts-quota hiccup fail the\n+         # job whose actual signal (the coverage run above) already passed (#2058-class).\n+         continue-on-error: true\n+         with:\n+           name: coverage-${{ github.run_id }}\n+           path: coverage/\n+           retention-days: 30\n+\n+\n+   sonar-deep-scan:\n+     name: SonarCloud deep scan (full branch analysis)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       # #1311: self-hosted persistent workspaces can keep a stale\n+       # .git/refs/**/*.lock from an interrupted run, which makes the\n+       # deep checkout below fail with \"cannot lock ref\". No-op on\n+       # ephemeral runners.\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+         with:\n+           fetch-depth: 0\n+\n+       # E1 (#1502): the full deep scan moved off the PR path (which now does only\n+       # incremental PR decoration). Best-effort — gated on SONAR_TOKEN being set.\n+       - name: SonarCloud deep scan\n+         if: env.SONAR_TOKEN != ''\n+         env:\n+           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}\n+         run: npx sonar-scanner -Dsonar.token=\"$SONAR_TOKEN\"\n+\n+   evidence-collect:\n+     name: Upload nightly evidence artifacts\n+     needs:\n+       - shared-security\n+       - fuzz\n+       - soak-e2e\n+       - gitleaks-history\n+       - coverage-report\n+\n+       - sonar-deep-scan\n+\n+       - gate-full-nightly\n+\n+\n+\n+     if: always()\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+\n+       - name: Collect evidence summary\n+         run: |\n+           mkdir -p nightly-evidence\n+           echo \"Nightly run: $(date -u +%Y-%m-%dT%H:%M:%SZ)\" > nightly-evidence/summary.txt\n+           echo \"Ref: ${{ github.sha }}\" >> nightly-evidence/summary.txt\n+       - name: Scrub repo evidence before it leaves the repo (#2018)\n+         # The artifact used to carry a timestamp and nothing else: the evidence\n+         # the repo actually holds (.arbiter/evidence/**) never travelled with it.\n+         # publish-evidence-snapshot strips tokens/keys/JWTs/PII and FAILS CLOSED\n+         # if any pattern survives its re-scan, so a leak blocks the upload instead\n+         # of shipping inside it. No-op where that scrubber is not present.\n+         run: |\n+           if [ ! -d .arbiter/evidence ]; then\n+             echo \"no evidence to publish (no .arbiter/evidence)\"\n+           elif [ -f scripts/publish-evidence-snapshot.mjs ]; then\n+             node scripts/publish-evidence-snapshot.mjs \\\n+               --input .arbiter/evidence \\\n+               --output nightly-evidence/evidence\n+           elif node --input-type=module -e '\n+             import { existsSync, readFileSync } from \"node:fs\";\n+             const path = \"scripts/publish-evidence-snapshot.mjs\";\n+             const manifestPath = \".arbiter-generated-manifest.json\";\n+             if (!existsSync(manifestPath)) process.exit(0);\n+             let manifest;\n+             try { manifest = JSON.parse(readFileSync(manifestPath, \"utf8\")); }\n+             catch (err) { console.error(`invalid ${manifestPath}: ${err.message}`); process.exit(2); }\n+             if (manifest?.$schemaVersion !== 1 || !manifest.files || typeof manifest.files !== \"object\") {\n+               console.error(`invalid ${manifestPath} shape`); process.exit(2);\n+             }\n+             process.exit(Object.hasOwn(manifest.files, path) ? 1 : 0);\n+           '; then\n+             echo \"no evidence scrubber emitted for this project\"\n+           else\n+             echo \"ERROR: scripts/publish-evidence-snapshot.mjs was emitted by arbiter but is now missing\"\n+             exit 1\n+           fi\n+       - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1\n+         # Supplementary artifact — never let an Artifacts-quota hiccup fail this\n+         # best-effort evidence job (#2058-class).\n+         continue-on-error: true\n+         with:\n+           name: nightly-evidence-${{ github.run_id }}\n+           path: nightly-evidence/\n+           retention-days: 90\n+\n+   cleanup-expired-artifacts:\n+     name: Cleanup expired Actions artifacts\n+     # GitHub's own artifact-expiry sweep does not always reliably reclaim\n+     # storage — an artifact can sit resident (and billed) well past its\n+     # retention-days cutoff. Best-effort safety net, never blocking:\n+     # standalone (no `needs`), not part of `nightly-required`'s aggregation —\n+     # a failure here must never red the actual nightly gate.\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 15\n+     permissions:\n+       contents: read\n+       actions: write\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+       - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0\n+         with:\n+           node-version-file: '.nvmrc'\n+       - name: Delete already-expired artifacts\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+         run: node scripts/gh-cleanup-expired-artifacts.mjs\n+\n+   nightly-required:\n+     name: Nightly gate\n+     needs:\n+       - shared-security\n+       - fuzz\n+       - soak-e2e\n+       - gitleaks-history\n+       - coverage-report\n+\n+\n+\n+\n+     if: always()\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - name: Assert no hard failures\n+         run: |\n+           RESULTS=(\n+             \"${{ needs.shared-security.result }}\"\n+             \"${{ needs.fuzz.result }}\"\n+             \"${{ needs.soak-e2e.result }}\"\n+             \"${{ needs.gitleaks-history.result }}\"\n+             \"${{ needs.coverage-report.result }}\"\n+\n+\n+\n+           )\n+           FAILED=0\n+           for r in \"${RESULTS[@]}\"; do\n+             if [ \"$r\" = \"failure\" ]; then\n+               FAILED=1\n+             fi\n+           done\n+           if [ \"$FAILED\" = \"1\" ]; then\n+             echo \"::error::One or more nightly hard-failure jobs failed.\"\n+             exit 1\n+           fi\n+           echo \"Nightly gate passed.\"\n+\n+       - name: File issue on failure\n+         if: failure()\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+           GH_REPO: ${{ github.repository }}\n+         run: |\n+           EXISTING=$(gh issue list \\\n+             --label nightly-regression \\\n+             --state open \\\n+             --limit 1 \\\n+             --json number \\\n+             --repo \"$GH_REPO\" \\\n+             --jq '.[0].number // empty')\n+           if [ -z \"$EXISTING\" ]; then\n+             gh issue create \\\n+               --title \"[nightly] regression detected (${{ github.run_id }})\" \\\n+               --body \"Nightly workflow failed. Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\" \\\n+               --label nightly-regression \\\n+               --repo \"$GH_REPO\"\n+           fi\n+\n\n ❯ expectBareNodeSetup __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts:17:18\n     15|\n     16| function expectBareNodeSetup(output: string): void {\n     17|   expect(output).toContain(\"node-version: '22'\")\n       |                  ^\n     18|   expect(output).toContain('package-manager-cache: false')\n     19|   expect(output).not.toContain('node-version-file: .nvmrc')\n ❯ __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts:40:5\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/8]⎯\n\n FAIL  __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts > non-TypeScript workflow Node tooling (#2259) > github/workflows/16-frontend-quality.yml.ejs has a Node setup path independent of a root lockfile\nAssertionError: expected 'name: Frontend Quality (T1)\\n\\non:\\n …' to contain 'node-version: \\'22\\''\n\n- Expected\n+ Received\n\n- node-version: '22'\n+ name: Frontend Quality (T1)\n+\n+ on:\n+   pull_request:\n+     branches: [main]\n+     paths:\n+       - 'src/**'\n+       - 'public/**'\n+       - '*.html'\n+       - '*.css'\n+       - 'package.json'\n+\n+ permissions:\n+   contents: read\n+\n+ concurrency:\n+   group: frontend-quality-${{ github.head_ref || github.ref_name }}\n+   cancel-in-progress: true\n+\n+ jobs:\n+   lighthouse:\n+     name: Lighthouse CI\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 15\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+\n+       - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0\n+         with:\n+           node-version-file: .nvmrc\n+           cache: npm\n+\n+       - run: npm ci\n+\n+       - name: Build\n+         run: npm run build\n+\n+       - name: Lighthouse CI (lhci)\n+         uses: treosh/lighthouse-ci-action@1b2e9e309eb8f43e89dc9f6ee37ab38f449e47ef # v12.1.0\n+         with:\n+           configPath: '.lighthouserc.json'\n+           uploadArtifacts: true\n+           temporaryPublicStorage: false\n+\n+   axe-accessibility:\n+     name: axe accessibility (WCAG 2.2, INV-61)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 20\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+\n+       - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0\n+         with:\n+           node-version-file: .nvmrc\n+           cache: npm\n+\n+       - run: npm ci\n+\n+       - name: Install Playwright browsers\n+         run: npx playwright install --with-deps chromium\n+\n+       - name: Build\n+         run: npm run build\n+\n+       - name: Resolve E2E start command\n+         id: axe-e2e-start\n+         run: |\n+           if [[ -n \"${E2E_START_CMD:-}\" ]] || node -e \"const fs = require('node:fs'); try { const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8')); process.exit(typeof pkg.scripts?.['start:test'] === 'string' && pkg.scripts['start:test'].trim() ? 0 : 1); } catch { process.exit(1); }\"; then\n+             echo \"available=true\" >> \"$GITHUB_OUTPUT\"\n+           else\n+             echo \"available=false\" >> \"$GITHUB_OUTPUT\"\n+             echo \"SKIP: E2E_START_CMD unset and package.json has no start:test script\"\n+           fi\n+\n+       - name: axe accessibility (Playwright E2E, WCAG 2.2 AA, INV-61)\n+         # Routes through the Playwright a11y spec which uses the generated\n+         # run-axe.ts wrapper with wcag2a+wcag2aa+wcag21a+wcag21aa+wcag22aa tags.\n+         # ephemeral-server starts the app, polls port readiness, runs spec, tears down.\n+         # Hard gate: a11y failures block the PR (INV-61). No soft-fail permitted.\n+         if: steps.axe-e2e-start.outputs.available == 'true'\n+         run: |\n+           node scripts/lib/ephemeral-server.mjs \\\n+             --start \"${E2E_START_CMD:-npm run start:test}\" \\\n+             --test \"npx playwright test tests/e2e/a11y.spec.ts\" \\\n+             --port \"${E2E_PORT:-4173}\"\n+         env:\n+           E2E_BASE_URL: \"http://localhost:${{ vars.E2E_PORT || '4173' }}\"\n+\n+   render-smoke:\n+     name: render smoke (behavioural gate, INV-127)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 20\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+\n+       - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0\n+         with:\n+           node-version-file: .nvmrc\n+           cache: npm\n+\n+       - run: npm ci\n+\n+       # Presence gate first: a frontend project with no render-smoke spec FAILs\n+       # here (fail-closed) before we build — catches a known failure mode from a prior internal project.\n+       - name: render-smoke presence (INV-127)\n+         run: node scripts/check-render-smoke.mjs\n+\n+       - name: Install Playwright browsers\n+         run: npx playwright install --with-deps chromium\n+\n+       - name: Build\n+         run: npm run build\n+\n+       - name: Resolve E2E start command\n+         id: render-smoke-e2e-start\n+         run: |\n+           if [[ -n \"${E2E_START_CMD:-}\" ]] || node -e \"const fs = require('node:fs'); try { const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8')); process.exit(typeof pkg.scripts?.['start:test'] === 'string' && pkg.scripts['start:test'].trim() ? 0 : 1); } catch { process.exit(1); }\"; then\n+             echo \"available=true\" >> \"$GITHUB_OUTPUT\"\n+           else\n+             echo \"available=false\" >> \"$GITHUB_OUTPUT\"\n+             echo \"SKIP: E2E_START_CMD unset and package.json has no start:test script\"\n+           fi\n+\n+       - name: render smoke (Playwright E2E — app shell mounts, no console errors)\n+         # Boots the BUILT SPA via ephemeral-server, runs the render-smoke spec.\n+         # Hard gate: a broken-layout / console-error render blocks the PR (INV-127).\n+         if: steps.render-smoke-e2e-start.outputs.available == 'true'\n+         run: |\n+           node scripts/lib/ephemeral-server.mjs \\\n+             --start \"${E2E_START_CMD:-npm run start:test}\" \\\n+             --test \"npx playwright test tests/e2e/render-smoke.spec.ts\" \\\n+             --port \"${E2E_PORT:-4173}\"\n+         env:\n+           E2E_BASE_URL: \"http://localhost:${{ vars.E2E_PORT || '4173' }}\"\n+\n\n ❯ expectBareNodeSetup __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts:17:18\n     15|\n     16| function expectBareNodeSetup(output: string): void {\n     17|   expect(output).toContain(\"node-version: '22'\")\n       |                  ^\n     18|   expect(output).toContain('package-manager-cache: false')\n     19|   expect(output).not.toContain('node-version-file: .nvmrc')\n ❯ __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts:40:5\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[7/8]⎯\n\n FAIL  __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts > non-TypeScript workflow Node tooling (#2259) > github/workflows/18-frontend-lane.yml.ejs has a Node setup path independent of a root lockfile\nAssertionError: expected 'name: Frontend Lane Gate (T1)\\n\\n# #1…' to contain 'node-version: \\'22\\''\n\n- Expected\n+ Received\n\n- node-version: '22'\n+ name: Frontend Lane Gate (T1)\n+\n+ # #1330 — per-lane frontend gate for a `frontend/` SUBTREE in a polyglot repo.\n+ # Runs the FE lane's own typecheck/test/build (the primary-language CI gate covers\n+ # only the backend). Path-scoped so it fires only on frontend/ subtree changes.\n+\n+ on:\n+   pull_request:\n+     branches: [main]\n+     paths:\n+       - 'frontend/**'\n+       - 'scripts/check-frontend-lane.mjs'\n+   push:\n+     branches: [main]\n+     paths:\n+       - 'frontend/**'\n+       - 'scripts/check-frontend-lane.mjs'\n+\n+ permissions:\n+   contents: read\n+\n+ concurrency:\n+   group: frontend-lane-${{ github.head_ref || github.ref_name }}\n+   cancel-in-progress: true\n+\n+ jobs:\n+   frontend-lane:\n+     name: Frontend lane gate (typecheck, test, build)\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 15\n+     steps:\n+       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n+\n+       - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0\n+         with:\n+           node-version-file: .nvmrc\n+           cache: npm\n+           cache-dependency-path: frontend/package-lock.json\n+\n+       - name: Install frontend subtree deps\n+         run: npm ci --prefix frontend\n+\n+       - name: Run frontend lane gate (full)\n+         run: node scripts/check-frontend-lane.mjs full\n+\n\n ❯ expectBareNodeSetup __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts:17:18\n     15|\n     16| function expectBareNodeSetup(output: string): void {\n     17|   expect(output).toContain(\"node-version: '22'\")\n       |                  ^\n     18|   expect(output).toContain('package-manager-cache: false')\n     19|   expect(output).not.toContain('node-version-file: .nvmrc')\n ❯ __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts:40:5\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[8/8]⎯\n\n",
  "observed_failure": "FAIL  __tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts",
  "recorded_at": "2026-08-09T18:55:52.147Z",
  "test_command": [
    "npx",
    "vitest",
    "run",
    "--config",
    "vitest.integration.config.ts",
    "__tests__/integration/e2e/functional/non-ts-node-tooling-workflows.test.ts"
  ]
}
