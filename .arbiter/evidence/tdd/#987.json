{
  "$schemaVersion": 1,
  "task_id": "#987",
  "test_path": "__tests__/scripts/gitleaks-install-shape.test.ts",
  "test_commit_sha": "ecf7e0f807218ce3f8775bc9da09f17cbd977a20",
  "test_run_log": "\n RUN  v4.1.7 /home/user/work/repos/arbiter.worktrees/987-fix-gitleaks-ubuntu\n\n ❯ __tests__/scripts/gitleaks-install-shape.test.ts (2 tests | 2 failed) 4ms\n     × rendered 01-pr-fast.yml installs gitleaks to $RUNNER_TEMP/bin 3ms\n     × ejs template uses RUNNER_TEMP not /usr/local/bin 1ms\n\n Test Files  1 failed (1)\n      Tests  2 failed (2)\n   Start at  03:00:15\n   Duration  85ms (transform 13ms, setup 13ms, import 6ms, tests 4ms, environment 0ms)\n\n\n\n⎯⎯⎯⎯⎯⎯⎯ Failed Tests 2 ⎯⎯⎯⎯⎯⎯⎯\n\n FAIL  __tests__/scripts/gitleaks-install-shape.test.ts > #987 gitleaks install uses RUNNER_TEMP not /usr/local/bin > rendered 01-pr-fast.yml installs gitleaks to $RUNNER_TEMP/bin\nAssertionError: expected 'name: PR Fast (T1)\\n\\npermissions:\\n …' to match /RUNNER_TEMP\\/bin/bin\n\n- Expected:\n/RUNNER_TEMP\\/bin/\n\n+ Received:\n\"name: PR Fast (T1)\n\npermissions:\n  contents: read\n\non:\n  push:\n    branches: [main, \\\"task/**\\\"]\n  pull_request:\n    types: [opened, synchronize, reopened, labeled]\n    branches: [main]\n\n# Cancel in-progress PR fast runs on the same branch to free capacity.\n# Main runs always complete so post-merge status isn't truncated (#357).\nconcurrency:\n  group: pr-fast-${{ github.head_ref || github.ref }}\n  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}\n\njobs:\n\n  security-early-fail:\n    name: Security (PII + Secrets)\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    steps:\n      - uses: actions/checkout@v6\n        with:\n          fetch-depth: 0\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n      - name: PII scan (INV-12, HARD)\n        run: node scripts/pii-scan.mjs\n      - name: Install gitleaks\n        run: |\n          mkdir -p \\\"$/usr/local/bin\\\"\n          curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \\\"$/usr/local/bin\\\" gitleaks\n          echo \\\"$/usr/local/bin\\\" >> \\\"$GITHUB_PATH\\\"\n      - name: Gitleaks secrets scan (INV-11)\n        run: |\n          gitleaks detect --source . --config .gitleaks.toml \\\\\n            --gitleaks-ignore-path suppressions/.gitleaksignore \\\\\n            --exit-code 1 --redact\n\n\n  classify-changes:\n    name: Classify Changes\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    outputs:\n      docs_only: ${{ steps.classify.outputs.docs_only }}\n      backend_changed: ${{ steps.classify.outputs.backend_changed }}\n      frontend_changed: ${{ steps.classify.outputs.frontend_changed }}\n      infra_changed: ${{ steps.classify.outputs.infra_changed }}\n      high_risk: ${{ steps.classify.outputs.high_risk }}\n    steps:\n      - uses: actions/checkout@v6\n        with:\n          fetch-depth: 0\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n      - id: classify\n        run: node scripts/ci-classify-changes.mjs\n        env:\n          BASE_SHA: ${{ github.event.pull_request.base.sha || 'origin/main' }}\n          HEAD_SHA: ${{ github.sha }}\n\n\n\n\n  gate:\n    name: Lint + Typecheck\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [security-early-fail, classify-changes]\n    if: needs.classify-changes.outputs.docs_only != 'true'\n    steps:\n      - uses: actions/checkout@v6\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n          cache: 'npm'\n      - run: npm ci\n      - run: npm run typecheck\n      - run: npm run lint\n\n      - name: Dep audit (INV-13)\n        run: npm audit --audit-level=high\n\n\n  unit-tests:\n    name: Unit Tests\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [gate]\n    if: needs.classify-changes.outputs.docs_only != 'true'\n    steps:\n      - uses: actions/checkout@v6\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n          cache: 'npm'\n      - run: npm ci\n      - run: npm run build && node scripts/build-kit.mjs\n      - run: npm run test:unit -- --reporter=junit --outputFile=test-results/junit.xml\n\n      - name: Upload test results\n        if: always()\n        uses: actions/upload-artifact@v7.0.1\n        with:\n          name: test-results-unit-${{ github.run_id }}\n          path: test-results/\n          retention-days: 7\n\n\n  contract-tests:\n    name: Contract Tests\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [gate]\n    if: needs.classify-changes.outputs.docs_only != 'true'\n    steps:\n      - uses: actions/checkout@v6\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n          cache: 'npm'\n      - run: npm ci\n      - run: npm run build && node scripts/build-kit.mjs\n      - run: npm run test:contract\n\n  integration-tests:\n    name: Integration Tests\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [unit-tests]\n    if: needs.classify-changes.outputs.docs_only != 'true'\n    steps:\n      - uses: actions/checkout@v6\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n          cache: 'npm'\n      - run: npm ci\n      - run: npm run build && node scripts/build-kit.mjs\n      - run: npm run test:integration\n\n  behavioral-tests:\n    name: Behavioral Tests\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [unit-tests]\n    if: needs.classify-changes.outputs.docs_only != 'true'\n    steps:\n      - uses: actions/checkout@v6\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n          cache: 'npm'\n      - run: npm ci\n      - run: npm run build && node scripts/build-kit.mjs\n      - run: npm run test:behavioral\n\n\n\n\n  docs-check:\n    name: Docs Check\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    if: github.event_name == 'pull_request'\n    steps:\n      - uses: actions/checkout@v6\n        with:\n          fetch-depth: 0\n      - name: Check docs updated with code changes\n        env:\n          BASE_SHA: ${{ github.event.pull_request.base.sha }}\n          HEAD_SHA: ${{ github.event.pull_request.head.sha }}\n        run: |\n          # #356: rebased-aware via merge-base; bypass via [skip-docs] commit token.\n          MB=$(git merge-base \\\"$BASE_SHA\\\" \\\"$HEAD_SHA\\\" || echo \\\"$BASE_SHA\\\")\n          CHANGED=$(git diff --name-only \\\"$MB\\\" \\\"$HEAD_SHA\\\")\n          LOG=$(git log --format=%B \\\"$MB..$HEAD_SHA\\\" || true)\n\n          HAS_CODE=$(echo \\\"$CHANGED\\\" | grep -E '^(src/|__tests__/)' || true)\n          HAS_DOCS=$(echo \\\"$CHANGED\\\" | grep -E '^(docs/|README\\\\.md)' || true)\n          BYPASS=$(echo \\\"$LOG\\\" | grep -F '[skip-docs]' || true)\n\n          if [ -n \\\"$HAS_CODE\\\" ] && [ -z \\\"$HAS_DOCS\\\" ] && [ -z \\\"$BYPASS\\\" ]; then\n            echo \\\"::error::Code changed without documentation update\\\"\n            echo \\\"\\\"\n            echo \\\"Files changed in src/ or __tests__/:\\\"\n            echo \\\"$HAS_CODE\\\"\n            echo \\\"\\\"\n            echo \\\"Please update docs/ or README.md, or add [skip-docs] to a commit message to bypass.\\\"\n            exit 1\n          fi\n          echo \\\"Docs check passed.\\\"\n\n\n\n\n  debt-gates:\n    name: Tech Debt Gates\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [gate]\n    steps:\n      - uses: actions/checkout@v6\n\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n          cache: 'npm'\n      - run: npm ci\n      - run: npm run build && node scripts/build-kit.mjs\n      - run: npx vitest run --coverage --coverage.thresholds.lines=80\n      - run: npx knip\n      - run: npx madge --circular src\n\n      - name: Upload coverage reports\n        uses: actions/upload-artifact@v7.0.1\n        if: always()\n        with:\n          name: coverage-${{ github.run_id }}\n          path: coverage/\n          retention-days: 30\n\n\n\n  debt-ratchet:\n    name: Debt Ratchet\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [gate]\n    steps:\n      - uses: actions/checkout@v6\n\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n          cache: 'npm'\n      - run: npm ci\n\n      - name: Run debt ratchet\n        run: node scripts/debt-report.mjs --gate\n\n\n\n  human-approval-required:\n    name: Human Approval Required (INV-74)\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    if: github.event_name == 'pull_request'\n    steps:\n      - name: Assert approved-by-human label (anti-bot gate)\n        uses: actions/github-script@v9\n        with:\n          github-token: ${{ secrets.GITHUB_TOKEN }}\n          script: |\n            const labels = context.payload.pull_request.labels.map(l => l.name);\n            if (!labels.includes('approved-by-human')) {\n              core.setFailed(\n                'PR requires the \\\"approved-by-human\\\" label applied by a human reviewer. ' +\n                'See 03-human-approval.yml — a bot approval does not satisfy this check.'\n              );\n            }\n\n  ci-required:\n    name: CI Required\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n\n    needs: [gate, human-approval-required, unit-tests, contract-tests, integration-tests, behavioral-tests, security-early-fail, docs-check, debt-gates, debt-ratchet, classify-changes]\n    if: always()\n    steps:\n      - run: |\n          if [[ \\\"${{ needs.gate.result }}\\\" != \\\"success\\\" ]]; then\n            echo \\\"gate failed\\\"\n            exit 1\n          fi\n\n          if [[ \\\"${{ needs.unit-tests.result }}\\\" != \\\"success\\\" ]]; then\n            echo \\\"unit-tests failed\\\"\n            exit 1\n          fi\n          if [[ \\\"${{ needs.contract-tests.result }}\\\" != \\\"success\\\" ]]; then\n            echo \\\"contract-tests failed\\\"\n            exit 1\n          fi\n          if [[ \\\"${{ needs.integration-tests.result }}\\\" != \\\"success\\\" ]]; then\n            echo \\\"integration-tests failed\\\"\n            exit 1\n          fi\n          if [[ \\\"${{ needs.behavioral-tests.result }}\\\" != \\\"success\\\" ]]; then\n            echo \\\"behavioral-tests failed\\\"\n            exit 1\n          fi\n\n          if [[ \\\"${{ needs.security-early-fail.result }}\\\" != \\\"success\\\" ]]; then\n            echo \\\"security-early-fail failed\\\"\n            exit 1\n          fi\n          if [[ \\\"${{ needs.docs-check.result }}\\\" == \\\"failure\\\" ]]; then\n            echo \\\"docs-check failed\\\"\n            exit 1\n          fi\n          if [[ \\\"${{ needs.debt-gates.result }}\\\" != \\\"success\\\" ]]; then\n            echo \\\"debt-gates failed\\\"\n            exit 1\n          fi\n          if [[ \\\"${{ needs.debt-ratchet.result }}\\\" != \\\"success\\\" ]]; then\n            echo \\\"debt-ratchet failed\\\"\n            exit 1\n          fi\n          if [[ \\\"${{ needs.human-approval-required.result }}\\\" == \\\"failure\\\" ]]; then\n            echo \\\"human-approval-required failed — PR needs approved-by-human label\\\"\n            exit 1\n          fi\n\"\n\n ❯ __tests__/scripts/gitleaks-install-shape.test.ts:6:17\n      4|   it('rendered 01-pr-fast.yml installs gitleaks to $RUNNER_TEMP/bin', …\n      5|     const yml = readFileSync('.github/workflows/01-pr-fast.yml', 'utf-…\n      6|     expect(yml).toMatch(/RUNNER_TEMP\\/bin/)\n       |                 ^\n      7|     expect(yml).toMatch(/GITHUB_PATH/)\n      8|     expect(yml).not.toMatch(/-C \\/usr\\/local\\/bin gitleaks/)\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/2]⎯\n\n FAIL  __tests__/scripts/gitleaks-install-shape.test.ts > #987 gitleaks install uses RUNNER_TEMP not /usr/local/bin > ejs template uses RUNNER_TEMP not /usr/local/bin\nAssertionError: expected '<% const _lanes = Array.isArray(lanes…' to match /RUNNER_TEMP\\/bin/bin\n\n- Expected:\n/RUNNER_TEMP\\/bin/\n\n+ Received:\n\"<% const _lanes = Array.isArray(lanes) ? lanes : []; const _multiLane = _lanes.length >= 2; %>name: PR Fast (T1)\n\npermissions:\n  contents: read\n\non:\n  push:\n    branches: [main, \\\"task/**\\\"]\n  pull_request:\n    types: [opened, synchronize, reopened, labeled]\n    branches: [main]\n\n# Cancel in-progress PR fast runs on the same branch to free capacity.\n# Main runs always complete so post-merge status isn't truncated (#357).\nconcurrency:\n  group: pr-fast-${{ github.head_ref || github.ref }}\n  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}\n\njobs:\n<% if (enableSoloDevMode) { %>\n  solo-dev-gate:\n    name: Solo-Dev Gate (INV-59)\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    steps:\n      - run: echo \\\"solo-dev mode — local L2 gate guards merge via INV-59 parity check\\\"\n<% } else { %><% if (enableSecurityScanning) { %>\n  security-early-fail:\n    name: Security (PII + Secrets)\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    steps:\n      - uses: actions/checkout@v6\n        with:\n          fetch-depth: 0\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n      - name: PII scan (INV-12, HARD)\n        run: node scripts/pii-scan.mjs\n      - name: Install gitleaks\n        run: |\n          mkdir -p \\\"$/usr/local/bin\\\"\n          curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.27.2/gitleaks_8.27.2_linux_x64.tar.gz | tar -xz -C \\\"$/usr/local/bin\\\" gitleaks\n          echo \\\"$/usr/local/bin\\\" >> \\\"$GITHUB_PATH\\\"\n      - name: Gitleaks secrets scan (INV-11)\n        run: |\n          gitleaks detect --source . --config .gitleaks.toml \\\\\n            --gitleaks-ignore-path suppressions/.gitleaksignore \\\\\n            --exit-code 1 --redact\n<% } %>\n<% if (governanceLevel !== 'L1' || _multiLane) { %>\n  classify-changes:\n    name: Classify Changes\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    outputs:\n      docs_only: ${{ steps.classify.outputs.docs_only }}\n      backend_changed: ${{ steps.classify.outputs.backend_changed }}\n      frontend_changed: ${{ steps.classify.outputs.frontend_changed }}\n      infra_changed: ${{ steps.classify.outputs.infra_changed }}\n      high_risk: ${{ steps.classify.outputs.high_risk }}\n    steps:\n      - uses: actions/checkout@v6\n        with:\n          fetch-depth: 0\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n      - id: classify\n        run: node scripts/ci-classify-changes.mjs\n        env:\n          BASE_SHA: ${{ github.event.pull_request.base.sha || 'origin/main' }}\n          HEAD_SHA: ${{ github.sha }}\n<% } %>\n<% if (_multiLane) { %>\n  cross-stack-guard:\n    name: Cross-Stack Guard\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [classify-changes]\n    if: github.event_name == 'pull_request'\n    permissions:\n      pull-requests: write\n    steps:\n<% if (governanceLevel === 'L3') { %>\n      - name: Enforce cross-stack split\n        if: needs.classify-changes.outputs.backend_changed == 'true' && needs.classify-changes.outputs.frontend_changed == 'true'\n        run: |\n          echo \\\"::error::This PR touches both frontend and backend lanes. Split into separate PRs.\\\"\n          exit 1\n<% } else { %>\n      - name: Warn cross-stack changes\n        if: needs.classify-changes.outputs.backend_changed == 'true' && needs.classify-changes.outputs.frontend_changed == 'true'\n        continue-on-error: true\n        uses: actions/github-script@v9\n        with:\n          github-token: ${{ secrets.GITHUB_TOKEN }}\n          script: |\n            github.rest.issues.createComment({\n              issue_number: context.issue.number,\n              owner: context.repo.owner,\n              repo: context.repo.repo,\n              body: '> [!WARNING]\\\\n> This PR touches both **frontend** and **backend** lanes. Consider splitting into separate PRs for cleaner review and safer deployment.'\n            })\n<% } %>\n<% } %>\n<% if (language === 'typescript') { %>\n<% const _tsNeeds=[]; if(enableSecurityScanning) _tsNeeds.push('security-early-fail'); if(governanceLevel !== 'L1' || _multiLane) _tsNeeds.push('classify-changes'); %>\n  gate:\n    name: Lint + Typecheck\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n<% if(_tsNeeds.length){ %>    needs: [<%= _tsNeeds.join(', ') %>]\n<% } if(governanceLevel !== 'L1' || _multiLane){ %>    if: needs.classify-changes.outputs.docs_only != 'true'\n<% } %>    steps:\n      - uses: actions/checkout@v6\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n          cache: 'npm'\n      - run: npm ci\n      - run: npm run typecheck\n      - run: npm run lint\n<% if (enableSecurityScanning) { %>\n      - name: Dep audit (INV-13)\n        run: npm audit --audit-level=high\n<% } %>\n\n  unit-tests:\n    name: Unit Tests\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [gate]\n<% if(governanceLevel !== 'L1' || _multiLane){ %>    if: needs.classify-changes.outputs.docs_only != 'true'\n<% } %>    steps:\n      - uses: actions/checkout@v6\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n          cache: 'npm'\n      - run: npm ci\n<% if (locals.ciPreTestBuild) { %>      - run: npm run build && node scripts/build-kit.mjs\n<% } %>      - run: npm run test:unit<% if (enableDebtGates) { %> -- --reporter=junit --outputFile=test-results/junit.xml<% } %>\n<% if (enableDebtGates) { %>\n      - name: Upload test results\n        if: always()\n        uses: actions/upload-artifact@v7.0.1\n        with:\n          name: test-results-unit-${{ github.run_id }}\n          path: test-results/\n          retention-days: 7\n<% } %>\n\n  contract-tests:\n    name: Contract Tests\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [gate]\n<% if(governanceLevel !== 'L1' || _multiLane){ %>    if: needs.classify-changes.outputs.docs_only != 'true'\n<% } %>    steps:\n      - uses: actions/checkout@v6\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n          cache: 'npm'\n      - run: npm ci\n<% if (locals.ciPreTestBuild) { %>      - run: npm run build && node scripts/build-kit.mjs\n<% } %>      - run: npm run test:contract\n\n  integration-tests:\n    name: Integration Tests\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [unit-tests]\n<% if(governanceLevel !== 'L1' || _multiLane){ %>    if: needs.classify-changes.outputs.docs_only != 'true'\n<% } %>    steps:\n      - uses: actions/checkout@v6\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n          cache: 'npm'\n      - run: npm ci\n<% if (locals.ciPreTestBuild) { %>      - run: npm run build && node scripts/build-kit.mjs\n<% } %>      - run: npm run test:integration\n\n  behavioral-tests:\n    name: Behavioral Tests\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [unit-tests]\n<% if(governanceLevel !== 'L1' || _multiLane){ %>    if: needs.classify-changes.outputs.docs_only != 'true'\n<% } %>    steps:\n      - uses: actions/checkout@v6\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n          cache: 'npm'\n      - run: npm ci\n<% if (locals.ciPreTestBuild) { %>      - run: npm run build && node scripts/build-kit.mjs\n<% } %>      - run: npm run test:behavioral\n\n<% } %><% if (language === 'rust') { %>\n  gate:\n    name: Lint + Test (Rust)\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n<% { const _n=[]; if(enableSecurityScanning) _n.push('security-early-fail'); if(governanceLevel !== 'L1' || _multiLane) _n.push('classify-changes'); if(_n.length){ %>    needs: [<%= _n.join(', ') %>]\n<% } if(governanceLevel !== 'L1' || _multiLane){ %>    if: needs.classify-changes.outputs.docs_only != 'true'\n<% } } %>    steps:\n      - uses: actions/checkout@v6\n      - uses: dtolnay/rust-toolchain@stable\n        with:\n          components: clippy, rustfmt\n      - uses: Swatinem/rust-cache@v2\n      - run: cargo fmt --check\n      - run: cargo clippy -- -D warnings\n      - run: cargo test\n<% if (enableSecurityScanning) { %>\n      - name: Dep audit (INV-13)\n        uses: rustsec/audit-check@v1\n        with:\n          token: ${{ secrets.GITHUB_TOKEN }}\n<% } %>\n<% } %><% if (language === 'java') { %>\n<% const _javaNeeds=[]; if(enableSecurityScanning) _javaNeeds.push('security-early-fail'); if(governanceLevel !== 'L1' || _multiLane) _javaNeeds.push('classify-changes'); %>\n  gate:\n    name: Lint (Java)\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n<% if(_javaNeeds.length){ %>    needs: [<%= _javaNeeds.join(', ') %>]\n<% } if(governanceLevel !== 'L1' || _multiLane){ %>    if: needs.classify-changes.outputs.docs_only != 'true'\n<% } %>    steps:\n      - uses: actions/checkout@v6\n      - uses: actions/setup-java@v4\n        with:\n          java-version: '21'\n          distribution: 'temurin'\n<% if (buildTool === 'gradle') { %>      - uses: gradle/actions/setup-gradle@v3\n      - run: ./gradlew checkstyleMain\n<% if (enableSecurityScanning) { %>\n      - name: Dep audit OWASP DC (INV-13)\n        run: ./gradlew dependencyCheckAnalyze\n<% } %>\n<% } else { %>      - run: mvn checkstyle:check\n<% if (enableSecurityScanning) { %>\n      - name: Dep audit OWASP DC (INV-13)\n        run: mvn org.owasp:dependency-check-maven:check\n<% } %>\n<% } %>\n\n  unit-tests:\n    name: Unit Tests (Java)\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [gate]\n<% if(governanceLevel !== 'L1' || _multiLane){ %>    if: needs.classify-changes.outputs.docs_only != 'true'\n<% } %>    steps:\n      - uses: actions/checkout@v6\n      - uses: actions/setup-java@v4\n        with:\n          java-version: '21'\n          distribution: 'temurin'\n<% if (buildTool === 'gradle') { %>      - uses: gradle/actions/setup-gradle@v3\n      - run: ./gradlew test\n<% if (enableDebtGates) { %>\n      - name: Upload test results\n        if: always()\n        uses: actions/upload-artifact@v7.0.1\n        with:\n          name: test-results-unit-${{ github.run_id }}\n          path: build/test-results/\n          retention-days: 7\n<% } %>\n<% } else { %>      - run: mvn test -Punit\n<% if (enableDebtGates) { %>\n      - name: Upload test results\n        if: always()\n        uses: actions/upload-artifact@v7.0.1\n        with:\n          name: test-results-unit-${{ github.run_id }}\n          path: target/surefire-reports/\n          retention-days: 7\n<% } %>\n<% } %>\n\n  contract-tests:\n    name: Contract Tests (Java)\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [gate]\n<% if(governanceLevel !== 'L1' || _multiLane){ %>    if: needs.classify-changes.outputs.docs_only != 'true'\n<% } %>    steps:\n      - uses: actions/checkout@v6\n      - uses: actions/setup-java@v4\n        with:\n          java-version: '21'\n          distribution: 'temurin'\n<% if (buildTool === 'gradle') { %>      - uses: gradle/actions/setup-gradle@v3\n      - run: ./gradlew contractTest\n<% } else { %>      - run: mvn test -Pcontract\n<% } %>\n\n  integration-tests:\n    name: Integration Tests (Java)\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [unit-tests]\n<% if(governanceLevel !== 'L1' || _multiLane){ %>    if: needs.classify-changes.outputs.docs_only != 'true'\n<% } %>    steps:\n      - uses: actions/checkout@v6\n      - uses: actions/setup-java@v4\n        with:\n          java-version: '21'\n          distribution: 'temurin'\n<% if (buildTool === 'gradle') { %>      - uses: gradle/actions/setup-gradle@v3\n      - run: ./gradlew integrationTest\n<% } else { %>      - run: mvn test -Pintegration\n<% } %>\n\n  behavioral-tests:\n    name: Behavioral Tests (Java)\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [unit-tests]\n<% if(governanceLevel !== 'L1' || _multiLane){ %>    if: needs.classify-changes.outputs.docs_only != 'true'\n<% } %>    steps:\n      - uses: actions/checkout@v6\n      - uses: actions/setup-java@v4\n        with:\n          java-version: '21'\n          distribution: 'temurin'\n<% if (buildTool === 'gradle') { %>      - uses: gradle/actions/setup-gradle@v3\n      - run: ./gradlew behavioralTest\n<% } else { %>      - run: mvn test -Pbehavioral\n<% } %>\n\n<% } %><% if (language === 'go') { %>\n  gate:\n    name: Lint + Test (Go)\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n<% { const _n=[]; if(enableSecurityScanning) _n.push('security-early-fail'); if(governanceLevel !== 'L1' || _multiLane) _n.push('classify-changes'); if(_n.length){ %>    needs: [<%= _n.join(', ') %>]\n<% } if(governanceLevel !== 'L1' || _multiLane){ %>    if: needs.classify-changes.outputs.docs_only != 'true'\n<% } } %>    steps:\n      - uses: actions/checkout@v6\n      - uses: actions/setup-go@v5\n        with:\n          go-version-file: 'go.mod'\n      - run: go vet ./...\n      - run: golangci-lint run\n      - run: go test ./... -race -coverprofile=coverage.out\n<% if (enableSecurityScanning) { %>\n      - name: Dep audit govulncheck (INV-13)\n        uses: golang/govulncheck-action@v1\n<% } %>\n<% } %><% if (language === 'python') { %>\n  gate:\n    name: Lint + Test (Python)\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n<% { const _n=[]; if(enableSecurityScanning) _n.push('security-early-fail'); if(governanceLevel !== 'L1' || _multiLane) _n.push('classify-changes'); if(_n.length){ %>    needs: [<%= _n.join(', ') %>]\n<% } if(governanceLevel !== 'L1' || _multiLane){ %>    if: needs.classify-changes.outputs.docs_only != 'true'\n<% } } %>    steps:\n      - uses: actions/checkout@v6\n      - uses: actions/setup-python@v5\n        with:\n          python-version: '3.12'\n      - run: pip install -e \\\".[dev]\\\"\n      - run: ruff check .\n      - run: ruff format --check .\n      - run: pytest --cov --junitxml=test-results/junit.xml\n<% if (enableSecurityScanning) { %>\n      - name: Dep audit (INV-13)\n        run: pip-audit\n<% } %>\n<% if (enableDebtGates) { %>\n      - name: Upload test results\n        if: always()\n        uses: actions/upload-artifact@v7.0.1\n        with:\n          name: test-results-${{ github.run_id }}\n          path: test-results/\n          retention-days: 7\n<% } %>\n<% } %>\n\n<% if (governanceLevel !== 'L1') { %>\n  docs-check:\n    name: Docs Check\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    if: github.event_name == 'pull_request'\n    steps:\n      - uses: actions/checkout@v6\n        with:\n          fetch-depth: 0\n      - name: Check docs updated with code changes\n        env:\n          BASE_SHA: ${{ github.event.pull_request.base.sha }}\n          HEAD_SHA: ${{ github.event.pull_request.head.sha }}\n        run: |\n          # #356: rebased-aware via merge-base; bypass via [skip-docs] commit token.\n          MB=$(git merge-base \\\"$BASE_SHA\\\" \\\"$HEAD_SHA\\\" || echo \\\"$BASE_SHA\\\")\n          CHANGED=$(git diff --name-only \\\"$MB\\\" \\\"$HEAD_SHA\\\")\n          LOG=$(git log --format=%B \\\"$MB..$HEAD_SHA\\\" || true)\n\n          HAS_CODE=$(echo \\\"$CHANGED\\\" | grep -E '^(src/|__tests__/)' || true)\n          HAS_DOCS=$(echo \\\"$CHANGED\\\" | grep -E '^(docs/|README\\\\.md)' || true)\n          BYPASS=$(echo \\\"$LOG\\\" | grep -F '[skip-docs]' || true)\n\n          if [ -n \\\"$HAS_CODE\\\" ] && [ -z \\\"$HAS_DOCS\\\" ] && [ -z \\\"$BYPASS\\\" ]; then\n            echo \\\"::error::Code changed without documentation update\\\"\n            echo \\\"\\\"\n            echo \\\"Files changed in src/ or __tests__/:\\\"\n            echo \\\"$HAS_CODE\\\"\n            echo \\\"\\\"\n            echo \\\"Please update docs/ or README.md, or add [skip-docs] to a commit message to bypass.\\\"\n            exit 1\n          fi\n          echo \\\"Docs check passed.\\\"\n<% } %>\n\n<% if (enableDebtGates) { %>\n<% const threshold = governanceLevel === 'L3' ? 85 : 80; %>\n  debt-gates:\n    name: Tech Debt Gates\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [gate]\n    steps:\n      - uses: actions/checkout@v6\n<% if (language === 'typescript') { %>\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n          cache: 'npm'\n      - run: npm ci\n<% if (locals.ciPreTestBuild) { %>      - run: npm run build && node scripts/build-kit.mjs\n<% } %>      - run: npx vitest run --coverage --coverage.thresholds.lines=<%= threshold %>\n      - run: npx knip\n      - run: npx madge --circular src\n<% } %><% if (language === 'rust') { %>\n      - uses: dtolnay/rust-toolchain@stable\n        with:\n          components: clippy\n      - uses: Swatinem/rust-cache@v2\n      - run: cargo install cargo-tarpaulin\n      - run: cargo tarpaulin --fail-under <%= threshold %> --out Html --out Xml --out Lcov --output-dir coverage/\n      - run: cargo clippy -- -W clippy::pedantic -D warnings\n<% } %><% if (language === 'java') { %>\n      - uses: actions/setup-java@v4\n        with:\n          java-version: '21'\n          distribution: 'temurin'\n<% if (buildTool === 'gradle') { %>      - uses: gradle/actions/setup-gradle@v3\n      - run: ./gradlew jacocoTestCoverageVerification\n      - run: ./gradlew pmdMain\n      - run: ./gradlew spotbugsMain\n      - name: Copy JaCoCo reports to coverage/\n        if: always()\n        run: |\n          mkdir -p coverage\n          cp -r build/coverage/html/. coverage/ 2>/dev/null || true\n          cp build/coverage/coverage.xml coverage/coverage.xml 2>/dev/null || true\n<% } else { %>      - run: mvn verify -Pjacoco\n      - run: mvn pmd:check\n      - run: mvn spotbugs:check\n      - name: Copy JaCoCo reports to coverage/\n        if: always()\n        run: |\n          mkdir -p coverage\n          cp -r target/coverage/. coverage/ 2>/dev/null || true\n<% } %><% } %><% if (language === 'go') { %>\n      - uses: actions/setup-go@v5\n        with:\n          go-version-file: 'go.mod'\n      - run: go test -coverprofile=coverage.out -covermode=atomic ./...\n      - name: Check coverage threshold\n        run: |\n          PCT=$(go tool cover -func=coverage.out | grep total | awk '{print $3}' | tr -d '%')\n          if (( $(echo \\\"$PCT < <%= threshold %>\\\" | bc -l) )); then\n            echo \\\"Coverage ${PCT}% is below <%= threshold %>%\\\"\n            exit 1\n          fi\n          echo \\\"Coverage ${PCT}% >= <%= threshold %>%\\\"\n      - name: Generate HTML coverage report\n        run: |\n          mkdir -p coverage\n          go tool cover -html=coverage.out -o coverage/coverage.html\n      - run: golangci-lint run --enable gocyclo,gocognit,deadcode,unused\n<% } %><% if (language === 'python') { %>\n      - uses: actions/setup-python@v5\n        with:\n          python-version: '3.12'\n      - run: pip install -e \\\".[dev]\\\"\n      - run: pytest --cov --cov-fail-under=<%= threshold %> --cov-report=html:coverage/html --cov-report=xml:coverage/coverage.xml\n      - run: ruff check --select C901,PLR0911,PLR0912,PLR0913,PLR0915 .\n      - run: ruff check --select F401,F811 .\n<% } %>\n      - name: Upload coverage reports\n        uses: actions/upload-artifact@v7.0.1\n        if: always()\n        with:\n          name: coverage-${{ github.run_id }}\n          path: coverage/\n          retention-days: 30\n<% } %>\n\n<% if (enableDebtGates) { %>\n  debt-ratchet:\n    name: Debt Ratchet\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [gate]\n    steps:\n      - uses: actions/checkout@v6\n<% if (language === 'typescript') { %>\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n          cache: 'npm'\n      - run: npm ci\n<% } %><% if (language === 'rust') { %>\n      - uses: dtolnay/rust-toolchain@stable\n      - uses: Swatinem/rust-cache@v2\n<% } %><% if (language === 'java') { %>\n      - uses: actions/setup-java@v4\n        with:\n          java-version: '21'\n          distribution: 'temurin'\n<% if (buildTool === 'gradle') { %>      - uses: gradle/actions/setup-gradle@v3\n<% } %><% } %><% if (language === 'go') { %>\n      - uses: actions/setup-go@v5\n        with:\n          go-version-file: 'go.mod'\n<% } %><% if (language === 'python') { %>\n      - uses: actions/setup-python@v5\n        with:\n          python-version: '3.12'\n      - run: pip install -e \\\".[dev]\\\"\n<% } %>\n      - name: Run debt ratchet\n        run: node scripts/debt-report.mjs<% if (governanceLevel === 'L3') { %> --require-improvement<% } else { %> --gate<% } %>\n<% } %>\n<% if (contractType && contractType !== 'none' && governanceLevel !== 'L1') { %>\n\n  contract-verify:\n    name: Contract Verify\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    needs: [gate]\n    steps:\n      - uses: actions/checkout@v6\n<% if (language === 'typescript') { %>\n      - uses: actions/setup-node@v6\n        with:\n          node-version-file: '.nvmrc'\n          cache: 'npm'\n      - run: npm ci\n<% if (contractType === 'rest-owned' || contractType === 'rest-public') { %>\n      - name: Run Pact contract tests\n        if: vars.PACT_BROKER_BASE_URL != ''\n        run: npx pact-broker can-i-deploy --pacticipant <%= projectName %> --latest\n        env:\n          PACT_BROKER_BASE_URL: ${{ vars.PACT_BROKER_BASE_URL }}\n          PACT_BROKER_TOKEN: ${{ secrets.PACT_BROKER_TOKEN }}\n<% } else if (contractType === 'graphql') { %>\n      - name: Run GraphQL schema contract tests\n        run: npx graphql-inspector diff schema.graphql\n<% } else if (contractType === 'grpc') { %>\n      - name: Run gRPC contract tests\n        run: npx buf breaking --against .git#branch=main\n<% } else if (contractType === 'message-queue') { %>\n      - name: Run Pact messaging contract tests\n        if: vars.PACT_BROKER_BASE_URL != ''\n        run: npx pact-broker can-i-deploy --pacticipant <%= projectName %> --latest\n        env:\n          PACT_BROKER_BASE_URL: ${{ vars.PACT_BROKER_BASE_URL }}\n          PACT_BROKER_TOKEN: ${{ secrets.PACT_BROKER_TOKEN }}\n<% } %>\n<% } else if (language === 'java') { %>\n      - uses: actions/setup-java@v4\n        with:\n          java-version: '21'\n          distribution: 'temurin'\n<% if (buildTool === 'gradle') { %>      - uses: gradle/actions/setup-gradle@v3\n<% } %>\n<% if (contractType === 'rest-owned' || contractType === 'rest-public' || contractType === 'message-queue') { %>\n      - name: Run Pact contract tests\n        if: vars.PACT_BROKER_BASE_URL != ''\n        run: <%= buildTool === 'gradle' ? './gradlew pactPublish pactVerify' : 'mvn pact:publish pact:verify' %>\n        env:\n          PACT_BROKER_BASE_URL: ${{ vars.PACT_BROKER_BASE_URL }}\n          PACT_BROKER_TOKEN: ${{ secrets.PACT_BROKER_TOKEN }}\n<% } else if (contractType === 'graphql') { %>\n      - name: Run GraphQL contract tests\n        run: <%= buildTool === 'gradle' ? './gradlew test --tests *.graphql.*' : 'mvn test -Dtest=*GraphQL*' %>\n<% } else if (contractType === 'grpc') { %>\n      - name: Run gRPC contract tests\n        run: <%= buildTool === 'gradle' ? './gradlew test --tests *.grpc.*' : 'mvn test -Dtest=*Grpc*' %>\n<% } %>\n<% } else if (language === 'rust') { %>\n      - uses: dtolnay/rust-toolchain@stable\n      - uses: Swatinem/rust-cache@v2\n      - name: Run contract tests\n        run: cargo test --test '*contract*'\n<% } else if (language === 'go') { %>\n      - uses: actions/setup-go@v5\n        with:\n          go-version-file: 'go.mod'\n      - name: Run contract tests\n        run: go test -tags contract -run Contract ./...\n<% } else if (language === 'python') { %>\n      - uses: actions/setup-python@v5\n        with:\n          python-version: '3.12'\n      - run: pip install -e \\\".[dev]\\\"\n      - name: Run contract tests\n        run: pytest tests/contract/\n<% } %>\n<% } %>\n\n  human-approval-required:\n    name: Human Approval Required (INV-74)\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n    if: github.event_name == 'pull_request'\n    steps:\n      - name: Assert approved-by-human label (anti-bot gate)\n        uses: actions/github-script@v9\n        with:\n          github-token: ${{ secrets.GITHUB_TOKEN }}\n          script: |\n            const labels = context.payload.pull_request.labels.map(l => l.name);\n            if (!labels.includes('approved-by-human')) {\n              core.setFailed(\n                'PR requires the \\\"approved-by-human\\\" label applied by a human reviewer. ' +\n                'See 03-human-approval.yml — a bot approval does not satisfy this check.'\n              );\n            }\n\n  ci-required:\n    name: CI Required\n    runs-on: ${{ vars.CI_BUILD_RUNNER_LABEL || 'ubuntu-latest' }}\n<%\n  const _ciNeeds = ['gate', 'human-approval-required'];\n  if (language === 'typescript' || language === 'java') {\n    _ciNeeds.push('unit-tests', 'contract-tests', 'integration-tests', 'behavioral-tests');\n  }\n  if (enableSecurityScanning) _ciNeeds.push('security-early-fail');\n  if (governanceLevel !== 'L1') _ciNeeds.push('docs-check');\n  if (enableDebtGates) _ciNeeds.push('debt-gates', 'debt-ratchet');\n  if (contractType && contractType !== 'none' && governanceLevel !== 'L1') _ciNeeds.push('contract-verify');\n  if (governanceLevel !== 'L1' || _multiLane) _ciNeeds.push('classify-changes');\n  if (_multiLane) _ciNeeds.push('cross-stack-guard');\n%>\n    needs: [<%= _ciNeeds.join(', ') %>]\n    if: always()\n    steps:\n      - run: |\n          if [[ \\\"${{ needs.gate.result }}\\\" != \\\"success\\\" ]]; then\n            echo \\\"gate failed\\\"\n            exit 1\n          fi\n<% if (language === 'typescript' || language === 'java') { %>\n          if [[ \\\"${{ needs.unit-tests.result }}\\\" != \\\"success\\\" ]]; then\n            echo \\\"unit-tests failed\\\"\n            exit 1\n          fi\n          if [[ \\\"${{ needs.contract-tests.result }}\\\" != \\\"success\\\" ]]; then\n            echo \\\"contract-tests failed\\\"\n            exit 1\n          fi\n          if [[ \\\"${{ needs.integration-tests.result }}\\\" != \\\"success\\\" ]]; then\n            echo \\\"integration-tests failed\\\"\n            exit 1\n          fi\n          if [[ \\\"${{ needs.behavioral-tests.result }}\\\" != \\\"success\\\" ]]; then\n            echo \\\"behavioral-tests failed\\\"\n            exit 1\n          fi\n<% } %><% if (enableSecurityScanning) { %>\n          if [[ \\\"${{ needs.security-early-fail.result }}\\\" != \\\"success\\\" ]]; then\n            echo \\\"security-early-fail failed\\\"\n            exit 1\n          fi<% } %><% if (governanceLevel !== 'L1') { %>\n          if [[ \\\"${{ needs.docs-check.result }}\\\" == \\\"failure\\\" ]]; then\n            echo \\\"docs-check failed\\\"\n            exit 1\n          fi<% } %><% if (enableDebtGates) { %>\n          if [[ \\\"${{ needs.debt-gates.result }}\\\" != \\\"success\\\" ]]; then\n            echo \\\"debt-gates failed\\\"\n            exit 1\n          fi\n          if [[ \\\"${{ needs.debt-ratchet.result }}\\\" != \\\"success\\\" ]]; then\n            echo \\\"debt-ratchet failed\\\"\n            exit 1\n          fi<% } %><% if (contractType && contractType !== 'none' && governanceLevel !== 'L1') { %>\n          if [[ \\\"${{ needs.contract-verify.result }}\\\" != \\\"success\\\" ]]; then\n            echo \\\"contract-verify failed\\\"\n            exit 1\n          fi<% } %><% if (_multiLane) { %>\n          if [[ \\\"${{ needs.cross-stack-guard.result }}\\\" == \\\"failure\\\" ]]; then\n            echo \\\"cross-stack-guard failed\\\"\n            exit 1\n          fi<% } %>\n          if [[ \\\"${{ needs.human-approval-required.result }}\\\" == \\\"failure\\\" ]]; then\n            echo \\\"human-approval-required failed — PR needs approved-by-human label\\\"\n            exit 1\n          fi<% } %>\n\"\n\n ❯ __tests__/scripts/gitleaks-install-shape.test.ts:12:17\n     10|   it('ejs template uses RUNNER_TEMP not /usr/local/bin', () => {\n     11|     const ejs = readFileSync('src/templates/github/workflows/01-pr-fas…\n     12|     expect(ejs).toMatch(/RUNNER_TEMP\\/bin/)\n       |                 ^\n     13|     expect(ejs).not.toMatch(/-C \\/usr\\/local\\/bin gitleaks/)\n     14|   })\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/2]⎯\n\n",
  "observed_failure": "FAIL  __tests__/scripts/gitleaks-install-shape.test.ts",
  "recorded_at": "2026-05-21T01:00:15.216Z"
}
