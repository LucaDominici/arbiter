{
  "$schemaVersion": 1,
  "task_id": "#1693",
  "test_path": "__tests__/templates/_weekly-render.test.ts",
  "test_commit_sha": "d1e0e38846e854bdc97128cc7b0fdf6065819a56",
  "test_run_log": "\n RUN  v4.1.9 /home/user/work/repos/arbiter/.claude/worktrees/wf_56b4216a-e9f-17\n\n ❯ __tests__/templates/_weekly-render.test.ts (48 tests | 8 failed) 30ms\n     × 'typescript': runnerProfile=solo adds fuzz + soak-e2e to weekly 5ms\n     × 'java': runnerProfile=solo adds fuzz + soak-e2e to weekly 2ms\n     × 'go': runnerProfile=solo adds fuzz + soak-e2e to weekly 1ms\n     × 'python': runnerProfile=solo adds fuzz + soak-e2e to weekly 1ms\n     × 'rust': runnerProfile=solo adds fuzz + soak-e2e to weekly 1ms\n     × solo: weekly-required needs includes fuzz + soak-e2e 1ms\n     × solo: weekly-required hard-fails (exit 1) when fuzz or soak-e2e fails 1ms\n     × solo: \"File issue on hard failures\" if: is extended with fuzz/soak-e2e OR-terms 1ms\n\n Test Files  1 failed (1)\n      Tests  8 failed | 40 passed (48)\n   Start at  21:30:18\n   Duration  193ms (transform 67ms, setup 12ms, import 73ms, tests 30ms, environment 0ms)\n\n\n\n⎯⎯⎯⎯⎯⎯⎯ Failed Tests 8 ⎯⎯⎯⎯⎯⎯⎯\n\n FAIL  __tests__/templates/_weekly-render.test.ts > _weekly.yml.ejs — runnerProfile axis (#1693, ADR-100) > 'typescript': runnerProfile=solo adds fuzz + soak-e2e to weekly\nAssertionError: expected 'name: Weekly jobs (reusable)\\n\\n\\non:…' to contain 'fuzz:'\n\n- Expected\n+ Received\n\n- fuzz:\n+ name: Weekly jobs (reusable)\n+\n+\n+ on:\n+   workflow_call:\n+\n+ permissions:\n+   contents: read\n+   issues: write\n+\n+ jobs:\n+\n+\n+   dep-freshness:\n+     name: Dependency freshness report\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+       - name: Check outdated packages\n+         run: |\n+           # FAIL-OPEN-INTENT: npm outdated exits 1 when packages are stale; step is informational\n+           npm outdated || true\n+\n+\n+   perf-trend:\n+     name: k6 performance trend\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3\n+       - name: k6 performance smoke vs 7-day baseline\n+         uses: grafana/k6-action@e4714b734f2b0afaabeb7b4a69142745548ab9ec  # v0.3.1\n+         with:\n+           filename: k6/perf-trend.js\n+           flags: '--duration=120s'\n+         continue-on-error: true\n+\n+   action-version-audit:\n+     name: Stale action pin audit\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3\n+       - name: Find SHA-pinned actions older than 180 days\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+         run: |\n+           WORKFLOWS=$(find .github/workflows -name '*.yml' | head -20)\n+           STALE=0\n+           CUTOFF=$(date -d '180 days ago' +%s 2>/dev/null || date -v-180d +%s)\n+           for wf in $WORKFLOWS; do\n+             # FAIL-OPEN-INTENT: grep exits 1 on no matches; empty REFS is valid\n+             REFS=$(grep -oP 'uses: \\S+@[a-f0-9]{40}' \"$wf\" | sed 's/uses: //' || true)\n+             for ref in $REFS; do\n+               ACTION=$(echo \"$ref\" | cut -d@ -f1)\n+               SHA=$(echo \"$ref\" | cut -d@ -f2)\n+               PUSHED=$(gh api \"repos/$ACTION/git/commits/$SHA\" --jq '.committer.date' 2>/dev/null || echo \"\")\n+               if [ -n \"$PUSHED\" ]; then\n+                 PUSHED_EPOCH=$(date -d \"$PUSHED\" +%s 2>/dev/null || date -j -f \"%Y-%m-%dT%H:%M:%SZ\" \"$PUSHED\" +%s)\n+                 if [ \"$PUSHED_EPOCH\" -lt \"$CUTOFF\" ]; then\n+                   echo \"::warning::Stale pin in $wf: $ref (pushed $PUSHED)\"\n+                   STALE=$((STALE + 1))\n+                 fi\n+               fi\n+             done\n+           done\n+           echo \"Stale pins found: $STALE\"\n+\n+   shared-security:\n+     uses: ./.github/workflows/_shared-security.yml\n+     with:\n+       nvd-cache-namespace: weekly\n+\n+   weekly-required:\n+     name: Weekly gate\n+     needs:\n+       - dep-freshness\n+       - perf-trend\n+       - action-version-audit\n+       - shared-security\n+\n+\n+     if: always()\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - name: Weekly gate summary\n+         run: |\n+           echo \"Weekly checks completed.\"\n+           echo \"All jobs ran (some informational, failures create issues).\"\n+\n+       - name: File issue on hard failures\n+         if: needs.dep-freshness.result == 'failure' || needs.action-version-audit.result == 'failure' || needs.shared-security.result == 'failure'\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+           GH_REPO: ${{ github.repository }}\n+         run: |\n+           EXISTING=$(gh issue list \\\n+             --label weekly-regression \\\n+             --state open \\\n+             --limit 1 \\\n+             --json number \\\n+             --repo \"$GH_REPO\" \\\n+             --jq '.[0].number // empty')\n+           if [ -z \"$EXISTING\" ]; then\n+             gh issue create \\\n+               --title \"[weekly] regression detected (${{ github.run_id }})\" \\\n+               --body \"Weekly workflow failed. Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\" \\\n+               --label weekly-regression \\\n+               --repo \"$GH_REPO\"\n+           fi\n+\n\n ❯ __tests__/templates/_weekly-render.test.ts:138:24\n    136|     ({ language, buildTool }) => {\n    137|       const rendered = renderWeeklyPartial({ language, buildTool, runn…\n    138|       expect(rendered).toContain('fuzz:')\n       |                        ^\n    139|       expect(rendered).toContain('soak-e2e:')\n    140|       expect(rendered).toContain('- fuzz')\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/8]⎯\n\n FAIL  __tests__/templates/_weekly-render.test.ts > _weekly.yml.ejs — runnerProfile axis (#1693, ADR-100) > 'java': runnerProfile=solo adds fuzz + soak-e2e to weekly\nAssertionError: expected 'name: Weekly jobs (reusable)\\n\\n\\non:…' to contain 'fuzz:'\n\n- Expected\n+ Received\n\n- fuzz:\n+ name: Weekly jobs (reusable)\n+\n+\n+ on:\n+   workflow_call:\n+\n+ permissions:\n+   contents: read\n+   issues: write\n+\n+ jobs:\n+\n+\n+   dep-freshness:\n+     name: Dependency freshness report\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3\n+\n+       - uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9  # v4\n+         with:\n+           distribution: temurin\n+           java-version: '21'\n+       - name: Check outdated dependencies\n+         run: |\n+\n+           ./gradlew dependencyUpdates || true\n+\n+\n+\n+   perf-trend:\n+     name: k6 performance trend\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3\n+       - name: k6 performance smoke vs 7-day baseline\n+         uses: grafana/k6-action@e4714b734f2b0afaabeb7b4a69142745548ab9ec  # v0.3.1\n+         with:\n+           filename: k6/perf-trend.js\n+           flags: '--duration=120s'\n+         continue-on-error: true\n+\n+   action-version-audit:\n+     name: Stale action pin audit\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3\n+       - name: Find SHA-pinned actions older than 180 days\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+         run: |\n+           WORKFLOWS=$(find .github/workflows -name '*.yml' | head -20)\n+           STALE=0\n+           CUTOFF=$(date -d '180 days ago' +%s 2>/dev/null || date -v-180d +%s)\n+           for wf in $WORKFLOWS; do\n+             # FAIL-OPEN-INTENT: grep exits 1 on no matches; empty REFS is valid\n+             REFS=$(grep -oP 'uses: \\S+@[a-f0-9]{40}' \"$wf\" | sed 's/uses: //' || true)\n+             for ref in $REFS; do\n+               ACTION=$(echo \"$ref\" | cut -d@ -f1)\n+               SHA=$(echo \"$ref\" | cut -d@ -f2)\n+               PUSHED=$(gh api \"repos/$ACTION/git/commits/$SHA\" --jq '.committer.date' 2>/dev/null || echo \"\")\n+               if [ -n \"$PUSHED\" ]; then\n+                 PUSHED_EPOCH=$(date -d \"$PUSHED\" +%s 2>/dev/null || date -j -f \"%Y-%m-%dT%H:%M:%SZ\" \"$PUSHED\" +%s)\n+                 if [ \"$PUSHED_EPOCH\" -lt \"$CUTOFF\" ]; then\n+                   echo \"::warning::Stale pin in $wf: $ref (pushed $PUSHED)\"\n+                   STALE=$((STALE + 1))\n+                 fi\n+               fi\n+             done\n+           done\n+           echo \"Stale pins found: $STALE\"\n+\n+   shared-security:\n+     uses: ./.github/workflows/_shared-security.yml\n+     with:\n+       nvd-cache-namespace: weekly\n+\n+   weekly-required:\n+     name: Weekly gate\n+     needs:\n+       - dep-freshness\n+       - perf-trend\n+       - action-version-audit\n+       - shared-security\n+\n+\n+     if: always()\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - name: Weekly gate summary\n+         run: |\n+           echo \"Weekly checks completed.\"\n+           echo \"All jobs ran (some informational, failures create issues).\"\n+\n+       - name: File issue on hard failures\n+         if: needs.dep-freshness.result == 'failure' || needs.action-version-audit.result == 'failure' || needs.shared-security.result == 'failure'\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+           GH_REPO: ${{ github.repository }}\n+         run: |\n+           EXISTING=$(gh issue list \\\n+             --label weekly-regression \\\n+             --state open \\\n+             --limit 1 \\\n+             --json number \\\n+             --repo \"$GH_REPO\" \\\n+             --jq '.[0].number // empty')\n+           if [ -z \"$EXISTING\" ]; then\n+             gh issue create \\\n+               --title \"[weekly] regression detected (${{ github.run_id }})\" \\\n+               --body \"Weekly workflow failed. Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\" \\\n+               --label weekly-regression \\\n+               --repo \"$GH_REPO\"\n+           fi\n+\n\n ❯ __tests__/templates/_weekly-render.test.ts:138:24\n    136|     ({ language, buildTool }) => {\n    137|       const rendered = renderWeeklyPartial({ language, buildTool, runn…\n    138|       expect(rendered).toContain('fuzz:')\n       |                        ^\n    139|       expect(rendered).toContain('soak-e2e:')\n    140|       expect(rendered).toContain('- fuzz')\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/8]⎯\n\n FAIL  __tests__/templates/_weekly-render.test.ts > _weekly.yml.ejs — runnerProfile axis (#1693, ADR-100) > 'go': runnerProfile=solo adds fuzz + soak-e2e to weekly\nAssertionError: expected 'name: Weekly jobs (reusable)\\n\\n\\non:…' to contain 'fuzz:'\n\n- Expected\n+ Received\n\n- fuzz:\n+ name: Weekly jobs (reusable)\n+\n+\n+ on:\n+   workflow_call:\n+\n+ permissions:\n+   contents: read\n+   issues: write\n+\n+ jobs:\n+\n+\n+   dep-freshness:\n+     name: Dependency freshness report\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3\n+\n+       - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff  # v5\n+         with:\n+           go-version-file: go.mod\n+       - name: Check outdated Go modules\n+         run: go list -u -m all 2>/dev/null | grep '\\[' || true\n+\n+\n+   perf-trend:\n+     name: k6 performance trend\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3\n+       - name: k6 performance smoke vs 7-day baseline\n+         uses: grafana/k6-action@e4714b734f2b0afaabeb7b4a69142745548ab9ec  # v0.3.1\n+         with:\n+           filename: k6/perf-trend.js\n+           flags: '--duration=120s'\n+         continue-on-error: true\n+\n+   action-version-audit:\n+     name: Stale action pin audit\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3\n+       - name: Find SHA-pinned actions older than 180 days\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+         run: |\n+           WORKFLOWS=$(find .github/workflows -name '*.yml' | head -20)\n+           STALE=0\n+           CUTOFF=$(date -d '180 days ago' +%s 2>/dev/null || date -v-180d +%s)\n+           for wf in $WORKFLOWS; do\n+             # FAIL-OPEN-INTENT: grep exits 1 on no matches; empty REFS is valid\n+             REFS=$(grep -oP 'uses: \\S+@[a-f0-9]{40}' \"$wf\" | sed 's/uses: //' || true)\n+             for ref in $REFS; do\n+               ACTION=$(echo \"$ref\" | cut -d@ -f1)\n+               SHA=$(echo \"$ref\" | cut -d@ -f2)\n+               PUSHED=$(gh api \"repos/$ACTION/git/commits/$SHA\" --jq '.committer.date' 2>/dev/null || echo \"\")\n+               if [ -n \"$PUSHED\" ]; then\n+                 PUSHED_EPOCH=$(date -d \"$PUSHED\" +%s 2>/dev/null || date -j -f \"%Y-%m-%dT%H:%M:%SZ\" \"$PUSHED\" +%s)\n+                 if [ \"$PUSHED_EPOCH\" -lt \"$CUTOFF\" ]; then\n+                   echo \"::warning::Stale pin in $wf: $ref (pushed $PUSHED)\"\n+                   STALE=$((STALE + 1))\n+                 fi\n+               fi\n+             done\n+           done\n+           echo \"Stale pins found: $STALE\"\n+\n+   shared-security:\n+     uses: ./.github/workflows/_shared-security.yml\n+     with:\n+       nvd-cache-namespace: weekly\n+\n+   weekly-required:\n+     name: Weekly gate\n+     needs:\n+       - dep-freshness\n+       - perf-trend\n+       - action-version-audit\n+       - shared-security\n+\n+\n+     if: always()\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - name: Weekly gate summary\n+         run: |\n+           echo \"Weekly checks completed.\"\n+           echo \"All jobs ran (some informational, failures create issues).\"\n+\n+       - name: File issue on hard failures\n+         if: needs.dep-freshness.result == 'failure' || needs.action-version-audit.result == 'failure' || needs.shared-security.result == 'failure'\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+           GH_REPO: ${{ github.repository }}\n+         run: |\n+           EXISTING=$(gh issue list \\\n+             --label weekly-regression \\\n+             --state open \\\n+             --limit 1 \\\n+             --json number \\\n+             --repo \"$GH_REPO\" \\\n+             --jq '.[0].number // empty')\n+           if [ -z \"$EXISTING\" ]; then\n+             gh issue create \\\n+               --title \"[weekly] regression detected (${{ github.run_id }})\" \\\n+               --body \"Weekly workflow failed. Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\" \\\n+               --label weekly-regression \\\n+               --repo \"$GH_REPO\"\n+           fi\n+\n\n ❯ __tests__/templates/_weekly-render.test.ts:138:24\n    136|     ({ language, buildTool }) => {\n    137|       const rendered = renderWeeklyPartial({ language, buildTool, runn…\n    138|       expect(rendered).toContain('fuzz:')\n       |                        ^\n    139|       expect(rendered).toContain('soak-e2e:')\n    140|       expect(rendered).toContain('- fuzz')\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/8]⎯\n\n FAIL  __tests__/templates/_weekly-render.test.ts > _weekly.yml.ejs — runnerProfile axis (#1693, ADR-100) > 'python': runnerProfile=solo adds fuzz + soak-e2e to weekly\nAssertionError: expected 'name: Weekly jobs (reusable)\\n\\n\\non:…' to contain 'fuzz:'\n\n- Expected\n+ Received\n\n- fuzz:\n+ name: Weekly jobs (reusable)\n+\n+\n+ on:\n+   workflow_call:\n+\n+ permissions:\n+   contents: read\n+   issues: write\n+\n+ jobs:\n+\n+\n+   dep-freshness:\n+     name: Dependency freshness report\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3\n+\n+       - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5\n+         with:\n+           python-version-file: .python-version\n+       - run: pip install pip-review\n+       - name: Check outdated packages\n+         run: pip-review || true\n+\n+\n+   perf-trend:\n+     name: k6 performance trend\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3\n+       - name: k6 performance smoke vs 7-day baseline\n+         uses: grafana/k6-action@e4714b734f2b0afaabeb7b4a69142745548ab9ec  # v0.3.1\n+         with:\n+           filename: k6/perf-trend.js\n+           flags: '--duration=120s'\n+         continue-on-error: true\n+\n+   action-version-audit:\n+     name: Stale action pin audit\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3\n+       - name: Find SHA-pinned actions older than 180 days\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+         run: |\n+           WORKFLOWS=$(find .github/workflows -name '*.yml' | head -20)\n+           STALE=0\n+           CUTOFF=$(date -d '180 days ago' +%s 2>/dev/null || date -v-180d +%s)\n+           for wf in $WORKFLOWS; do\n+             # FAIL-OPEN-INTENT: grep exits 1 on no matches; empty REFS is valid\n+             REFS=$(grep -oP 'uses: \\S+@[a-f0-9]{40}' \"$wf\" | sed 's/uses: //' || true)\n+             for ref in $REFS; do\n+               ACTION=$(echo \"$ref\" | cut -d@ -f1)\n+               SHA=$(echo \"$ref\" | cut -d@ -f2)\n+               PUSHED=$(gh api \"repos/$ACTION/git/commits/$SHA\" --jq '.committer.date' 2>/dev/null || echo \"\")\n+               if [ -n \"$PUSHED\" ]; then\n+                 PUSHED_EPOCH=$(date -d \"$PUSHED\" +%s 2>/dev/null || date -j -f \"%Y-%m-%dT%H:%M:%SZ\" \"$PUSHED\" +%s)\n+                 if [ \"$PUSHED_EPOCH\" -lt \"$CUTOFF\" ]; then\n+                   echo \"::warning::Stale pin in $wf: $ref (pushed $PUSHED)\"\n+                   STALE=$((STALE + 1))\n+                 fi\n+               fi\n+             done\n+           done\n+           echo \"Stale pins found: $STALE\"\n+\n+   shared-security:\n+     uses: ./.github/workflows/_shared-security.yml\n+     with:\n+       nvd-cache-namespace: weekly\n+\n+   weekly-required:\n+     name: Weekly gate\n+     needs:\n+       - dep-freshness\n+       - perf-trend\n+       - action-version-audit\n+       - shared-security\n+\n+\n+     if: always()\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - name: Weekly gate summary\n+         run: |\n+           echo \"Weekly checks completed.\"\n+           echo \"All jobs ran (some informational, failures create issues).\"\n+\n+       - name: File issue on hard failures\n+         if: needs.dep-freshness.result == 'failure' || needs.action-version-audit.result == 'failure' || needs.shared-security.result == 'failure'\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+           GH_REPO: ${{ github.repository }}\n+         run: |\n+           EXISTING=$(gh issue list \\\n+             --label weekly-regression \\\n+             --state open \\\n+             --limit 1 \\\n+             --json number \\\n+             --repo \"$GH_REPO\" \\\n+             --jq '.[0].number // empty')\n+           if [ -z \"$EXISTING\" ]; then\n+             gh issue create \\\n+               --title \"[weekly] regression detected (${{ github.run_id }})\" \\\n+               --body \"Weekly workflow failed. Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\" \\\n+               --label weekly-regression \\\n+               --repo \"$GH_REPO\"\n+           fi\n+\n\n ❯ __tests__/templates/_weekly-render.test.ts:138:24\n    136|     ({ language, buildTool }) => {\n    137|       const rendered = renderWeeklyPartial({ language, buildTool, runn…\n    138|       expect(rendered).toContain('fuzz:')\n       |                        ^\n    139|       expect(rendered).toContain('soak-e2e:')\n    140|       expect(rendered).toContain('- fuzz')\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/8]⎯\n\n FAIL  __tests__/templates/_weekly-render.test.ts > _weekly.yml.ejs — runnerProfile axis (#1693, ADR-100) > 'rust': runnerProfile=solo adds fuzz + soak-e2e to weekly\nAssertionError: expected 'name: Weekly jobs (reusable)\\n\\n\\non:…' to contain 'fuzz:'\n\n- Expected\n+ Received\n\n- fuzz:\n+ name: Weekly jobs (reusable)\n+\n+\n+ on:\n+   workflow_call:\n+\n+ permissions:\n+   contents: read\n+   issues: write\n+\n+ jobs:\n+\n+\n+   dep-freshness:\n+     name: Dependency freshness report\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3\n+\n+       - name: Install Rust toolchain\n+         uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable\n+       - name: Check outdated crates\n+         run: |\n+           cargo install cargo-outdated --locked || true\n+           cargo outdated || true\n+\n+\n+   perf-trend:\n+     name: k6 performance trend\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3\n+       - name: k6 performance smoke vs 7-day baseline\n+         uses: grafana/k6-action@e4714b734f2b0afaabeb7b4a69142745548ab9ec  # v0.3.1\n+         with:\n+           filename: k6/perf-trend.js\n+           flags: '--duration=120s'\n+         continue-on-error: true\n+\n+   action-version-audit:\n+     name: Stale action pin audit\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3\n+       - name: Find SHA-pinned actions older than 180 days\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+         run: |\n+           WORKFLOWS=$(find .github/workflows -name '*.yml' | head -20)\n+           STALE=0\n+           CUTOFF=$(date -d '180 days ago' +%s 2>/dev/null || date -v-180d +%s)\n+           for wf in $WORKFLOWS; do\n+             # FAIL-OPEN-INTENT: grep exits 1 on no matches; empty REFS is valid\n+             REFS=$(grep -oP 'uses: \\S+@[a-f0-9]{40}' \"$wf\" | sed 's/uses: //' || true)\n+             for ref in $REFS; do\n+               ACTION=$(echo \"$ref\" | cut -d@ -f1)\n+               SHA=$(echo \"$ref\" | cut -d@ -f2)\n+               PUSHED=$(gh api \"repos/$ACTION/git/commits/$SHA\" --jq '.committer.date' 2>/dev/null || echo \"\")\n+               if [ -n \"$PUSHED\" ]; then\n+                 PUSHED_EPOCH=$(date -d \"$PUSHED\" +%s 2>/dev/null || date -j -f \"%Y-%m-%dT%H:%M:%SZ\" \"$PUSHED\" +%s)\n+                 if [ \"$PUSHED_EPOCH\" -lt \"$CUTOFF\" ]; then\n+                   echo \"::warning::Stale pin in $wf: $ref (pushed $PUSHED)\"\n+                   STALE=$((STALE + 1))\n+                 fi\n+               fi\n+             done\n+           done\n+           echo \"Stale pins found: $STALE\"\n+\n+   shared-security:\n+     uses: ./.github/workflows/_shared-security.yml\n+     with:\n+       nvd-cache-namespace: weekly\n+\n+   weekly-required:\n+     name: Weekly gate\n+     needs:\n+       - dep-freshness\n+       - perf-trend\n+       - action-version-audit\n+       - shared-security\n+\n+\n+     if: always()\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - name: Weekly gate summary\n+         run: |\n+           echo \"Weekly checks completed.\"\n+           echo \"All jobs ran (some informational, failures create issues).\"\n+\n+       - name: File issue on hard failures\n+         if: needs.dep-freshness.result == 'failure' || needs.action-version-audit.result == 'failure' || needs.shared-security.result == 'failure'\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+           GH_REPO: ${{ github.repository }}\n+         run: |\n+           EXISTING=$(gh issue list \\\n+             --label weekly-regression \\\n+             --state open \\\n+             --limit 1 \\\n+             --json number \\\n+             --repo \"$GH_REPO\" \\\n+             --jq '.[0].number // empty')\n+           if [ -z \"$EXISTING\" ]; then\n+             gh issue create \\\n+               --title \"[weekly] regression detected (${{ github.run_id }})\" \\\n+               --body \"Weekly workflow failed. Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\" \\\n+               --label weekly-regression \\\n+               --repo \"$GH_REPO\"\n+           fi\n+\n\n ❯ __tests__/templates/_weekly-render.test.ts:138:24\n    136|     ({ language, buildTool }) => {\n    137|       const rendered = renderWeeklyPartial({ language, buildTool, runn…\n    138|       expect(rendered).toContain('fuzz:')\n       |                        ^\n    139|       expect(rendered).toContain('soak-e2e:')\n    140|       expect(rendered).toContain('- fuzz')\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/8]⎯\n\n FAIL  __tests__/templates/_weekly-render.test.ts > _weekly.yml.ejs — runnerProfile axis (#1693, ADR-100) > solo: weekly-required needs includes fuzz + soak-e2e\nAssertionError: expected '      - dep-freshness\\n      - perf-t…' to contain '- fuzz'\n\n- Expected\n+ Received\n\n- - fuzz\n+       - dep-freshness\n+       - perf-trend\n+       - action-version-audit\n+       - shared-security\n+\n\n ❯ __tests__/templates/_weekly-render.test.ts:155:28\n    153|     const needsMatch = jobSlice.match(/needs:\\s*\\n((?:\\s+-[^\\n]*\\n?)*)…\n    154|     expect(needsMatch).not.toBeNull()\n    155|     expect(needsMatch![1]).toContain('- fuzz')\n       |                            ^\n    156|     expect(needsMatch![1]).toContain('- soak-e2e')\n    157|   })\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/8]⎯\n\n FAIL  __tests__/templates/_weekly-render.test.ts > _weekly.yml.ejs — runnerProfile axis (#1693, ADR-100) > solo: weekly-required hard-fails (exit 1) when fuzz or soak-e2e fails\nAssertionError: expected 'name: Weekly jobs (reusable)\\n\\n\\non:…' to contain 'needs.fuzz.result'\n\n- Expected\n+ Received\n\n- needs.fuzz.result\n+ name: Weekly jobs (reusable)\n+\n+\n+ on:\n+   workflow_call:\n+\n+ permissions:\n+   contents: read\n+   issues: write\n+\n+ jobs:\n+\n+\n+   dep-freshness:\n+     name: Dependency freshness report\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3\n+\n+       - uses: ./.github/actions/setup-node-pnpm\n+       - name: Check outdated packages\n+         run: |\n+           # FAIL-OPEN-INTENT: npm outdated exits 1 when packages are stale; step is informational\n+           npm outdated || true\n+\n+\n+   perf-trend:\n+     name: k6 performance trend\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3\n+       - name: k6 performance smoke vs 7-day baseline\n+         uses: grafana/k6-action@e4714b734f2b0afaabeb7b4a69142745548ab9ec  # v0.3.1\n+         with:\n+           filename: k6/perf-trend.js\n+           flags: '--duration=120s'\n+         continue-on-error: true\n+\n+   action-version-audit:\n+     name: Stale action pin audit\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3\n+       - name: Find SHA-pinned actions older than 180 days\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+         run: |\n+           WORKFLOWS=$(find .github/workflows -name '*.yml' | head -20)\n+           STALE=0\n+           CUTOFF=$(date -d '180 days ago' +%s 2>/dev/null || date -v-180d +%s)\n+           for wf in $WORKFLOWS; do\n+             # FAIL-OPEN-INTENT: grep exits 1 on no matches; empty REFS is valid\n+             REFS=$(grep -oP 'uses: \\S+@[a-f0-9]{40}' \"$wf\" | sed 's/uses: //' || true)\n+             for ref in $REFS; do\n+               ACTION=$(echo \"$ref\" | cut -d@ -f1)\n+               SHA=$(echo \"$ref\" | cut -d@ -f2)\n+               PUSHED=$(gh api \"repos/$ACTION/git/commits/$SHA\" --jq '.committer.date' 2>/dev/null || echo \"\")\n+               if [ -n \"$PUSHED\" ]; then\n+                 PUSHED_EPOCH=$(date -d \"$PUSHED\" +%s 2>/dev/null || date -j -f \"%Y-%m-%dT%H:%M:%SZ\" \"$PUSHED\" +%s)\n+                 if [ \"$PUSHED_EPOCH\" -lt \"$CUTOFF\" ]; then\n+                   echo \"::warning::Stale pin in $wf: $ref (pushed $PUSHED)\"\n+                   STALE=$((STALE + 1))\n+                 fi\n+               fi\n+             done\n+           done\n+           echo \"Stale pins found: $STALE\"\n+\n+   shared-security:\n+     uses: ./.github/workflows/_shared-security.yml\n+     with:\n+       nvd-cache-namespace: weekly\n+\n+   weekly-required:\n+     name: Weekly gate\n+     needs:\n+       - dep-freshness\n+       - perf-trend\n+       - action-version-audit\n+       - shared-security\n+\n+\n+     if: always()\n+     runs-on: ${{ fromJSON(vars.RUNNER_LABELS_TEST || '[\"ubuntu-latest\"]') }}\n+     timeout-minutes: 60\n+     steps:\n+       - name: Weekly gate summary\n+         run: |\n+           echo \"Weekly checks completed.\"\n+           echo \"All jobs ran (some informational, failures create issues).\"\n+\n+       - name: File issue on hard failures\n+         if: needs.dep-freshness.result == 'failure' || needs.action-version-audit.result == 'failure' || needs.shared-security.result == 'failure'\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+           GH_REPO: ${{ github.repository }}\n+         run: |\n+           EXISTING=$(gh issue list \\\n+             --label weekly-regression \\\n+             --state open \\\n+             --limit 1 \\\n+             --json number \\\n+             --repo \"$GH_REPO\" \\\n+             --jq '.[0].number // empty')\n+           if [ -z \"$EXISTING\" ]; then\n+             gh issue create \\\n+               --title \"[weekly] regression detected (${{ github.run_id }})\" \\\n+               --body \"Weekly workflow failed. Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\" \\\n+               --label weekly-regression \\\n+               --repo \"$GH_REPO\"\n+           fi\n+\n\n ❯ __tests__/templates/_weekly-render.test.ts:167:22\n    165|       runnerProfile: 'solo',\n    166|     })\n    167|     expect(rendered).toContain('needs.fuzz.result')\n       |                      ^\n    168|     expect(rendered).toContain('needs.soak-e2e.result')\n    169|     expect(rendered).toMatch(/exit 1/)\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[7/8]⎯\n\n FAIL  __tests__/templates/_weekly-render.test.ts > _weekly.yml.ejs — runnerProfile axis (#1693, ADR-100) > solo: \"File issue on hard failures\" if: is extended with fuzz/soak-e2e OR-terms\nAssertionError: expected 'File issue on hard failures\\n        …' to contain 'needs.fuzz.result == \\'failure\\''\n\n- Expected\n+ Received\n\n- needs.fuzz.result == 'failure'\n+ File issue on hard failures\n+         if: needs.dep-freshness.result == 'failure' || needs.action-version-audit.result == 'failure' || needs.shared-security.result == 'failure'\n+         env:\n+           GH_TOKEN: ${{ github.token }}\n+           GH_REPO: ${{ github.repository }}\n+         run: |\n+           EXISTING=$(gh issue list \\\n+             --label weekly-regression \\\n+             --state open \\\n+          \n\n ❯ __tests__/templates/_weekly-render.test.ts:184:20\n    182|     const idx = rendered.indexOf('File issue on hard failures')\n    183|     const ifLine = rendered.slice(idx, idx + 400)\n    184|     expect(ifLine).toContain(\"needs.fuzz.result == 'failure'\")\n       |                    ^\n    185|     expect(ifLine).toContain(\"needs.soak-e2e.result == 'failure'\")\n    186|   })\n\n⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[8/8]⎯\n\n",
  "observed_failure": "FAIL  __tests__/templates/_weekly-render.test.ts",
  "recorded_at": "2026-07-01T19:30:18.890Z"
}
