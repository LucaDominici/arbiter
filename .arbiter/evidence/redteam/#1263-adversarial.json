{
  "schemaVersion": "AdversarialVerifierV1",
  "taskId": "#1263",
  "phase": "refactor",
  "role": "adversarial-verifier (inline — no Task spawn in lane)",
  "checks": [
    {"id": "AV-01", "claim": "--batch flag is wired end-to-end into the CLI", "method": "node dist/cli.js ship --help | grep batch", "result": "PASS — flag rendered with description + #1263 ref"},
    {"id": "AV-02", "claim": "batch run produces a report file + per-issue isolation", "method": "node dist/cli.js ship --batch '111,222,333' --dir <tmp>; inspect batch-report-<date>.json", "result": "PASS — 3 issues, all isolated as blocked (default seam unwired), loop completed, report written"},
    {"id": "AV-03", "claim": "invalid issue input is rejected", "method": "ship --batch '1,abc'", "result": "PASS — throws 'invalid issue id \"abc\"'"},
    {"id": "AV-04", "claim": "no dead/unused exports introduced", "method": "npx knip --include exports | grep batch", "result": "PASS — none; todayIso de-exported, defaultIssueRunner covered by test"},
    {"id": "AV-05", "claim": "no circular dependency in src/batch/", "method": "npx madge --circular src/batch/", "result": "PASS — pure core has no import of the adapter"},
    {"id": "AV-06", "claim": "single-id ship path is regression-free", "method": "node dist/cli.js ship 1263 --tier XS (no --batch)", "result": "PASS — existing step-output path unchanged; --batch branch only taken when flag present"},
    {"id": "AV-07", "claim": "the defensive malformed-status branch in runBatch is reachable (not dead)", "method": "test 'a runner that returns a blocked outcome (no throw)'", "result": "PASS — blocked-return path counted, else-branch exercised"},
    {"id": "AV-08", "claim": "per-issue STOP isolation: a mid-batch throw never aborts the batch", "method": "batch.test.ts 'a throw on a middle issue does NOT abort the batch'", "result": "PASS — #A/#C shipped, #B blocked-with-reason, all 3 present"}
  ],
  "verdict": "VERIFIED",
  "note": "CLI flag wiring confirmed end-to-end against the built dist; no dead code; the clean-context sub-agent spawn is a documented seam (defaultIssueRunner) owned by the harness, not exercised by tests by design (no live LLM). Scope frozen to #1263 — no dispatch-matrix (#1267) or autonomy-levels (#1258) logic built (tie-in comment only)."
}
