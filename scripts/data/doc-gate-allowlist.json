{
  "$schemaVersion": 1,
  "description": "#2408: dated per-file excuses for the three doc gates (check-doc-links, check-doc-style, check-phantom-command-scan). An entry is only legitimate when the real fix is a prose or product decision owned by another issue — a path/link/frontmatter one-liner gets fixed, never listed. Every entry expires; the gate goes RED the day after, so the debt cannot rot silently. Rules: doc-links | doc-style | phantom-command.",
  "entries": [
    {
      "path": "src/templates/governance/qa-audit-phases.md.ejs",
      "rule": "phantom-command",
      "reason": "The emitted governance playbook documents `arbiter audit run` and says so explicitly (\"planned, not yet shipped\"). Excusing the file also keeps `audit` out of the emitted-surface ledger, which would otherwise demand a row for a command that deliberately does not exist.",
      "issue": "#2415",
      "expires": "2026-09-25"
    },
    {
      "path": "docs/REFERENCE/gdpr-overlay.md",
      "rule": "phantom-command",
      "reason": "Cites `node scripts/check-gdpr-controls.mjs`, which is an overlay-conditional gate emitted into the CONSUMER's tree — correctly absent from arbiter's own scripts/. Do not create the file; the fix is resolving emitted-doc citations against the emitted tree.",
      "issue": "#2415",
      "expires": "2026-09-25"
    }
  ]
}
