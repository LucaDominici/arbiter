# <%= projectName %> — Cursor Rules

> **Governance rules are in `AGENTS.md` (canonical, AAIF standard).**
> This file adds Cursor-specific configuration only.

---

## Governance

All invariants, coding standards, testing policy, commit conventions, and branch strategy
are defined in [`AGENTS.md`](../AGENTS.md). Read it before making any changes.

## Stack

| Fact | Value |
|------|-------|
| **Language** | <%= language %><%= framework ? ` / ${framework}` : '' %> |
| **Build** | `<%= buildCommand %>` |
| **Test** | `<%= testCommand %>` |
| **Gate** | `./scripts/check-all.sh` |

## Cursor-Specific Rules

- Always read `AGENTS.md` before starting a task
- Follow the commit convention defined in `AGENTS.md` §Commit Convention
- Run `./scripts/check-all.sh L1` before committing
- Branch naming: `task/#NNN-short-description`
- Do not edit files listed in `.cursorignore` (if present)

## Quick Commands

```bash
# Gate
./scripts/check-all.sh L1   # pre-commit
./scripts/check-all.sh L2   # pre-push (matches CI)

# Build
<%= buildCommand %>

# Test
<%= testCommand %>
```
