// Generated by arbiter — Spring Boot ONLY.
// This template hardcodes @SpringBootTest, @LocalServerPort and org.springframework.* imports.
// Emission is gated by archunit.ts on framework ∈ {spring-boot, *spring*} (#491).
// Quarkus / Micronaut / unknown-framework Java projects do NOT receive this file.
// For Quarkus, use @QuarkusTest + @QuarkusTestResource; for Micronaut, use @MicronautTest.
package <%= basePackage ? basePackage + '.support' : 'support' %>;

import io.restassured.RestAssured;
import org.junit.jupiter.api.BeforeEach;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.web.server.LocalServerPort;
import org.springframework.test.context.DynamicPropertyRegistry;
import org.springframework.test.context.DynamicPropertySource;
import org.testcontainers.containers.PostgreSQLContainer;
import org.testcontainers.junit.jupiter.Container;
import org.testcontainers.junit.jupiter.Testcontainers;

/**
 * Base class for all integration tests — generated by arbiter (M22, hexagonal).
 *
 * Provides:
 *   - PostgreSQL via Testcontainers (shared container across all subclasses)
 *   - RestAssured base URI wired to the random server port
 *   - {@link #authHeader(String)} helper — replace with your auth flavor
 *     (OAuth2 token endpoint, mTLS cert loader, or remove if auth-free)
 *
 * Generated once by arbiter (skipIfExists). Edit freely after generation.
 */
@Testcontainers
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
public abstract class RestAssuredBaseIT {

    @Container
    protected static final PostgreSQLContainer<?> postgres =
        new PostgreSQLContainer<>("postgres:16-alpine")
            .withDatabaseName("testdb")
            .withUsername("test")
            .withPassword("test");

    @DynamicPropertySource
    static void overrideDataSourceProperties(DynamicPropertyRegistry registry) {
        registry.add("spring.datasource.url", postgres::getJdbcUrl);
        registry.add("spring.datasource.username", postgres::getUsername);
        registry.add("spring.datasource.password", postgres::getPassword);
    }

    @LocalServerPort
    protected int port;

    @BeforeEach
    void configureRestAssured() {
        RestAssured.port = port;
        RestAssured.basePath = "";
    }

    /**
     * Returns a Bearer token header value for the given subject.
     * Replace this implementation with your project's auth mechanism.
     */
    protected String authHeader(String subject) {
        // TODO(#70): replace with real JWT/OAuth2 helper for your auth flavor
        throw new UnsupportedOperationException(
            "RestAssuredBaseIT.authHeader() is a stub — implement for your auth provider."
        );
    }
}
