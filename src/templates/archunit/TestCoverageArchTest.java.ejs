package <%= basePackage ? basePackage + '.architecture' : 'architecture' %>;

import com.tngtech.archunit.junit.AnalyzeClasses;
import com.tngtech.archunit.junit.ArchTest;
import com.tngtech.archunit.lang.ArchRule;

import static com.tngtech.archunit.lang.syntax.ArchRuleDefinition.noClasses;

/**
 * Test coverage enforcement — generated by arbiter (M22, hexagonal).
 *
 * Ensures integration test classes (*ControllerIT) reside in the correct package.
 * Prevents stray IT tests from being placed outside the adapter.in.web layer.
 *
 * MVP: enforces correct placement. For completeness enforcement (every Controller
 * must have an IT), replace with a custom ArchCondition — see AGENTS.md.
 *
 * Generated by arbiter. Edit the template to change enforcement.
 */
@AnalyzeClasses(packages = "<%= basePackage ? basePackage : '' %>")
public class TestCoverageArchTest {

    @ArchTest
    public static final ArchRule every_controller_must_have_integration_test =
        noClasses()
            .that().haveSimpleNameEndingWith("ControllerIT")
            .should().resideOutsideOfPackage("<%= basePackage ? basePackage + '.adapter.in.web..' : 'adapter.in.web..' %>")
            .because("Integration tests for web controllers must reside in the adapter.in.web package (test coverage placement enforcement).");
}
