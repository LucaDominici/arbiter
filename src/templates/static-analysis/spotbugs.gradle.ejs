// spotbugs.gradle — generated by arbiter
// Task report configuration only. The plugin declaration AND the extension
// config (effort/reportLevel/excludeFilter) live in the root build's
// arbiter-managed block: applied scripts cannot declare plugins (Gradle forbids
// the plugins DSL here) and cannot see the plugin classpath either
// (script-plugin classloader isolation — the Effort/Confidence enums do not
// resolve from this file).
// Re-generate: arbiter update

spotbugsMain {
    reports {
        xml {
            required = true
            outputLocation = layout.buildDirectory.file('reports/spotbugs/main.xml')
        }
        html { required = false }
    }
}
