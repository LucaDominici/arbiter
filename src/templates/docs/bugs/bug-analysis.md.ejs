# Bug Analysis — <%= projectName %>

_Generated by arbiter. Structured root-cause analysis. The goal is to move from
"it's broken" to a single, evidenced root cause **before** writing any fix._

> Companion to the auto-captured `.evidence/<task>/DEBUG_STATE.md` written by the
> `debug-state-on-failure` hook when a gate/test command fails. Pull its
> Command / Output / History into the sections below.

## 1. Reproduction (confirmed)

- [ ] Reproduced locally on a clean checkout
- [ ] Minimal reproduction isolated (smallest input that triggers it)

```
# minimal repro command / input
```

## 2. Hypotheses

List candidate causes, then rule each in or out with evidence. Do not stop at the
first plausible one.

| # | Hypothesis | Evidence for | Evidence against | Verdict |
| - | ---------- | ------------ | ---------------- | ------- |
| 1 | _…_        | _…_          | _…_              | ruled in/out |
| 2 | _…_        | _…_          | _…_              | ruled in/out |

## 3. Root Cause

State the single root cause, supported by the evidence above. Distinguish the
**root cause** (why it happened) from the **symptom** (what the user saw). A fix
that only addresses the symptom is a stop condition — record tech debt or fix the
cause.

## 4. Blast Radius

- Other code paths that share this root cause: _…_
- Tests that *should* have caught it but didn't: _…_ (this gap is part of the fix)

## 5. Fix Strategy

- Root-cause fix: _…_
- Regression test to add first (TDD red): _path + assertion_
- Out of scope: _…_

→ Once fixed, verify via `docs/bugs/bug-verification.md`.
