# <%= title %>

## Pyramid

<!-- The test levels this project uses (unit/integration/e2e/contract) and roughly what share of
     coverage each owns. Link the real ratio source (e.g. test-pyramid.json) if one exists. -->

## Coverage policy

<!-- What must be covered before merge (new code, critical paths) and what is intentionally not. -->

## Gate mapping

<!-- Which test level runs at which gate (pre-push / PR / nightly / release) and why. -->
