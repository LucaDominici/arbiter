      - name: macOS — restore DOCKER_CONFIG
        if: runner.os == 'macOS' && always()
        run: |
          if [ -n "${DOCKER_CONFIG:-}" ] && [ -d "${DOCKER_CONFIG}" ]; then
            rm -rf "${DOCKER_CONFIG}"
          fi
