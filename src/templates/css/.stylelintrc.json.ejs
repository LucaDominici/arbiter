{
  "_comment_design_tokens": "#352 — HARD design-token enforcement for ts-frontend-spa. Hex colors and unit-zero are migration-don't-extend violations. Migrate to var(--color-*) in src/styles/theme.css; add legacy file paths to a documented allowlist with rationale before granting an exemption.",
  "rules": {
    "color-no-hex": [
      true,
      {
        "message": "Use var(--color-*) from src/styles/theme.css instead of a raw hex color. See docs/PRODUCT/UI-UX-STANDARDS.md."
      }
    ],
    "length-zero-no-unit": true,
    "custom-property-no-missing-var-function": true
  }
}
