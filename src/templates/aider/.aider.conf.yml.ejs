# <%= projectName %> — Aider configuration
# Governance rules are in AGENTS.md (canonical, AAIF standard).
# Read AGENTS.md before each coding session.

# model: gpt-4o
# model: claude-3-5-sonnet-20241022

# Convention map — must-follow rules (full list in AGENTS.md)
convention:
  - Read AGENTS.md before any coding session
  - Follow commit convention: type(#NNN): summary
  - Branch naming: task/#NNN-short-description
  - Run node scripts/check-all.mjs L1 before committing
  - Run node scripts/check-all.mjs L2 before pushing

# Project stack
# Language: <%= language %><%= framework ? ` / ${framework}` : '' %>
# Build:    <%= buildCommand %>
# Test:     <%= testCommand %>
# Lint:     <%= lintCommand %>

auto-commits: false
dirty-commits: false
read:
  - AGENTS.md
