// eslint.config.playwright.mjs — generated by arbiter (M27, ESLint v9 flat-config).
// Run in isolation by check-all.mjs via
// `eslint --config eslint.config.playwright.mjs --no-config-lookup src/**/*.spec.ts` so it
// enforces ONLY the Playwright quality rules, independent of your main eslint.config.mjs.
// The legacy .eslintrc-playwright.json is retained for tooling that still reads
// eslintrc-format config but is NOT used by the gate (ESLint v9 removed
// --no-eslintrc/-c, which broke this check on every project that installed the
// plugin — #1491-class fix, mirrors eslint.config.static.mjs).
// Requires: npm install -D eslint-plugin-playwright
import tseslint from 'typescript-eslint'
import playwright from 'eslint-plugin-playwright'

export default [
  {
    languageOptions: {
      parser: tseslint.parser,
      ecmaVersion: 2022,
      sourceType: 'module',
    },
    plugins: { playwright },
    rules: {
      ...playwright.configs['flat/recommended'].rules,
      'playwright/no-force-option': 'error',
      'playwright/no-wait-for-timeout': 'error',
      'playwright/no-page-pause': 'error',
      'playwright/prefer-web-first-assertions': 'warn',
    },
  },
]
