# Commands Catalog

_Generated by arbiter for **<%= projectName %>**. Update `arbiter.json` to reflect changes._

## Development Commands

| Purpose | Command                |
| ------- | ---------------------- |
| Build   | `<%= buildCommand %>`  |
| Test    | `<%= testCommand %>`   |
| Lint    | `<%= lintCommand %>`   |
| Format  | `<%= formatCommand %>` |

## Gate Commands

| Purpose                 | Command                                  |
| ----------------------- | ---------------------------------------- |
| Local preflight         | `node scripts/check-all.mjs preflight`  |
| Full gate (CI authority) | `node scripts/check-all.mjs L2`         |

> Run targeted checks while editing. The pre-push hook runs one local preflight on the frozen
> candidate; CI runs the full gate on that exact SHA. Record its verdict instead of repeating either.
