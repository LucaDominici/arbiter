# KIT Canonical Dimensions Reference

<%= dims.length %> security and quality dimensions from the KIT framework.
Generated by `arbiter kit generate`. Per-dimension detail lives under `coverage/`.

| ID | TML | Gate | Status | Name |
|---|---|---|---|---|
<% for (const dim of dims) { -%>
| [<%= dim.id %>](coverage/<%= dim.filename %>) | <%= dim.tml %> | <%= dim.gate %> | <%= dim.status %> | <%= dim.name %> |
<% } %>
