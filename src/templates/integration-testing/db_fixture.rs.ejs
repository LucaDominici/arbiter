// Integration test database fixture — generated by arbiter (M26, INV-34)
// File: tests/db_fixture.rs
//
// sqlx::test automatically provisions a fresh PostgreSQL database per test
// when DATABASE_URL is set. Run integration tests with:
//
//   DATABASE_URL=postgres://user:pass@localhost/testdb cargo test
//
// With Testcontainers-rs (optional):
//   use testcontainers::{clients, images::postgres};

/// Set up environment for sqlx database tests.
/// DATABASE_URL must point to a real PostgreSQL instance.
pub fn require_database_url() -> String {
    std::env::var("DATABASE_URL").unwrap_or_else(|_| {
        panic!(
            "DATABASE_URL must be set for integration tests (INV-34). \
             Use Testcontainers or a local PostgreSQL instance."
        )
    })
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn database_url_env_is_configured() {
        let url = require_database_url();
        assert!(url.starts_with("postgres"), "DATABASE_URL must be a PostgreSQL connection string");
    }
}
