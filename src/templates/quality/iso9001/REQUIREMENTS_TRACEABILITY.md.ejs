<%# ISO 9001 §8.3 requirement→test RTM — mirrors the FEATURE_MATRIX RTM schema (#1253). %>
# Requirements Traceability Matrix (RTM) — ISO 9001 §8.3 / §8.5

_Generated by arbiter for **<%= projectName %>** (governance **<%= governanceLevel %>**).
The quality-process overlay's single authoritative requirement→test record. Each quality
requirement traces to the test that verifies it. Update as your implementation matures._

> **Gate:** `scripts/check-iso9001.mjs` (fail-closed). Validates this RTM, the
> document-control register, and the CAPA log on every run.
>
> Reuses the FEATURE_MATRIX RTM machinery: same sentinel-delimited table and the same
> `status` ladder so the requirement→test chain is gate-checkable, not just prose.

---

## Status vocabulary

| Status    | Meaning                              | Required fields                     |
| --------- | ------------------------------------ | ----------------------------------- |
| `Missing` | No verification — tracked by issue   | `issue_ref` in `#NNN` format        |
| `Partial` | Code exists; test incomplete         | `code_ref` non-empty                |
| `Done`    | Requirement verified by a test       | `code_ref` + `test_ref` non-empty   |

Promotion is fail-closed (cannot skip a step).

This RTM shares its schema with FEATURE_MATRIX.md but does not carry the optional
`source_ref` (upward anchor) column or the `test_ref` glob-ban ratchet added there for
arbiter #2163 — both are validated by `scripts/check-feature-matrix.mjs`, not
`scripts/check-iso9001.mjs`. A project that wants upward traceability and glob-ban
enforcement on this RTM's own rows should adopt those rules directly in
`FEATURE_MATRIX.md`; retrofitting them into this ISO 9001 overlay's separate gate is a
future, separately-scoped change.

---

<!-- ISO9001_RTM_START -->
| requirement_id | requirement | code_ref | test_ref | status | issue_ref | note |
|---|---|---|---|---|---|---|
| QR-001 | Customer requirements captured and reviewed (§8.2) | | | Missing | #1 | Replace with your requirements-review ref |
| QR-002 | Design & development verification (§8.3) | | | Missing | #2 | Replace with your design-verification ref |
| QR-003 | Nonconformity detection & control (§8.7) | | | Missing | #3 | Replace with your nonconformity ref |
<!-- ISO9001_RTM_END -->

---

## Summary

| Status | Count |
|---|---|
| Done | 0 |
| Partial | 0 |
| Missing | 3 |
| **Total** | **3** |

---

## Version history

| doc_version | Date | Changes |
|---|---|---|
| 1.0.0 | Initial | Scaffolded by arbiter at <%= governanceLevel %> |
