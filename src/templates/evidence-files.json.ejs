{
  "version": 1,
  "_comment": "Defines which files are load-bearing for done evidence (SHA-pinning). Generated by arbiter.",
  "pin_dirs": [<% if (language === 'typescript') { %>"src", "__tests__"<% } else if (language === 'java') { %>"src/main/java", "src/test/java"<% } else if (language === 'python') { %>"src", "tests"<% } else if (language === 'rust') { %>"src", "tests"<% } else if (language === 'go') { %>"."<% } else { %>"src"<% } %>],
  "pin_extensions": [<% if (language === 'typescript') { %>".ts", ".tsx", ".mjs", ".js"<% } else if (language === 'java') { %>".java"<% } else if (language === 'python') { %>".py"<% } else if (language === 'rust') { %>".rs"<% } else if (language === 'go') { %>".go"<% } else { %>".ts"<% } %>],
  "exclude_dirs": ["node_modules", "dist", "build", "coverage", ".git", "target", "vendor", "__pycache__", ".venv"],
  "reality_contact": {
    "archetype": "<%- archetype %>",
    "required": <% if (archetype === 'backend-web-db' || archetype === 'frontend-spa') { %>true<% } else { %>false<% } %>,
    "suite": "<% if (archetype === 'frontend-spa') { %>render-smoke<% } else { %>live-api-e2e<% } %>",
    "command": <% if (archetype === 'backend-web-db') { %>"./tests/api/run.sh"<% } else if (archetype === 'frontend-spa') { %>["node", "scripts/lib/ephemeral-server.mjs", "--start", "npm run start:test", "--test", "npx playwright test tests/e2e/render-smoke.spec.ts", "--port", "4173"]<% } else { %>""<% } %>
  }
}
