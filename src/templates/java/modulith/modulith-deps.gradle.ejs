// modulith-deps.gradle — generated by arbiter (#1886)
// Wired automatically: applied from the root build's arbiter-managed block.
// Re-generate: arbiter update
//
// Pinned to the Spring Modulith 1.4.x line, which targets Spring Boot 3.x /
// Spring Framework 6 (Spring Modulith 2.x requires Spring Boot 4 / Framework 7 —
// see https://github.com/spring-projects/spring-modulith/issues/971). arbiter
// detects "spring-boot" in the build file without resolving the exact Boot
// version, so the 1.4.x line is the safe default for the common case.

dependencies {
    testImplementation 'org.springframework.modulith:spring-modulith-starter-test:1.4.12'
}
