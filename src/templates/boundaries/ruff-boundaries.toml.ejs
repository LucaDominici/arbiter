# ruff-boundaries.toml — generated by arbiter (M22d)
# Domain-purity enforcement via ruff's flake8-tidy-imports banned-api (TID251).
# Run: ruff check --config ruff-boundaries.toml src/domain/
# Only the `src/domain/` target path is ever passed to ruff with this config,
# so the banned-api list applies exclusively to domain code.

target-version = "py310"

[lint]
select = ["TID251"]

[lint.flake8-tidy-imports.banned-api]
"django".msg = "framework module: not allowed in hexagonal domain"
"django.db".msg = "framework module: not allowed in hexagonal domain"
"flask".msg = "framework module: not allowed in hexagonal domain"
"fastapi".msg = "framework module: not allowed in hexagonal domain"
"fastapi.routing".msg = "framework module: not allowed in hexagonal domain"
"starlette".msg = "framework module: not allowed in hexagonal domain"
"sqlalchemy".msg = "framework module: not allowed in hexagonal domain"
"sqlalchemy.orm".msg = "framework module: not allowed in hexagonal domain"
"sqlalchemy.ext.asyncio".msg = "framework module: not allowed in hexagonal domain"
"tortoise".msg = "framework module: not allowed in hexagonal domain"
"peewee".msg = "framework module: not allowed in hexagonal domain"
"pymongo".msg = "framework module: not allowed in hexagonal domain"
"redis".msg = "framework module: not allowed in hexagonal domain"
"requests".msg = "framework module: not allowed in hexagonal domain"
"httpx".msg = "framework module: not allowed in hexagonal domain"
"aiohttp".msg = "framework module: not allowed in hexagonal domain"
"celery".msg = "framework module: not allowed in hexagonal domain"
"boto3".msg = "framework module: not allowed in hexagonal domain"
"psycopg".msg = "framework module: not allowed in hexagonal domain"
"asyncpg".msg = "framework module: not allowed in hexagonal domain"
