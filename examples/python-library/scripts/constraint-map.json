{
  "//": "INV-115 constraint-scan map (scripts/check-constraint-scan.mjs). Curate an entry",
  "//1": "for every free-text prohibition in AGENTS.md / CANON.md / CLAUDE.md, so the scan",
  "//2": "reports it COVERED or ACCEPTED instead of leaving it in the UNENFORCEABLE backlog.",
  "//3": "Naming a non-existent enforcer is MAP-FICTION and fails the gate — only add an",
  "//4": "entry once the enforcer actually exists.",
  "//5": "Shape: \"<token>\": { \"kind\": \"gate|hook|githook|script|inv|lint|template\", \"enforcer\": \"<ref>\" }",
  "//6": "  gate     — enforcer is a check referenced in scripts/check-all.mjs",
  "//7": "  hook     — enforcer is a file under .claude/hooks/",
  "//8": "  githook  — enforcer is a file under .githooks/ (e.g. pre-commit)",
  "//9": "  script   — enforcer is a script file that exists on disk",
  "//10": "  inv      — enforcer is an invariant id (e.g. INV-04) present in the catalog",
  "//11": "  lint     — enforcer is a rule id active in an eslint config",
  "//12": "  template — enforcer is a path under src/templates/",
  "//13": "A prohibition with no grep-able token is keyed by its verbatim governance line:",
  "//14": "  \"prose:- Skip the gate before committing\": { \"kind\": \"githook\", \"enforcer\": \"pre-commit\" }",
  "//15": "A prose entry matching no line is MAP-DEAD (the map cannot rot silently).",
  "//16": "A judgment-level rule no mechanism can carry is triaged, not hidden:",
  "//17": "  \"prose:<line>\": { \"kind\": \"accepted\", \"rationale\": \"<>=40 chars of why>\" }",
  "//18": "An accepted entry without that rationale is MAP-INVALID and fails the gate.",
  "//19": "Coverage is ratcheted against scripts/data/constraint-coverage-baseline.json;",
  "//20": "seed or tighten it with --update-baseline (it refuses to record a regression).",
  "//21": "Example (uncomment and adapt once the enforcer exists):",
  "//22": "  \"any\": { \"kind\": \"lint\", \"enforcer\": \"@typescript-eslint/no-explicit-any\" }"
}
