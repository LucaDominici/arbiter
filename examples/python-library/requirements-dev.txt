# Arbiter-generated — dev/test toolchain the generated gate (check-all.mjs)
# invokes. Install with:  python -m pip install -r requirements-dev.txt
#
# L1 (fast) gate: ruff (lint+format) and pytest (unit tests).
# L2 (full) gate additionally exercises coverage and the pytest-bdd behavioural
# suite. pytest-bdd is L2-only — the L1 unit run skips the BDD module cleanly when
# it is absent (see tests/bdd/test_example_bdd.py), so L1 is green without it.
ruff>=0.4
pytest>=8.0
pytest-cov>=5.0
pytest-bdd>=7.0
