# .env.example — arbiter local CI environment variables for go-library
# Generated by arbiter (INV-87). Copy to .env and edit as needed.
# .env is gitignored; .env.example is committed.

# ARBITER_LEVEL — governance level for local gate runs (L1, L2, L3).
# Default: L1 (fast local checks only).
# ARBITER_LEVEL=L1

# ARBITER_EVIDENCE_DIR — path to evidence artifact directory.
# Default: .arbiter/evidence
# ARBITER_EVIDENCE_DIR=.arbiter/evidence

# NODE_ENV — Node.js environment string.
# Not set by default (enables development behavior).
# NODE_ENV=production
