---
'@arbiter/cli': minor
---

feat(#721 #722): generate MCP fallback and safe batch-execution governance rules for target projects. Two new rule files are emitted by `generateClaude`: `45-mcp-fallback.md` (approved fallback equivalents when MCP tools are unavailable) and `50-batch-execution.md` (parallel agent safety contract with anti-rot checklist).
