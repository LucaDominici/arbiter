# Agent Lifecycle Rule

When creating, modifying, or removing sub-agents:

1. Update the agent file in `.claude/agents/<name>.md`
2. Update this rules file if the agent affects always-loaded rules
3. Document the change in `docs/SYSTEM/DECISIONS.md` if the change is architectural
